import assert from"node:assert/strict";import{retryFinalRecovery}from"../ai/MovieMentorRecoveryFinalizationRetryControl.js";
const fp="a".repeat(64),id={incidentId:"cat-1",recoveryEpoch:29,authorityDecisionReference:"decision-A",authorityReference:"root-A",bindingFingerprint:fp,previousStateReference:"state-28",transitionReference:"transition-29"};
const A={valid:true,authoritative:true,recoveryEpoch:29,clockEpoch:7,clockSequence:900,provenanceEpoch:14,authorityDecisionReference:"decision-A",bindingFingerprint:fp,reference:"snapshot-A",verificationReference:"verify-A"};
const B={...A,reference:"snapshot-B",verificationReference:"verify-B"};
const proof=(type,h=A)=>({valid:true,incidentId:id.incidentId,recoveryEpoch:29,authorityDecisionReference:id.authorityDecisionReference,bindingFingerprint:fp,clockEpoch:h.clockEpoch,clockSequence:h.clockSequence,provenanceEpoch:h.provenanceEpoch,freshnessSnapshotReference:h.reference,freshnessVerificationReference:h.verificationReference,reference:`${type}-absence`,...(type==="transaction"?{safeToRetry:true,retryMustUseSameIdentity:true}:{absent:true,authoritative:true,quorum:true})});
async function run(tx,gate,h,commit){let reads=0;const r=await retryFinalRecovery({originalIdentity:id,retryIdentity:{...id},absenceProof:tx,gateAbsenceProof:gate,currentHighWatermark:h},{commitFinalRecoveryRetry:commit||async()=>{throw Error("CAS must not run")},readFinalRecoveryByIdentity:async()=>{reads++;return{}}});return{r,reads}}
let x=await run(proof("transaction",A),proof("gate",A),B);assert.equal(x.r.retried,false);assert.ok(x.r.reasons.includes("transaction_absence_proof_freshness_snapshot_mismatch"));assert.equal(x.reads,0);
x=await run(proof("transaction",A),proof("gate",B),A);assert.equal(x.r.retried,false);assert.ok(x.r.reasons.includes("gate_absence_proof_freshness_snapshot_mismatch"));assert.ok(x.r.reasons.includes("absence_proofs_freshness_snapshot_diverged"));
x=await run({...proof("transaction",A),freshnessVerificationReference:"verify-B"},proof("gate",A),A);assert.equal(x.r.retried,false);assert.ok(x.r.reasons.includes("transaction_absence_proof_freshness_verification_mismatch"));
let mutations=0;x=await run(proof("transaction",A),proof("gate",A),A,async({freshnessHighWatermarkReference,freshnessVerificationReference,requirements})=>{assert.equal(freshnessHighWatermarkReference,"snapshot-A");assert.equal(freshnessVerificationReference,"verify-A");assert.equal(requirements.recheckFreshnessSnapshotReferenceAtMutation,true);assert.equal(requirements.recheckFreshnessVerificationReferenceAtMutation,true);return{committed:false,indeterminate:false,reason:"freshness_snapshot_lineage_rotated_at_retry_cas"}});assert.equal(x.r.retried,false);assert.ok(x.r.reasons.includes("freshness_snapshot_lineage_rotated_at_retry_cas"));assert.equal(mutations,0);assert.equal(x.reads,0);
// A storage layer claiming commit without the new lineage comparisons is indeterminate, never success.
x=await run(proof("transaction",A),proof("gate",A),A,async()=>({committed:true,identityCompared:true,previousStateReferenceCompared:true,authorityHighWatermarkCompared:true,absenceProofIdentityBindingsCompared:true,proofFreshnessHighWatermarkCompared:true,proofFreshnessSnapshotLineageCompared:false,noExistingMatchingCommitCompared:true,noUnresolvedRetryGateCompared:true,transactionAbsenceRechecked:true,gateAbsenceRechecked:true,recoveryEpochRechecked:true,signedClockRechecked:true,provenanceEpochRechecked:true,authorityDecisionRechecked:true,freshnessSnapshotReferenceRechecked:false,freshnessVerificationReferenceRechecked:false,sameAtomicTransaction:true,reference:"lying-commit"}));assert.equal(x.r.retried,false);assert.equal(x.r.indeterminate,true);assert.ok(x.r.reasons.includes("final_retry_freshness_cas_fence_incomplete"));assert.equal(x.reads,0);
console.log("SNAPSHOT LINEAGE TORTURE PASSED: internally valid snapshot B cannot replace snapshot A after proof minting; mixed A/B proofs fail; verification lineage substitution fails; CAS-time snapshot rotation yields zero retry mutation; incomplete lineage fencing is indeterminate.");