import assert from"node:assert/strict";import{createEffectIntent,createEffectReceipt,validateEffectIntent,validateEffectReceipt,reconcileDistributedEffect,evaluateRetryPermission}from"../ai/MovieMentorOperationsDistributedEffectReconciliation.js";
const expected={incidentId:"inc-1",requestId:"req-1",executionId:"exec-1",operationType:"recovery",actionId:"restart-worker",scopeFingerprint:"scope-1",parametersHash:"params-1"};const intent=createEffectIntent({...expected,effectId:"effect-1",idempotencyKey:"idem-1",provider:"provider-a",preparedAt:"2026-08-24T13:30:00Z"});assert.equal(validateEffectIntent(intent,expected).valid,true);const receipt=createEffectReceipt(intent,{providerEffectId:"provider-effect-1",providerIdempotencyKey:"idem-1",outcome:"completed",observedAt:"2026-08-24T13:30:01Z",receiptReference:"receipt-1"});assert.equal(validateEffectReceipt(intent,receipt).valid,true);
let r=await reconcileDistributedEffect({intent,providerReceipt:receipt},{verifyProviderReceipt:async()=>({valid:true,reference:"proof-1"})});assert.equal(r.resolved,true);assert.equal(r.effectApplied,true);assert.equal(evaluateRetryPermission(r).permitted,false);
const tampered=structuredClone(receipt);tampered.outcome="not-applied";assert.equal(validateEffectReceipt(intent,tampered).valid,false);const wrongKey=createEffectReceipt(intent,{providerIdempotencyKey:"idem-evil",outcome:"completed",receiptReference:"receipt-2"});assert.equal(validateEffectReceipt(intent,wrongKey).valid,false);
r=await reconcileDistributedEffect({intent},{queryProviderEffect:async()=>({applied:true,reference:"provider-query-1"})});assert.equal(r.state,"reconciled-applied");assert.equal(evaluateRetryPermission(r).permitted,false);
r=await reconcileDistributedEffect({intent},{queryProviderEffect:async()=>({applied:false,authoritative:true,reference:"provider-query-2"})});assert.equal(r.state,"reconciled-not-applied");assert.equal(evaluateRetryPermission(r).permitted,true);
r=await reconcileDistributedEffect({intent},{queryProviderEffect:async()=>({applied:false,authoritative:false})});assert.equal(r.state,"indeterminate");assert.equal(evaluateRetryPermission(r).permitted,false);
r=await reconcileDistributedEffect({intent},{});assert.equal(r.state,"indeterminate");assert.equal(evaluateRetryPermission(r).permitted,false);const badIntent=structuredClone(intent);badIntent.idempotencyKey="idem-2";assert.equal(validateEffectIntent(badIntent,expected).valid,false);console.log("Movie Mentor Operations distributed effect reconciliation verification passed.");
