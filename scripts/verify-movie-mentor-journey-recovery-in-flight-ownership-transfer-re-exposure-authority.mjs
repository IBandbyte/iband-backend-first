import assert from "node:assert/strict";
import { createMovieMentorJourneyRecoveryExpressRouter } from "../ai/MovieMentorJourneyRecoveryExpressRouterFactory.js";
console.log("5A.31 — Journey recovery in-flight ownership transfer re-exposure authority");
function makeResponse(){return{statusCode:200,body:null,exposures:0,forbidden:0,status(code){this.statusCode=code;return this;},json(body){this.body=body;if(body?.success===true)this.exposures+=1;if(this.statusCode===403&&body?.success===false)this.forbidden+=1;return this;}};}
async function run({transferBeforeExposure=false}={}){let currentOwner={principalId:"creator-A",ownershipRef:"ownership-A",ownershipRevision:7};let publicationCompleted=false,authorizeCalls=0;const requestAuthority={async authorize(){authorizeCalls+=1;return{authorized:true,principalId:currentOwner.principalId,projectId:"project-1",ownershipRef:currentOwner.ownershipRef,ownershipRevision:currentOwner.ownershipRevision,authenticationSource:"adversarial-verifier",authorizationSource:"adversarial-verifier"};}};const publicationBoundary={async publish(){publicationCompleted=true;return{status:"published",recoveryStatus:"created",projectId:"project-1",principalId:"creator-A",ownershipRef:"ownership-A",ownershipRevision:7,authenticationSource:"adversarial-verifier",authorizationSource:"adversarial-verifier",recoveryRevision:1,recoveryGeneration:1,recoveryReference:"recovery-1",recoveryFingerprint:"recovery-fp-1",lineageId:"lineage-1",authorityGeneration:4,progressionRevision:2,envelopeFingerprint:"env-fp",capturedAt:"2026-09-06T18:00:00.000Z"};}};const router=createMovieMentorJourneyRecoveryExpressRouter({verifyCredential:async()=>({}),expectedIssuer:"issuer",expectedAudience:"audience",createRequestAuthority:()=>requestAuthority,createPublicationBoundary:()=>publicationBoundary,createHttpAdapter:({publicationBoundary})=>({async handle({request,projectId}){const publication=await publicationBoundary.publish({request,projectId,expectedRecoveryRevision:0,envelope:{}});assert.equal(publication.principalId,"creator-A");assert.equal(publication.ownershipRef,"ownership-A");assert.equal(publication.ownershipRevision,7);if(transferBeforeExposure&&publicationCompleted)currentOwner={principalId:"creator-B",ownershipRef:"ownership-B",ownershipRevision:8};return{statusCode:200,authorityBinding:{principalId:publication.principalId,projectId:publication.projectId,ownershipRef:publication.ownershipRef,ownershipRevision:publication.ownershipRevision},body:{success:true,status:publication.recoveryStatus,projectId:publication.projectId,recoveryRevision:publication.recoveryRevision,recoveryGeneration:publication.recoveryGeneration}};}})});const layer=router.stack.find(entry=>entry.route?.path==="/:projectId/recovery");assert.ok(layer);const res=makeResponse();await layer.route.stack[0].handle({params:{projectId:"project-1"},body:{expectedRecoveryRevision:0,envelope:{}},headers:{authorization:"Bearer creator-A-token"}},res);return{res,authorizeCalls};}
{const{res,authorizeCalls}=await run({transferBeforeExposure:false});assert.equal(res.exposures,1);assert.equal(res.statusCode,200);assert.equal(authorizeCalls,1);}
{const{res,authorizeCalls}=await run({transferBeforeExposure:true});assert.equal(res.exposures,0);assert.equal(res.statusCode,403);assert.equal(res.forbidden,1);assert.equal(authorizeCalls,1);}
console.log("PASS Journey recovery in-flight ownership transfer re-exposure authority.");
console.log("LAW: current authority must match the principal/project/reference/revision universe that earned the result.");
