import assert from"node:assert/strict";import{retryFinalRecovery}from"../ai/MovieMentorRecoveryFinalizationRetryControl.js";
const rrh="c".repeat(64),identity={incidentId:"cat-1",recoveryEpoch:29,authorityDecisionReference:"decision-cat1-e29",authorityReference:"recovery-root-B",bindingFingerprint:"a".repeat(64),previousStateReference:"distributed-state-cat1-e29",transitionReference:"global:decision-cat1-e29"};
const hwm={valid:true,authoritative:true,recoveryEpoch:29,clockEpoch:7,clockSequence:900,provenanceEpoch:14,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,reference:"snapshot-current",verificationReference:"verify-current"};
const common={clockEpoch:7,clockSequence:900,provenanceEpoch:14,freshnessSnapshotReference:hwm.reference,freshnessVerificationReference:hwm.verificationReference};
const tx=(o={})=>({valid:true,safeToRetry:true,retryMustUseSameIdentity:true,reference:"tx-absence-current",incidentId:identity.incidentId,recoveryEpoch:identity.recoveryEpoch,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,...common,...o});
const gate=(o={})=>({valid:true,absent:true,authoritative:true,quorum:true,reference:"gate-absence-current",incidentId:identity.incidentId,recoveryEpoch:identity.recoveryEpoch,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,...common,...o});
async function deny(absenceProof,gateAbsenceProof,reason,{hash=rrh,highWatermark=hwm}={}){let attempts=0;const r=await retryFinalRecovery({originalIdentity:identity,retryIdentity:structuredClone(identity),absenceProof,gateAbsenceProof,currentHighWatermark:highWatermark,reconciliationRealityHash:hash},{commitFinalRecoveryRetry:async()=>{attempts++;throw new Error("RECYCLED PROOF MUST NEVER REACH CAS")},readFinalRecoveryByIdentity:async()=>{throw new Error("NO READBACK")}});assert.equal(r.retried,false);assert.ok(r.reasons.includes(reason),`${reason}:${r.reasons}`);assert.equal(attempts,0)}
await deny(tx({incidentId:"cat-2"}),gate(),"transaction_absence_proof_identity_mismatch");await deny(tx({recoveryEpoch:28}),gate(),"transaction_absence_proof_identity_mismatch");await deny(tx({authorityDecisionReference:"decision-cat1-e28"}),gate(),"transaction_absence_proof_identity_mismatch");await deny(tx({bindingFingerprint:"b".repeat(64)}),gate(),"transaction_absence_proof_identity_mismatch");
await deny(tx(),gate({incidentId:"cat-2"}),"gate_absence_proof_identity_mismatch");await deny(tx(),gate({recoveryEpoch:28}),"gate_absence_proof_identity_mismatch");await deny(tx(),gate({authorityDecisionReference:"decision-cat1-e28"}),"gate_absence_proof_identity_mismatch");await deny(tx(),gate({bindingFingerprint:"b".repeat(64)}),"gate_absence_proof_identity_mismatch");
await deny(tx({recoveryEpoch:28,reference:"tx-genuine-e28"}),gate(),"transaction_absence_proof_identity_mismatch");await deny(tx(),gate({recoveryEpoch:28,reference:"gate-genuine-e28"}),"gate_absence_proof_identity_mismatch");await deny(tx({incidentId:"cat-2",reference:"tx-genuine-cat2"}),gate({incidentId:"cat-3",reference:"gate-genuine-cat3"}),"transaction_absence_proof_identity_mismatch");
// Fresh proofs bound to a different certified snapshot are recycled evidence too.
await deny(tx({freshnessSnapshotReference:"snapshot-old"}),gate(),"transaction_absence_proof_freshness_snapshot_mismatch");await deny(tx(),gate({freshnessVerificationReference:"verify-old"}),"gate_absence_proof_freshness_verification_mismatch");
// Missing reconciliation universe is rejected before storage, even when both absence proofs are otherwise current.
await deny(tx(),gate(),"reconciliation_recovery_reality_hash_required",{hash:""});
let attempts=0;let r=await retryFinalRecovery({originalIdentity:identity,retryIdentity:{...identity,authorityReference:"recovery-root-evil"},absenceProof:tx(),gateAbsenceProof:gate(),currentHighWatermark:hwm,reconciliationRealityHash:rrh},{commitFinalRecoveryRetry:async()=>{attempts++},readFinalRecoveryByIdentity:async()=>({})});assert.equal(r.retried,false);assert.ok(r.reasons.includes("final_retry_identity_drift_denied"));assert.equal(attempts,0);
console.log("PROOF RECYCLING BIN PASSED v1.6: incident/epoch/Authority/digest/snapshot-lineage proof recycling and missing reconciliation reality are non-transferable; every recycled combination dies before CAS.");