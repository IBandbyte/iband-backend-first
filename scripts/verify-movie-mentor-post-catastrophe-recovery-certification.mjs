import assert from"node:assert/strict";import{certifyPostCatastropheRecovery}from"../ai/MovieMentorPostCatastropheRecoveryCertification.js";
const creator={generation:28,fingerprint:"creator-28-fingerprint",authorityReference:"merge-112"},atomic={committed:true,indeterminate:false,reference:"atomic-after-phone-reconnect"},soul={verified:true,reference:"soul-after-merge-112"},intent={valid:true,allBranchesIncluded:true,creatorIntentPreserved:true,reference:"intent-laptop-phone-112"},replay={valid:true,same_operation_replay_denied:true,reference:"replay-denied-cap-cat-28"},ledger={valid:true,incidentClosed:true,allEffectsReconciled:true,reference:"ledger-cat-1-closed"},verify=async()=>({authoritative:true,...creator,reference:"authoritative-creator-28"}),args={incidentId:"cat-1",atomicMutationResult:atomic,postMutationSoul:soul,noLostIntentProof:intent,replayProtectionProof:replay,ledgerClosureProof:ledger,expectedCreatorState:creator};
let r=await certifyPostCatastropheRecovery(args,{verifyAuthoritativeCreatorState:verify});assert.equal(r.certified,true);assert.equal(r.status,"recovered");assert.equal(r.creatorState.authorityReference,"merge-112");
for(const patch of[{atomicMutationResult:{...atomic,committed:false}},{atomicMutationResult:{...atomic,indeterminate:true}},{postMutationSoul:{verified:false,reference:"x"}},{noLostIntentProof:{...intent,allBranchesIncluded:false}},{noLostIntentProof:{...intent,creatorIntentPreserved:false}},{replayProtectionProof:{...replay,same_operation_replay_denied:false}},{ledgerClosureProof:{...ledger,incidentClosed:false}},{ledgerClosureProof:{...ledger,allEffectsReconciled:false}},{expectedCreatorState:{...creator,authorityReference:""}}]){r=await certifyPostCatastropheRecovery({...args,...patch},{verifyAuthoritativeCreatorState:verify});assert.equal(r.certified,false);assert.equal(r.status,"reconciling")}
for(const current of[{authoritative:false,...creator,reference:"x"},{authoritative:true,...creator,generation:29,reference:"x"},{authoritative:true,...creator,fingerprint:"changed",reference:"x"},{authoritative:true,...creator,authorityReference:"merge-112-other-timeline",reference:"x"},{authoritative:true,...creator,reference:""}]){r=await certifyPostCatastropheRecovery(args,{verifyAuthoritativeCreatorState:async()=>current});assert.equal(r.certified,false);assert.equal(r.status,"reconciling");assert.deepEqual(r.reasons,["post_catastrophe_creator_truth_not_current"])}
r=await certifyPostCatastropheRecovery(args,{});assert.equal(r.certified,false);assert.equal(r.status,"reconciling");r=await certifyPostCatastropheRecovery(args,{verifyAuthoritativeCreatorState:async()=>{throw new Error("temporal read unavailable")}});assert.equal(r.certified,false);assert.equal(r.status,"reconciling");
// Time-dilation attack: every completion proof is valid for generation 28, but authoritative creator truth advances to generation 29 before final certification. Recovery must remain open rather than certify a historical truth.
let live={...creator};r=await certifyPostCatastropheRecovery(args,{verifyAuthoritativeCreatorState:async()=>{live={generation:29,fingerprint:"creator-29-fingerprint",authorityReference:"merge-113"};return{authoritative:true,...live,reference:"future-head-29"}}});assert.equal(r.certified,false);assert.equal(r.status,"reconciling");
console.log("Movie Mentor post-catastrophe certification verification passed: infrastructure recovery, Soul, intent preservation, replay denial and ledger closure cannot certify RECOVERED after creator truth advances or changes lineage.");