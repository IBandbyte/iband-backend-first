import assert from "node:assert/strict";import fs from "node:fs";import {createMovieMentorInferenceSpendAuthority} from "../ai/MovieMentorInferenceSpendAuthority.js";
const binding={authenticated:true,projectAuthorized:true,principalId:"creator-1",projectId:"project-1"};function memoryStore(){let state="reserved";return{reserve:async q=>({granted:true,reservation:{...q,status:"reserved",entitlementRevision:2}}),settle:async q=>{if(state!=="reserved"&&state!==q.outcome){const e=new Error("conflict");e.code="MOVIE_MENTOR_INFERENCE_SPEND_SETTLEMENT_CONFLICT";throw e;}const idempotent=state===q.outcome;state=q.outcome;return{settled:true,idempotent,reservation:{reservationId:q.reservationId,principalId:q.principalId,projectId:"project-1",operation:"movie-mentor-turn",units:1,entitlementRevision:3,status:state}};},state:()=>state};}
{const store=memoryStore(),authority=createMovieMentorInferenceSpendAuthority({store,createReservationId:()=>"r1"}),reservation=await authority.reserveTurn({serverAuthority:binding,projectId:"project-1"});const first=await authority.settleTurn({reservation,outcome:"consumed"});const retry=await authority.settleTurn({reservation,outcome:"consumed"});assert.equal(first.outcome,"consumed");assert.equal(retry.idempotent,true);await assert.rejects(()=>authority.settleTurn({reservation,outcome:"released"}),e=>e.code==="MOVIE_MENTOR_INFERENCE_SPEND_SETTLEMENT_CONFLICT");}
const storeSource=fs.readFileSync(new URL("../ai/MovieMentorInferenceSpendMongoStore.js",import.meta.url),"utf8"),runtimeSource=fs.readFileSync(new URL("../ai/MovieMentorTurnRuntime.js",import.meta.url),"utf8"),resultSource=fs.readFileSync(new URL("../ai/MovieMentorCanonicalResultAuthority.js",import.meta.url),"utf8");assert.match(storeSource,/enum:\["reserved","consumed","released"\]/);assert.match(storeSource,/reservedUnits:-durable\.units,consumedUnits:durable\.units/);assert.match(storeSource,/reservedUnits:-durable\.units,remainingUnits:durable\.units/);assert.match(storeSource,/withTransaction/);assert.match(runtimeSource,/outcome:"released"/);assert.doesNotMatch(runtimeSource,/outcome:"consumed"/);assert.match(runtimeSource,/commitCanonicalResult/);assert.match(runtimeSource,/settlement:"reserved"/);assert.match(resultSource,/reservationId:current\.reservationId/);assert.ok(runtimeSource.indexOf("reserveTurn")<runtimeSource.indexOf("const result=await orchestrate("));assert.ok(runtimeSource.indexOf("const result=await orchestrate(")<runtimeSource.lastIndexOf("commitCanonicalResult"));console.log("PASS Door 5A.8 regression — spend settlement remains atomic/idempotent, while 5A.24 removes process-local success as consume authority: successful live inference remains RESERVED until immutable canonical result evidence can drive deterministic settlement reconciliation.");
