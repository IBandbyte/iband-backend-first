import assert from "node:assert/strict";
import {readFileSync} from "node:fs";
import {createMovieMentorInferenceExecutionLeaseAuthority} from "../ai/MovieMentorInferenceExecutionLeaseAuthority.js";
import {createFencedInferenceOrchestrationDeps} from "../ai/MovieMentorTurnRuntime.js";
import {createMovieMentorProductionInferenceExecutionComposition} from "../ai/MovieMentorProductionInferenceExecutionComposition.js";
const clone=v=>v==null?v:structuredClone(v);let clock=new Date("2031-01-01T00:00:00.000Z"),durable=null,id=0;
const store={getStatus:()=>({configured:true,readiness:"injected-proven",durable:true,cas:"reservation-binding-active-closure-frozen-universe-provider-reality-revision-finalized-result-binding-and-atomic-abort"}),async readExecution(executionId){return durable?.executionId===executionId?clone(durable):null;},async readExecutionByCreatorTurn({principalId,projectId,creatorTurnId}={}){return durable&&durable.principalId===principalId&&durable.projectId===projectId&&durable.creatorTurnId===creatorTurnId?clone(durable):null;},async createExecution(next){if(durable)return null;durable={domain:"iband.movie-mentor.inference-execution-store",schema:6,...clone(next)};return clone(durable);},async replaceExecution(next,expected={}){if(!durable||durable.phase!==expected.expectedPhase||durable.leaseGeneration!==expected.expectedLeaseGeneration||durable.leaseReference!==expected.expectedLeaseReference)return null;if(expected.expectedLeaseExpiresAt&&durable.leaseExpiresAt!==expected.expectedLeaseExpiresAt)return null;durable=clone(next);return clone(durable);},async claimProviderCall(input={}){if(!durable||durable.executionId!==input.executionId)return{claimed:false,execution:null,existingProviderCall:null};const existing=durable.providerCalls.find(call=>call.slotId===input.slotId)||null;const live=durable.schema===6&&durable.phase==="active"&&durable.ownerId===input.ownerId&&durable.leaseGeneration===input.leaseGeneration&&durable.leaseReference===input.leaseReference&&durable.fencingToken===input.fencingToken&&new Date(durable.leaseExpiresAt)>new Date(input.admittedAt);if(existing||!live||durable.providerCallsClaimed>=durable.maxProviderCalls)return{claimed:false,execution:clone(durable),existingProviderCall:clone(existing)};const providerCall={providerCallId:input.providerCallId,slotId:input.slotId,task:input.task,state:"admitted",leaseGeneration:input.leaseGeneration,leaseReference:input.leaseReference,fencingToken:input.fencingToken,admittedAt:input.admittedAt};durable.providerCalls.push(providerCall);durable.providerCallsClaimed+=1;return{claimed:true,execution:clone(durable),providerCall:clone(providerCall)};},async beginClosing(){return null;},async recoverExpiredIntoClosing(){return null;},async completeClosing(){return null;},async quarantineExecution(){return null;}};
const authority=createMovieMentorInferenceExecutionLeaseAuthority({store,now:()=>new Date(clock),leaseMs:1000,maxProviderCalls:2,randomId:()=>`id-${++id}`});const binding={creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"digest-1",ownerId:"worker-A",maxProviderCalls:2};
console.log("5A.24 Round Eight — execution owner-proof plus historical-input mount torture");const g1=await authority.openExecution(binding);assert.equal(g1.authorized,true);assert.equal(g1.schema,6);assert.equal(g1.leaseGeneration,1);const semantic=await authority.claimProviderCall({execution:g1,slotId:"semantic",task:"movie-mentor-semantic"});assert.equal(semantic.dispatchAuthorized,true);const duplicate=await authority.claimProviderCall({execution:g1,slotId:"semantic",task:"movie-mentor-semantic"});assert.equal(duplicate.dispatchAuthorized,false);assert.equal(duplicate.reason,"provider-call-slot-already-admitted");clock=new Date(clock.getTime()+1001);const g2=await authority.acquireExecution({executionId:g1.executionId,ownerId:"worker-B"});assert.equal(g2.authorized,true);assert.equal(g2.schema,6);assert.equal(g2.leaseGeneration,2);const zombie=await authority.claimProviderCall({execution:g1,slotId:"story",task:"movie-mentor-specialist:story"});assert.equal(zombie.dispatchAuthorized,false);assert.equal(zombie.reason,"execution-lease-fenced");const story=await authority.claimProviderCall({execution:g2,slotId:"story",task:"movie-mentor-specialist:story"});assert.equal(story.dispatchAuthorized,true);const exhausted=await authority.claimProviderCall({execution:g2,slotId:"synthesis",task:"movie-mentor-synthesis"});assert.equal(exhausted.dispatchAuthorized,false);assert.equal(exhausted.reason,"provider-call-budget-exhausted");
for(const candidate of[{store},{store:{...store,getStatus:undefined}},{store:{...store,getStatus:()=>({configured:true,readiness:"injected"})}}]){const rejected=createMovieMentorProductionInferenceExecutionComposition(candidate);assert.equal(rejected.ready,false);assert.equal(rejected.reason,"inference-execution-external-store-provenance-not-owned");}
const provenEffectStore={getStatus:()=>({configured:true,readiness:"injected-proven",cas:"revision",crossLedgerSerialization:"execution-providerEffectRealityRevision"}),readEffect:async()=>null,beginUnknown:async()=>null,appendEvidence:async()=>null};const rejectedResult=createMovieMentorProductionInferenceExecutionComposition({store,effectStore:provenEffectStore,resultStore:{readByExecution:async()=>null,readByCreatorTurn:async()=>null,commit:async()=>null}});assert.equal(rejectedResult.ready,false);assert.equal(rejectedResult.reason,"inference-execution-external-store-provenance-not-owned");const rejectedCandidate=createMovieMentorProductionInferenceExecutionComposition({store,effectStore:provenEffectStore,candidateStore:{stageCandidate:async()=>null,readByExecution:async()=>null}});assert.equal(rejectedCandidate.ready,false);assert.equal(rejectedCandidate.reason,"inference-execution-external-store-provenance-not-owned");
const serverSource=readFileSync(new URL("../server.js",import.meta.url),"utf8");assert.match(serverSource,/const EXECUTION_DOMAIN="iband\.movie-mentor\.production-inference-execution-composition"/);assert.match(serverSource,/function executionCompositionProven\(composition,status\)/);assert.match(serverSource,/status===composition\?\.status/);assert.match(serverSource,/status\?\.authority===composition\?\.authority/);assert.match(serverSource,/status\?\.storeStatus===composition\?\.storeStatus/);assert.match(serverSource,/status\?\.effectStoreStatus===composition\?\.effectStoreStatus/);assert.match(serverSource,/status\?\.resultStoreStatus===composition\?\.resultStoreStatus/);assert.match(serverSource,/status\?\.candidateStoreStatus===composition\?\.candidateStoreStatus/);assert.match(serverSource,/status\?\.fullExecutionAuthority===true/);assert.ok(serverSource.indexOf("!executionCompositionProven(executionComposition,executionStatus)")<serverSource.indexOf("createMovieMentorTurnRouter({requestAuthority"));
const slots=[],executed=[],dispatches=[],evidence=[],inputBindings=[];const fakeExecution={authorized:true,schema:6,executionAuthorized:true,executionId:"execution-live",ownerId:"owner-live",leaseGeneration:7,leaseReference:"lease-7",fencingToken:"fence-7"};const fakeAuthority={async claimProviderCall({slotId,task}){slots.push(slotId);return{authorized:true,dispatchAuthorized:true,providerCallId:`call-${slotId}`,slotId,task,executionId:"execution-live",ownerId:"owner-live",leaseGeneration:7,leaseReference:"lease-7",fencingToken:"fence-7"};},async bindProviderReconstructionInput({providerCall,reconstructionInput}){inputBindings.push(providerCall.slotId);dispatches.push(`bind:${providerCall.slotId}`);return{authorized:true,inputBound:true,providerCallId:providerCall.providerCallId,reconstructionInput:clone(reconstructionInput)};},async beginProviderDispatch({providerCall}){dispatches.push(`begin:${providerCall.slotId}`);assert.ok(inputBindings.includes(providerCall.slotId));return{authorized:true,dispatchAuthorized:true,providerCallId:providerCall.providerCallId};},async assertProviderDispatch({providerCall}){dispatches.push(`assert:${providerCall.slotId}`);return{authorized:true,dispatchAuthorized:true,providerCallId:providerCall.providerCallId};},async contributeProviderEffectEvidence(input){evidence.push(input);return{authorized:true,recorded:true};}};const fenced=createFencedInferenceOrchestrationDeps({execution:fakeExecution,inferenceExecutionAuthority:fakeAuthority,deps:{interpretSemantics:async()=>{executed.push("semantic");return{ok:true};},prepareContinuityHistoricalInput:async workOrder=>clone(workOrder),executeSpecialistWorkOrder:async workOrder=>{executed.push(workOrder.agentId);return{contribution:{agentId:workOrder.agentId},metadata:{}};},synthesizeResponse:async()=>{executed.push("synthesis");return{success:true,text:"ok"};}}});await fenced.interpretSemantics({});const planResult=await fenced.executeSpecialistPlan({workOrders:[{agentId:"story"},{agentId:"character"},{agentId:"continuity"}]});assert.equal(planResult.status,"completed",JSON.stringify(planResult.failures));await fenced.synthesizeResponse({});assert.deepEqual(slots,["semantic","story","character","continuity","synthesis"]);assert.deepEqual(executed,slots);assert.deepEqual(inputBindings,slots);assert.deepEqual(dispatches,["bind:semantic","begin:semantic","assert:semantic","bind:story","begin:story","assert:story","bind:character","begin:character","assert:character","bind:continuity","begin:continuity","assert:continuity","bind:synthesis","begin:synthesis","assert:synthesis"]);assert.equal(evidence.length,0);
let escaped=false,deniedBindings=0;const denied=createFencedInferenceOrchestrationDeps({execution:fakeExecution,inferenceExecutionAuthority:{async claimProviderCall(){return{authorized:false,dispatchAuthorized:false,reason:"execution-lease-fenced"};},async bindProviderReconstructionInput(){deniedBindings+=1;throw new Error("must not bind");},async beginProviderDispatch(){throw new Error("must not dispatch");},async assertProviderDispatch(){throw new Error("must not fence");},async contributeProviderEffectEvidence(){throw new Error("must not evidence");}},deps:{interpretSemantics:async()=>{escaped=true;return{};}}});await assert.rejects(()=>denied.interpretSemantics({}),error=>error.code==="MOVIE_MENTOR_INFERENCE_PROVIDER_CALL_NOT_AUTHORIZED");assert.equal(escaped,false);assert.equal(deniedBindings,0);
console.log("✓ provider-call claim is durable and slot-bounded");console.log("✓ duplicate logical slot cannot mint second dispatch authority");console.log("✓ generation takeover fences zombie provider execution");console.log("✓ provider-call budget is enforced at admission");console.log("✓ externally injected stores receive zero production provenance credit");console.log("✓ production execution composition may mint owner proof only from production stores");console.log("✓ server consumes exact production execution owner proof before creator router construction");console.log("✓ all provider slots bind historical input before UNKNOWN and fresh dispatch fence");console.log("LAW: PRODUCTION-OWNED DURABLE STORES → PRODUCTION EXECUTION COMPOSITION OWNS ONE STABLE PROOF → SERVER CONSUMES THAT EXACT OWNER PROOF → ROUTE");console.log("LAW: READY + METHOD SHAPE + SELF-ATTESTED STATUS IS NOT PRODUCTION AUTHORITY. PROOF DOES NOT TELEPORT.");console.log("LAW: NO RECOVERABLE PROVIDER SLOT MAY CROSS UNKNOWN WITHOUT ITS EXACT TASK INPUT ALREADY DURABLE.");console.log("5A.24 Round Eight torture: GREEN");
