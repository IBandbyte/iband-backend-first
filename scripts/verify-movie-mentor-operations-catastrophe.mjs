import assert from"node:assert/strict";import{createHash}from"node:crypto";import{certifyPostCatastropheRecovery as certify}from"../ai/MovieMentorPostCatastropheRecoveryCertification.js";
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==="object"?Object.keys(v).sort().reduce((o,k)=>(o[k]=stable(v[k]),o),{}):v,hash=v=>createHash("sha256").update(JSON.stringify(stable(v))).digest("hex"),now=Date.parse("2026-08-24T13:02:45Z"),scope="recovery:cat-1",creator28={generation:28,fingerprint:hash({laptop:110,phone:"reconnect",revision:112,merge:"merge-112"}),authorityReference:"merge-112"},creator29={generation:29,fingerprint:hash({revision:113,merge:"merge-113"}),authorityReference:"merge-113"},recoveryReality={gauntletApprovalHash:"gauntlet-generation-28",creatorStateGeneration:28,creatorStateFingerprint:creator28.fingerprint,creatorAuthorityReference:"merge-112",authorityTerm:12,recoveryEpoch:9,fencingToken:44},reconciliationRealityHash=hash(recoveryReality),atomic={committed:true,indeterminate:false,reference:"atomic-gauntlet-generation-28"},soul={verified:true,reference:"soul-post-cat-28"},intent={valid:true,allBranchesIncluded:true,creatorIntentPreserved:true,reference:"merge-112"},ledger={valid:true,incidentClosed:true,allEffectsReconciled:true,reference:"ledger-cat-closed"},replayBinding={incidentId:"cat-1",operationType:"recovery",actionId:"restart-provider-worker",requestId:"req-cat",executionId:"exec-cat",scopeFingerprint:"creator-project-77",gauntletApprovalHash:recoveryReality.gauntletApprovalHash,capabilityKey:"cap-cat-28",creatorStateGeneration:28,creatorStateFingerprint:creator28.fingerprint,creatorAuthorityReference:"merge-112",recoveryEpoch:9},roots={"soul-authority-cat":{authorityReference:"soul-authority-cat",trustRootId:"trust-soul-cat",credentialRootFingerprint:"kms-soul-cat",controlPlaneRootId:"plane-soul-cat",operatorDomainId:"ops-soul-cat",provenanceEpoch:9,credentialGeneration:41},"intent-authority-cat":{authorityReference:"intent-authority-cat",trustRootId:"trust-intent-cat",credentialRootFingerprint:"kms-intent-cat",controlPlaneRootId:"plane-intent-cat",operatorDomainId:"ops-intent-cat",provenanceEpoch:12,credentialGeneration:73},"ledger-authority-cat":{authorityReference:"ledger-authority-cat",trustRootId:"trust-ledger-cat",credentialRootFingerprint:"kms-ledger-cat",controlPlaneRootId:"plane-ledger-cat",operatorDomainId:"ops-ledger-cat",provenanceEpoch:6,credentialGeneration:18}},clock={valid:true,unixMs:now,timeEpoch:7,sequence:501,authorityReference:"clock-authority-A",proofReference:"clock-proof-cat-501",scopeFingerprint:scope,trustRootFingerprint:"clock-root-A"},clockHwm={timeEpoch:7,sequence:500,unixMs:now-1000,authorityReference:"clock-authority-A",trustRootFingerprint:"clock-root-A",reference:"clock-hwm-cat-500"},args={incidentId:"cat-1",atomicMutationResult:atomic,postMutationSoul:soul,noLostIntentProof:intent,ledgerClosureProof:ledger,expectedCreatorState:creator28,replayBinding,timeProof:{signed:"signed-clock-cat-501"},timeScopeFingerprint:scope,recoveryEpoch:9,recoveryStateReference:"distributed-state-cat1-e9",reconciliationRealityHash};
const durable=()=>({durable:true,recovered:true,sealReference:"recovered-cat-1",commitReference:"durable-cat-commit-28",singleAtomicCommit:true,crashRecoveryVerified:true,creatorHighWatermark:{...creator28},authorityHighWatermarks:{soul:{...roots["soul-authority-cat"]},intent:{...roots["intent-authority-cat"]},ledger:{...roots["ledger-authority-cat"]}},trustedTimeHighWatermark:{timeEpoch:7,sequence:501,unixMs:now,authorityReference:"clock-authority-A",trustRootFingerprint:"clock-root-A"},reference:"restart-readback-cat-28"}),node=id=>({nodeId:id,...durable()}),seal=async()=>({committed:true,evidenceIdentityCompared:true,creatorGenerationCompared:true,creatorFingerprintCompared:true,creatorAuthorityReferenceCompared:true,authorityProvenanceRootsCompared:true,authorityProvenanceEpochsCompared:true,authorityCredentialGenerationsCompared:true,trustedTimeAuthorityCompared:true,trustedTimeTrustRootCompared:true,trustedTimeEpochCompared:true,trustedTimeSequenceCompared:true,trustedTimeUnixMsCompared:true,creatorHighWatermarkAdvanced:true,authorityHighWatermarksAdvanced:true,trustedTimeHighWatermarkAdvanced:true,singleDurableAtomicCommit:true,recoveredSealWritten:true,reference:"recovered-cat-1"}),decision=(b,f)=>({valid:true,authorized:true,reference:"recovery-decision-cat1-e9",authorityReference:"recovery-root-A",bindingFingerprint:f,incidentId:b.incidentId,recoveryEpoch:b.recoveryEpoch,creatorFingerprint:b.creatorFingerprint,sealReference:b.sealReference,resurrectionReference:b.resurrectionReference,timeAuthorityReference:b.timeAuthorityReference,timeEpoch:b.timeEpoch,timeSequence:b.timeSequence}),finalDurable=(identity,commitReference="final-cat-commit-28",realityHash=reconciliationRealityHash)=>({found:true,durable:true,sameAtomicTransaction:true,commitReference,reconciliationRealityHash:realityHash,authorityHighWatermark:{recoveryEpoch:identity.recoveryEpoch,decisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint},globalState:{state:"globally_recovered",recoveryEpoch:identity.recoveryEpoch,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,transitionReference:identity.transitionReference},reference:"final-restart-readback-cat-28"}),absent=id=>({nodeId:id,found:false,absenceProven:true,reference:`absence-${id}`}),gateAbsent=id=>({nodeId:id,found:false,absenceProven:true,reference:`gate-absence-${id}`});let lastFinalTuple,gateDisk=null;
const base={verifyTimeProof:async()=>({...clock}),readTimeHighWatermark:async()=>({...clockHwm}),verifyPostMutationSoul:async()=>({verified:true,reference:soul.reference,authorityReference:"soul-authority-cat"}),verifyNoLostIntent:async()=>({valid:true,allBranchesIncluded:true,creatorIntentPreserved:true,reference:intent.reference,authorityReference:"intent-authority-cat"}),verifyLedgerClosure:async()=>({valid:true,incidentClosed:true,allEffectsReconciled:true,reference:ledger.reference,authorityReference:"ledger-authority-cat"}),verifyEvidenceAuthorityProvenance:async({role,authorityReference})=>({valid:true,proofReference:`provenance-cat-${role}`,validFrom:"2026-08-24T12:00:00Z",validUntil:"2026-08-24T14:00:00Z",revoked:false,...roots[authorityReference]}),verifyAuthoritativeCreatorState:async()=>({authoritative:true,...creator28,reference:"creator-final-28"}),inspectConsumption:async()=>({consumed:true,consumptionId:"consume-cat-28",reference:"consume-proof"}),attemptReplay:async()=>({committed:false,mutationPerformed:false,denied:true,reason:"single_use_operation_already_consumed",reference:"replay-denied-cat"}),commitRecoverySeal:seal,readDurableRecoveryState:async()=>durable(),readNodeRecoveryViews:async()=>[node("restart-A"),node("restart-B")],verifyReplicaAuthority:async()=>({authoritative:true,staleReplica:false,promotable:true}),verifyResurrectionReplayState:async()=>({stable:true,partiallyReplayed:false,sameOperationReplayDenied:true,reference:"global-resurrection-cat-28"}),readRecoveryAuthorityHighWatermark:async()=>({recoveryEpoch:8,decisionReference:"decision-e8"}),verifyRecoveryAuthorityDecision:async({binding,bindingFingerprint})=>decision(binding,bindingFingerprint),commitFinalRecovery:async({tuple})=>(lastFinalTuple=structuredClone(tuple),{committed:true,indeterminate:false,authorityHighWatermarkWritten:true,globalStateWritten:true,sameAtomicTransaction:true,reference:"final-cat-commit-28"}),readFinalRecovery:async()=>finalDurable(lastFinalTuple),verifyAbsenceAuthority:async({view})=>({authoritative:true,staleReplica:false,nodeId:view.nodeId}),readRetryGate:async()=>gateDisk?structuredClone(gateDisk):{found:false},readRetryGateAbsenceViews:async()=>[gateAbsent("A"),gateAbsent("B")],verifyRetryGateAbsenceAuthority:async({view})=>({authoritative:true,staleReplica:false,nodeId:view.nodeId}),readRetryProofHighWatermark:async({identity})=>({authoritative:true,sameAtomicSnapshot:true,recoveryEpoch:identity.recoveryEpoch,clockEpoch:7,clockSequence:501,provenanceEpoch:12,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,reference:"retry-proof-snapshot-cat1-e9",clockHighWatermarkReference:"clock-hwm-cat-501",provenanceHighWatermarkReference:"prov-hwm-cat-12",authorityHighWatermarkReference:"authority-hwm-cat-e9"}),verifyRetryProofHighWatermark:async()=>({valid:true,clockVerified:true,provenanceVerified:true,authorityVerified:true,sameAtomicSnapshotVerified:true,reference:"verify-retry-proof-snapshot-cat1-e9"}),commitRetryGate:async({gate})=>{gateDisk={found:true,durable:true,...structuredClone(gate),gateReference:"durable-retry-gate-cat1-e9"};return{committed:true,identityCompared:true,incidentEpochCompared:true,newerGateExcluded:true,reference:"durable-retry-gate-cat1-e9"}}};
let r=await certify(args,base);assert.equal(r.certified,true);assert.equal(r.status,"globally_recovered");
r=await certify(args,{...base,commitFinalRecovery:async({tuple})=>(lastFinalTuple=tuple,{committed:false,indeterminate:true,reason:"ack_lost_after_possible_commit"})});assert.equal(r.certified,false);assert.equal(r.indeterminate,true);
let identityA;r=await certify({...args,finalizationMode:"reconcile"},{...base,readFinalRecoveryByIdentity:async({identity})=>{identityA=structuredClone(identity);throw new Error("power_cut_during_reconciliation")}});assert.equal(r.certified,false);assert.equal(r.status,"finalization_reconciliation_indeterminate");
r=await certify({...args,finalizationMode:"reconcile"},{...base,readFinalRecoveryByIdentity:async({identity})=>finalDurable(identity)});assert.equal(r.certified,true);assert.equal(r.finalizationMode,"reconcile");
// Late original transaction appears after distributed absence + freshness certification + recovery-reality binding but before retry CAS.
gateDisk=null;let phase="absence",retryCasAttempts=0,originalCommitIdentity=null;r=await certify({...args,finalizationMode:"retry"},{...base,readFinalRecoveryByIdentity:async({identity})=>phase==="absence"?{found:false}:finalDurable(originalCommitIdentity||identity,"original-racing-commit-28"),readAbsenceViews:async()=>[absent("B"),absent("C")],commitFinalRecoveryRetry:async({identity,reconciliationRealityHash:rh,requirements})=>{retryCasAttempts++;assert.equal(rh,reconciliationRealityHash);assert.equal(requirements.compareReconciliationRecoveryRealityHash,true);assert.equal(requirements.recomputeCurrentRecoveryRealityHashAtMutation,true);assert.equal(requirements.recheckReconciliationRecoveryRealityAtMutation,true);assert.equal(requirements.recheckGateAbsenceAtMutation,true);assert.equal(requirements.recheckTransactionAbsenceAtMutation,true);originalCommitIdentity=structuredClone(identity);phase="original-visible";return{committed:false,indeterminate:false,reason:"matching_final_commit_appeared_before_retry_cas"}}});assert.equal(r.certified,false);assert.ok(r.reasons.includes("matching_final_commit_appeared_before_retry_cas"));assert.equal(retryCasAttempts,1);
// Reconciliation reruns after the retry CAS loses and adopts the original durable transaction. Exactly one global truth survives.
let adoptionRetryWrites=0;r=await certify({...args,finalizationMode:"reconcile"},{...base,readFinalRecoveryByIdentity:async({identity})=>finalDurable(originalCommitIdentity||identity,"original-racing-commit-28"),commitFinalRecoveryRetry:async()=>{adoptionRetryWrites++;throw new Error("reconciliation must adopt, never retry")}});assert.equal(r.certified,true);assert.equal(r.status,"globally_recovered");assert.equal(r.finalizationMode,"reconcile");assert.equal(r.finalCommitReference,"original-racing-commit-28");assert.equal(adoptionRetryWrites,0);
// Mirror: retry CAS wins durably but its ACK is lost; Certification must persist the unresolved gate before returning.
gateDisk=null;let retryWonIdentity=null,retryCommitVisible=false,retryWinAttempts=0;r=await certify({...args,finalizationMode:"retry"},{...base,readFinalRecoveryByIdentity:async({identity})=>retryCommitVisible?finalDurable(retryWonIdentity||identity,"retry-winning-commit-28"):{found:false},readAbsenceViews:async()=>[absent("B"),absent("C")],commitFinalRecoveryRetry:async({identity,reconciliationRealityHash:rh})=>{retryWinAttempts++;assert.equal(rh,reconciliationRealityHash);retryWonIdentity=structuredClone(identity);retryCommitVisible=true;return{committed:false,indeterminate:true,reason:"ack_lost_after_retry_commit"}}});assert.equal(r.certified,false);assert.equal(r.status,"finalization_retry_indeterminate_reconciliation_required");assert.equal(r.indeterminate,true);assert.equal(r.reconciliationRequired,true);assert.equal(retryWinAttempts,1);assert.equal(gateDisk?.reconciliationRequired,true);
// POWER CUT erases caller memory. Fresh retry recovers durable gate first and adopts the retry-created durable transaction; no second retry CAS.
let restartRetryWrites=0;r=await certify({...args,finalizationMode:"retry"},{...base,readFinalRecoveryByIdentity:async({identity})=>finalDurable(retryWonIdentity||identity,"retry-winning-commit-28"),commitFinalRecoveryRetry:async()=>{restartRetryWrites++;throw new Error("second retry forbidden")}});assert.equal(r.certified,true);assert.equal(r.status,"globally_recovered");assert.equal(r.finalizationMode,"reconcile-adopted-after-indeterminate-retry");assert.equal(r.finalCommitReference,"retry-winning-commit-28");assert.equal(restartRetryWrites,0);
// Wrong recovery universe on crash-durable readback can never be adopted as this retry's global truth.
gateDisk=null;r=await certify({...args,finalizationMode:"retry"},{...base,readFinalRecoveryByIdentity:async({identity})=>finalDurable(identity,"wrong-universe-commit-28","c".repeat(64)),readAbsenceViews:async()=>[absent("B"),absent("C")],commitFinalRecoveryRetry:async()=>{throw new Error("existing transaction should reconcile before retry")}});assert.equal(r.certified,true);assert.equal(r.finalizationMode,"reconcile-adopted-before-retry");
// Stale Generation-29 replica can never be promoted into the recovered truth.
const stale29=node("restart-B");stale29.creatorHighWatermark={...creator29};stale29.commitReference="stale-replica-commit-29";let absenceRan=false;r=await certify({...args,finalizationMode:"reconcile"},{...base,readNodeRecoveryViews:async()=>[node("restart-A"),stale29],readFinalRecoveryByIdentity:async()=>({found:false}),readAbsenceViews:async()=>{absenceRan=true;return[absent("A"),absent("B")]}});assert.equal(r.certified,false);assert.ok(r.reasons.includes("distributed_restart_truth_fork_detected"));assert.equal(absenceRan,false);
console.log("RECOVERY CATASTROPHE v4 PASSED: Laptop 110 -> phone -> merge-112 -> Generation 28 -> ACK loss -> distributed absence quorum -> certified freshness + reconciliation reality -> late original transaction defeats retry CAS -> reconciliation adopts original durable commit -> retry ACK loss -> crash-durable unresolved gate -> POWER CUT -> restart adopts exactly one GLOBALLY_RECOVERED truth; stale Generation 29 promotion denied.");