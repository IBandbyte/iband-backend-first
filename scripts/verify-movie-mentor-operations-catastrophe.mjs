import assert from"node:assert/strict";import{createHash}from"node:crypto";import{certifyPostCatastropheRecovery}from"../ai/MovieMentorPostCatastropheRecoveryCertification.js";
const stable=v=>Array.isArray(v)?v.map(stable):v&&typeof v==="object"?Object.keys(v).sort().reduce((o,k)=>(o[k]=stable(v[k]),o),{}):v,hash=v=>createHash("sha256").update(JSON.stringify(stable(v))).digest("hex"),now=Date.parse("2026-08-24T13:02:45Z"),scope="recovery:cat-1";
// Upstream catastrophe invariants already proved by the Atomic Gauntlet path: laptop 110 -> reconnecting phone branch -> merge-112 -> Generation 28 -> regenerated Gauntlet -> atomic recovery.
const creator28={generation:28,fingerprint:hash({project:"project-77",revision:112,merge:"merge-112"}),authorityReference:"merge-112"},creator29={generation:29,fingerprint:hash({project:"project-77",revision:113,merge:"merge-113"}),authorityReference:"merge-113"},atomic={committed:true,indeterminate:false,reference:"atomic-after-phone-reconnect"},soul={verified:true,reference:"soul-post-cat-28"},intent={valid:true,allBranchesIncluded:true,creatorIntentPreserved:true,reference:"merge-112"},ledger={valid:true,incidentClosed:true,allEffectsReconciled:true,reference:"ledger-cat-closed"},replayBinding={incidentId:"cat-1",operationType:"recovery",actionId:"restart-provider-worker",requestId:"req-cat",executionId:"exec-cat",scopeFingerprint:"creator-project-77",gauntletApprovalHash:"gauntlet-generation-28",capabilityKey:"cap-cat-28",creatorStateGeneration:28,creatorStateFingerprint:creator28.fingerprint,creatorAuthorityReference:"merge-112",recoveryEpoch:9};
const roots={"soul-authority-cat":{trustRootId:"trust-soul-cat",credentialRootFingerprint:"kms-soul-cat",controlPlaneRootId:"plane-soul-cat",operatorDomainId:"ops-soul-cat",provenanceEpoch:9,credentialGeneration:41},"intent-authority-cat":{trustRootId:"trust-intent-cat",credentialRootFingerprint:"kms-intent-cat",controlPlaneRootId:"plane-intent-cat",operatorDomainId:"ops-intent-cat",provenanceEpoch:12,credentialGeneration:73},"ledger-authority-cat":{trustRootId:"trust-ledger-cat",credentialRootFingerprint:"kms-ledger-cat",controlPlaneRootId:"plane-ledger-cat",operatorDomainId:"ops-ledger-cat",provenanceEpoch:6,credentialGeneration:18}},clock={valid:true,unixMs:now,timeEpoch:7,sequence:501,authorityReference:"clock-authority-A",proofReference:"clock-proof-cat-501",scopeFingerprint:scope,trustRootFingerprint:"clock-root-A"},clockHwm={timeEpoch:7,sequence:500,unixMs:now-1000,authorityReference:"clock-authority-A",trustRootFingerprint:"clock-root-A",reference:"clock-hwm-cat-500"},timeProof={signed:"signed-clock-cat-501"};
const args={incidentId:"cat-1",atomicMutationResult:atomic,postMutationSoul:soul,noLostIntentProof:intent,ledgerClosureProof:ledger,expectedCreatorState:creator28,replayBinding,timeProof,timeScopeFingerprint:scope},baseDeps={verifyTimeProof:async()=>({...clock}),readTimeHighWatermark:async()=>({...clockHwm}),verifyPostMutationSoul:async()=>({verified:true,reference:soul.reference,authorityReference:"soul-authority-cat"}),verifyNoLostIntent:async()=>({valid:true,allBranchesIncluded:true,creatorIntentPreserved:true,reference:intent.reference,authorityReference:"intent-authority-cat"}),verifyLedgerClosure:async()=>({valid:true,incidentClosed:true,allEffectsReconciled:true,reference:ledger.reference,authorityReference:"ledger-authority-cat"}),verifyEvidenceAuthorityProvenance:async({role,authorityReference})=>({valid:true,authorityReference,proofReference:`provenance-cat-${role}`,...roots[authorityReference],validFrom:"2026-08-24T12:00:00Z",validUntil:"2026-08-24T14:00:00Z",revoked:false}),verifyAuthoritativeCreatorState:async()=>({authoritative:true,...creator28,reference:"creator-final-28"}),inspectConsumption:async()=>({consumed:true,consumptionId:"consume-cat-28",reference:"consume-proof"}),attemptReplay:async()=>({committed:false,mutationPerformed:false,denied:true,reason:"single_use_operation_already_consumed",reference:"replay-denied-cat"})},sealSuccess=async()=>({committed:true,evidenceIdentityCompared:true,creatorGenerationCompared:true,creatorFingerprintCompared:true,creatorAuthorityReferenceCompared:true,authorityProvenanceRootsCompared:true,authorityProvenanceEpochsCompared:true,authorityCredentialGenerationsCompared:true,authorityProvenanceStillUnrevokedVerified:true,authorityProvenanceStillWithinValidityWindowVerified:true,trustedTimeAuthorityCompared:true,trustedTimeTrustRootCompared:true,trustedTimeEpochCompared:true,trustedTimeSequenceCompared:true,trustedTimeUnixMsCompared:true,trustedTimeHighWatermarkAdvanced:true,trustedTimeStillAuthoritativeVerified:true,incidentStillClosableVerified:true,replayConsumptionStableVerified:true,recoveredSealWritten:true,reference:"recovered-cat-1"});
const baseline=await certifyPostCatastropheRecovery(args,{...baseDeps,commitRecoverySeal:sealSuccess});assert.equal(baseline.certified,true);assert.equal(baseline.status,"recovered");assert.equal(baseline.creatorState.authorityReference,"merge-112");assert.equal(baseline.evidenceIdentity.trustedTimeIdentity.sequence,501);assert.equal(baseline.evidenceIdentity.authorityProvenance.soul.credentialGeneration,41);
// THE COMPLETE CATASTROPHE: after every precondition proves Generation 28, temporal evidence and signed clock 7/501, all three truth planes advance inside the one final CAS.
let liveCreator={...creator28},liveSoul={...roots["soul-authority-cat"],revoked:false},liveClock={...clock};const complete=await certifyPostCatastropheRecovery(args,{...baseDeps,verifyAuthoritativeCreatorState:async()=>({authoritative:true,...liveCreator,reference:"pre-seal-merge-112"}),commitRecoverySeal:async tx=>{assert.equal(tx.expectedCreatorState.generation,28);assert.equal(tx.expectedCreatorState.authorityReference,"merge-112");assert.equal(tx.evidenceIdentity.authorityProvenance.soul.provenanceEpoch,9);assert.equal(tx.evidenceIdentity.authorityProvenance.soul.credentialGeneration,41);assert.equal(tx.evidenceIdentity.trustedTimeIdentity.authorityReference,"clock-authority-A");assert.equal(tx.evidenceIdentity.trustedTimeIdentity.timeEpoch,7);assert.equal(tx.evidenceIdentity.trustedTimeIdentity.sequence,501);
// Phone/branch world moves again: merge-113 becomes creator truth.
liveCreator={...creator29};
// Evidence authority rotates, advances provenance, and revokes the credential that certified Generation 28.
liveSoul={...liveSoul,credentialRootFingerprint:"kms-soul-cat-rotated",credentialGeneration:42,provenanceEpoch:10,revoked:true,revocationReference:"revoke-soul-generation-41"};
// Simultaneously the trusted clock authority itself rotates into a new epoch/root.
liveClock={...liveClock,authorityReference:"clock-authority-B",trustRootFingerprint:"clock-root-B",timeEpoch:8,sequence:1,unixMs:now+1,proofReference:"clock-proof-cat-epoch8-1"};
const creatorMoved=liveCreator.generation!==tx.expectedCreatorState.generation||liveCreator.fingerprint!==tx.expectedCreatorState.fingerprint||liveCreator.authorityReference!==tx.expectedCreatorState.authorityReference,authorityMoved=liveSoul.provenanceEpoch!==tx.evidenceIdentity.authorityProvenance.soul.provenanceEpoch||liveSoul.credentialGeneration!==tx.evidenceIdentity.authorityProvenance.soul.credentialGeneration||liveSoul.credentialRootFingerprint!==tx.evidenceIdentity.authorityProvenance.soul.credentialRootFingerprint||liveSoul.revoked,clockMoved=liveClock.authorityReference!==tx.evidenceIdentity.trustedTimeIdentity.authorityReference||liveClock.trustRootFingerprint!==tx.evidenceIdentity.trustedTimeIdentity.trustRootFingerprint||liveClock.timeEpoch!==tx.evidenceIdentity.trustedTimeIdentity.timeEpoch||liveClock.sequence!==tx.evidenceIdentity.trustedTimeIdentity.sequence;if(creatorMoved&&authorityMoved&&clockMoved)return{committed:false,reason:"creator_evidence_and_clock_authority_advanced_before_seal"};throw new Error("complete catastrophe must never write RECOVERED")}});assert.equal(complete.certified,false);assert.equal(complete.status,"reconciling");assert.equal(complete.reasons[0],"creator_evidence_and_clock_authority_advanced_before_seal");
// Clock movement alone also defeats a stale otherwise-perfect Generation 28 recovery seal.
const clockOnly=await certifyPostCatastropheRecovery(args,{...baseDeps,commitRecoverySeal:async tx=>{const rotated={authorityReference:"clock-authority-B",trustRootFingerprint:"clock-root-B",timeEpoch:8,sequence:1,unixMs:now+1};if(rotated.authorityReference!==tx.evidenceIdentity.trustedTimeIdentity.authorityReference||rotated.trustRootFingerprint!==tx.evidenceIdentity.trustedTimeIdentity.trustRootFingerprint||rotated.timeEpoch!==tx.evidenceIdentity.trustedTimeIdentity.timeEpoch)return{committed:false,reason:"trusted_time_authority_rotated_before_seal"};throw new Error("clock-only catastrophe must not seal")}});assert.equal(clockOnly.certified,false);assert.equal(clockOnly.reasons[0],"trusted_time_authority_rotated_before_seal");
console.log("Full catastrophe Clock Chamber passed: laptop 110 -> phone -> merge-112 -> Generation 28 -> atomic recovery -> temporal evidence -> signed clock -> final CAS; simultaneous merge-113/Generation 29 + evidence credential 42/provenance epoch 10/revocation + clock authority B/epoch 8/root B fails closed. Clock-only rotation also fails closed.");