import assert from"node:assert/strict";import{certifyPostCatastropheRecovery as certify}from"../ai/MovieMentorPostCatastropheRecoveryCertification.js";
const id={incidentId:"cat-1",recoveryEpoch:9,authorityDecisionReference:"decision-cat1-e9",authorityReference:"recovery-root-A",bindingFingerprint:"a".repeat(64),previousStateReference:"distributed-state-cat1-e9",transitionReference:"global:decision-cat1-e9"},reconciliationRealityHash="b".repeat(64),input={finalizationMode:"retry",incidentId:"cat-1",recoveryEpoch:9,finalizationIdentity:id,reconciliationRealityHash};
let disk={found:true,durable:true,reconciliationRequired:true,identity:structuredClone(id),gateReference:"gate-commit-unknown-ack"};let retryWrites=0,reconcileReads=0;
const restartDeps={readRetryGate:async()=>structuredClone(disk),readFinalRecoveryByIdentity:async()=>{reconcileReads++;return{found:false}},commitFinalRecoveryRetry:async()=>{retryWrites++;throw new Error("RETRY CAS MUST NOT RUN WHILE DURABLE GATE EXISTS")}};
// POWER CUT erased caller memory. Fresh retry carries no gate. Durable gate resurrects first and routes exclusively into reconciliation. The exact recovery universe is restored before the gate can be trusted as a retry blocker.
let r=await certify(input,restartDeps);assert.equal(r.certified,false);assert.equal(r.reconciliationRequired,true);assert.equal(r.finalizationMode,"retry-blocked-pending-reconciliation");assert.equal(retryWrites,0);assert.ok(reconcileReads>=0);
// If gate storage is unreadable after the second ACK loss, uncertainty itself blocks CAS before transaction reconciliation.
retryWrites=0;reconcileReads=0;r=await certify(input,{...restartDeps,readRetryGate:async()=>{throw new Error("gate-store-power-loss")}});assert.equal(r.certified,false);assert.equal(r.status,"durable_retry_gate_read_indeterminate");assert.equal(r.reconciliationRequired,true);assert.equal(retryWrites,0);assert.equal(reconcileReads,0);
// Torn gate bytes are not interpreted as no gate.
retryWrites=0;reconcileReads=0;disk={found:true,durable:false,reconciliationRequired:true,identity:structuredClone(id)};r=await certify(input,restartDeps);assert.equal(r.certified,false);assert.equal(r.status,"durable_retry_gate_invalid");assert.equal(retryWrites,0);assert.equal(reconcileReads,0);
// Durable gate identity substitution is caught by Certification perimeter before transaction reads/CAS.
retryWrites=0;reconcileReads=0;disk={found:true,durable:true,reconciliationRequired:true,identity:{...id,bindingFingerprint:"b".repeat(64)},gateReference:"poisoned-gate"};r=await certify(input,restartDeps);assert.equal(r.certified,false);assert.equal(r.status,"finalization_retry_gate_invalid");assert.equal(retryWrites,0);assert.equal(reconcileReads,0);assert.ok(r.reasons.includes("retry_gate_bindingFingerprint_mismatch"));
// Even a valid durable unresolved gate cannot be used to enter recovery with an unknown universe.
retryWrites=0;reconcileReads=0;disk={found:true,durable:true,reconciliationRequired:true,identity:structuredClone(id),gateReference:"gate-valid-but-no-universe"};r=await certify({...input,reconciliationRealityHash:""},restartDeps);assert.equal(r.certified,false);assert.equal(r.status,"finalization_retry_reconciliation_reality_missing");assert.ok(r.reasons.includes("reconciliation_recovery_reality_hash_required"));assert.equal(retryWrites,0);assert.equal(reconcileReads,0);
console.log("DOUBLE ACK-LOSS ABYSS v1.6 PASSED: retry ACK and gate ACK may both vanish; after POWER CUT the exact reconciliation universe plus durable gate resurrection blocks every retry CAS. Missing universe and unreadable/torn/poisoned gate state fail closed.");