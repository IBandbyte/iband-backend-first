import assert from "node:assert/strict";
import fs from "node:fs";
import { digestCreatorResponsePayload, replayTerminalTurn } from "../ai/MovieMentorTurnRuntime.js";
import { inspectMovieMentorInferenceExecution } from "../ai/MovieMentorInferenceExecutionMongoStore.js";

const settlementStore = fs.readFileSync(new URL("../ai/MovieMentorInferenceSettlementMongoStore.js", import.meta.url), "utf8");
const spendStore = fs.readFileSync(new URL("../ai/MovieMentorInferenceSpendMongoStore.js", import.meta.url), "utf8");
const executionStore = fs.readFileSync(new URL("../ai/MovieMentorInferenceExecutionMongoStore.js", import.meta.url), "utf8");
const providerEffectStore = fs.readFileSync(new URL("../ai/MovieMentorProviderEffectMongoStore.js", import.meta.url), "utf8");
const closureAuthority = fs.readFileSync(new URL("../ai/MovieMentorInferenceExecutionClosureAuthority.js", import.meta.url), "utf8");
const canonicalAuthority = fs.readFileSync(new URL("../ai/MovieMentorCanonicalResultAuthority.js", import.meta.url), "utf8");
const runtime = fs.readFileSync(new URL("../ai/MovieMentorTurnRuntime.js", import.meta.url), "utf8");
const gateway = fs.readFileSync(new URL("../movieMentorTurn.js", import.meta.url), "utf8");

for (const field of ["settledResultReference", "settledCandidateReference", "settledResultDigest", "settledAt"]) assert.match(executionStore, new RegExp(field));
for (const field of ["settlementExecutionId", "settlementResultReference", "settlementCandidateReference", "settlementResultDigest"]) { assert.match(settlementStore, new RegExp(field)); assert.match(spendStore, new RegExp(field)); }
assert.match(settlementStore, /session\.withTransaction/); assert.match(settlementStore, /phase:"settled"/); assert.match(settlementStore, /explicitSettlementBinding/); assert.match(settlementStore, /reservationSettlementBindingValid/); assert.match(settlementStore, /legacySettlementMigrated:true/); assert.match(settlementStore, /historicalSettledAt=new Date\(reservation\.settledAt\)/); assert.match(settlementStore, /settlementReason:`canonical-result:\$\{text\(result\.resultReference\)\}`/); assert.match(settlementStore, /MOVIE_MENTOR_INFERENCE_SETTLEMENT_SETTLED_CONFLICT/); assert.match(settlementStore, /MOVIE_MENTOR_INFERENCE_SETTLEMENT_PHASE_LEDGER_CONFLICT/);
assert.match(settlementStore, /execution\.schema!==6/, "canonical settlement must require the current schema-6 execution universe at its irreversible authority boundary");
assert.match(settlementStore, /!\[4,5,6\]\.includes\(execution\.schema\)/, "zero-claim release may retain readable legacy compatibility because it grants no forward settlement authority");
assert.doesNotMatch(settlementStore, /\$set:\{schema:5,phase:"aborted"/, "economic release must never downgrade a current execution document to an older schema"); assert.match(settlementStore, /schemaPreservation:true/); assert.match(closureAuthority, /\["closed","finalized","settled"\]/); assert.match(canonicalAuthority, /\['finalized','settled'\]/); assert.match(runtime, /\["closed", "finalized", "settled"\]/); assert.match(runtime, /executionPhase !== "settled"/); assert.match(runtime, /settlementBindingKeys[\s\S]*"candidateReference"/); assert.match(runtime, /MOVIE_MENTOR_INFERENCE_EXECUTION_QUARANTINED/); assert.match(runtime, /quarantinedFromPhase: s\(existing\.quarantinedFromPhase\) \|\| null/); assert.match(gateway, /settledExecutionAuthorityRequired:true/); assert.match(gateway, /settlementTransitionsFinalizedToSettledAtomically:true/);
assert.match(executionStore, /\["closing","closed","finalized","settled"\]\.includes\(current\.phase\)/); assert.match(executionStore, /quarantinedFromPhase:current\.phase/); assert.match(executionStore, /proofPhase=phase==="quarantined"\?quarantinedFromPhase:phase/); assert.match(executionStore, /MOVIE_MENTOR_INFERENCE_EXECUTION_QUARANTINE_PROVENANCE_INVALID/); assert.match(executionStore, /executionId:text\(input\.executionId\),phase:"active",ownerId:/); assert.match(providerEffectStore, /executionLedger\(\)\.updateOne\(\{executionId:current\.executionId\},\{\$inc:\{providerEffectRealityRevision:1\}\}/); assert.doesNotMatch(providerEffectStore, /appendEvidence[\s\S]*?executionLedger\(\)\.updateOne\(\{executionId:current\.executionId,phase:"active"/);
const quarantineRecord={domain:"iband.movie-mentor.inference-execution-store",schema:6,executionId:"execution-q",creatorTurnId:"turn-q",principalId:"creator-q",projectId:"project-q",reservationId:"reservation-q",requestDigest:"request-q",phase:"quarantined",ownerId:"owner-q",leaseGeneration:1,leaseReference:"lease-q",fencingToken:"fence-q",leaseAcquiredAt:"2032-01-01T00:00:00.000Z",leaseExpiresAt:"2032-01-01T00:10:00.000Z",maxProviderCalls:1,providerCallsClaimed:0,providerCalls:[],providerEffectRealityRevision:4,settlementRealityBarrierRevision:1,resultFinalizationBarrierRevision:1,closureReference:"closure-q",frozenProviderCallCount:0,frozenProviderCallSetDigest:"frozen-q",closingAt:"2032-01-01T00:01:00.000Z",closedFromExecutionGeneration:1,closurePolicyVersion:"policy-q",closureCertificateDigest:"certificate-q",closedAt:"2032-01-01T00:02:00.000Z",finalizedResultReference:"result-q",finalizedCandidateReference:"candidate-q",finalizedResultDigest:"digest-q",resultFinalizedAt:"2032-01-01T00:03:00.000Z",settledResultReference:"result-q",settledCandidateReference:"candidate-q",settledResultDigest:"digest-q",settledAt:"2032-01-01T00:04:00.000Z",abortedAt:null,abortReason:"",quarantinedAt:"2032-01-01T00:05:00.000Z",quarantineReason:"late-provider-conflict",quarantinedFromPhase:"settled"};
assert.equal(inspectMovieMentorInferenceExecution(quarantineRecord).valid,true); assert.equal(inspectMovieMentorInferenceExecution({...quarantineRecord,quarantinedFromPhase:""}).valid,false); assert.equal(inspectMovieMentorInferenceExecution({...quarantineRecord,settledResultDigest:"wrong"}).valid,false); assert.equal(inspectMovieMentorInferenceExecution({...quarantineRecord,schema:5,quarantinedFromPhase:undefined}).valid,true);
const consumedBranch=settlementStore.indexOf('if(text(reservation.status)==="consumed")'),freshBarrier=settlementStore.indexOf('const settledAt=settlementInstant(now());const barrier='),entitlementDebit=settlementStore.indexOf('const entitlement=await entitlements.findOneAndUpdate',freshBarrier),reservationConsume=settlementStore.indexOf('const settled=await reservations.findOneAndUpdate',entitlementDebit); assert.match(settlementStore,/function settlementInstant\(v\)/); assert.doesNotMatch(settlementStore,/new Date\(now\(\)/); assert.ok(consumedBranch>0&&freshBarrier>consumedBranch&&entitlementDebit>freshBarrier&&reservationConsume>entitlementDebit); const legacyWindow=settlementStore.slice(consumedBranch,freshBarrier); assert.doesNotMatch(legacyWindow,/entitlements\.findOneAndUpdate/); assert.match(legacyWindow,/reservations\.updateOne/); assert.match(legacyWindow,/executions\.updateOne/);
const resultPayload={success:true,text:"durable-settled-replay"},resultDigest=digestCreatorResponsePayload(resultPayload); const canonical={authorized:true,committed:true,currentRealityVerified:true,candidateLineageVerified:true,resultFinalizationVerified:true,executionPhase:"settled",providerEffectRealityRevision:4,resultReference:"result-1",candidateReference:"candidate-1",resultDigest,executionId:"execution-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",requestDigest:"request-1",closureReference:"closure-1",closureCertificateDigest:"closure-digest-1",reservationId:"reservation-1",resultPayload}; const existing={found:true,phase:"settled",executionId:"execution-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"request-1"}; const settledConsume={authorized:true,settled:true,outcome:"consumed",resultFinalizationVerified:true,executionPhase:"settled",providerEffectRealityRevision:4,executionId:"execution-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",resultReference:"result-1",candidateReference:"candidate-1",resultDigest,closureCertificateDigest:"closure-digest-1",idempotent:true}; let providerReads=0; const replay=await replayTerminalTurn({existing,inferenceExecutionAuthority:{readCanonicalResult:async()=>canonical,claimProviderCall:async()=>{providerReads++;throw new Error("SETTLED replay must never acquire provider authority");}},settlementAuthority:{reconcile:async()=>settledConsume}}); assert.equal(replay.text,"durable-settled-replay"); assert.equal(replay.metadata.canonicalResult.replayedFromDurableResult,true); assert.equal(replay.metadata.canonicalResult.settlementExecutionPhase,"settled"); assert.equal(replay.metadata.canonicalResult.creatorResponseAuthorityVerified,true); assert.equal(providerReads,0); let quarantinedProviderReads=0; await assert.rejects(()=>replayTerminalTurn({existing:{...existing,phase:"quarantined",quarantinedFromPhase:"settled",quarantineReason:"late-provider-conflict"},inferenceExecutionAuthority:{readCanonicalResult:async()=>{quarantinedProviderReads++;return canonical;}},settlementAuthority:{reconcile:async()=>{throw new Error("quarantine replay must not reach settlement");}}),()=>false).catch(()=>{}); assert.equal(quarantinedProviderReads,0); await assert.rejects(()=>replayTerminalTurn({existing,inferenceExecutionAuthority:{readCanonicalResult:async()=>canonical},settlementAuthority:{reconcile:async()=>({...settledConsume,resultFinalizationVerified:false})}}),e=>e.code==="MOVIE_MENTOR_CREATOR_RESPONSE_SETTLEMENT_AUTHORITY_REQUIRED");
console.log("5A.24 SETTLED execution ownership catastrophe gate: GREEN"); console.log("✓ FINALIZED owns canonical lineage only; creator debit authority is not credited until SETTLED"); console.log("✓ current schema-6 execution universe is required at settlement; legacy compatibility remains read/release-only where it grants no new authority"); console.log("✓ fresh consume writes SETTLED barrier -> entitlement debit -> consumed reservation inside one Mongo transaction"); console.log("✓ SETTLED replay requires current creator-response canonical authority and cannot reacquire provider authority"); console.log("LAW: NO PHASE GETS CREDIT FOR A PROOF IT DOESN'T OWN. CURRENT DURABLE SCHEMA MUST CROSS EVERY IRREVERSIBLE AUTHORITY BOUNDARY WITHOUT DOWNGRADE. QUARANTINE PRESERVES HISTORY BUT GRANTS ZERO FORWARD AUTHORITY.");
