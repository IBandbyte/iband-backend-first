import assert from"node:assert/strict";import{certifyPostCatastropheRecovery as certify}from"../ai/MovieMentorPostCatastropheRecoveryCertification.js";import{RECOVERY_AUTHORITY_BINDING_HASH_DOMAIN as DOMAIN,RECOVERY_AUTHORITY_BINDING_HASH_SCHEMA as SCHEMA}from"../ai/MovieMentorRecoveryAuthority.js";
const identity={incidentId:"cat-1",recoveryEpoch:9,authorityDecisionReference:"decision-cat1-e9",authorityReference:"recovery-root-A",bindingFingerprintDomain:DOMAIN,bindingFingerprintSchema:SCHEMA,bindingFingerprint:"a".repeat(64),previousStateReference:"distributed-state-cat1-e9",transitionReference:"global:decision-cat1-e9"},reconciliationRealityHash="b".repeat(64),gate={reconciliationRequired:true,identity,gateReference:"gate:cat-1:e9"},baseArgs={finalizationMode:"retry",incidentId:"cat-1",recoveryEpoch:9,finalizationIdentity:identity,reconciliationRealityHash};
const durable=g=>async()=>({found:true,durable:true,reconciliationRequired:true,identity:g.identity,gateReference:g.gateReference||"gate:test"});
const tripwires=()=>{let reads=0,writes=0;return{deps:{readFinalRecoveryByIdentity:async()=>{reads++;throw new Error("RECONCILIATION MUST NOT RUN")},readAbsenceViews:async()=>{reads++;throw new Error("ABSENCE MUST NOT RUN")},commitFinalRecoveryRetry:async()=>{writes++;throw new Error("CAS MUST NOT RUN")}},counts:()=>({reads,writes})}};
// Restart truth requires both the exact recovery universe and durable gate state. Missing universe dies before storage can be used as retry authority.
{const t=tripwires();let gateReads=0;let r=await certify({...baseArgs,reconciliationRealityHash:""},{...t.deps,readRetryGate:async()=>{gateReads++;return{found:true,durable:true,...gate}}});assert.equal(r.certified,false);assert.equal(r.status,"finalization_retry_reconciliation_reality_missing");assert.equal(gateReads,0);assert.deepEqual(t.counts(),{reads:0,writes:0})}
// A valid unresolved durable Authority-v2 gate may enter reconciliation, but incomplete downstream evidence still fails closed and CAS remains unreachable.
let writes=0,r=await certify(baseArgs,{readRetryGate:durable(gate),commitFinalRecoveryRetry:async()=>{writes++;throw new Error("CAS MUST NOT RUN")}});assert.equal(r.certified,false);assert.equal(r.reconciliationRequired,true);assert.equal(writes,0);assert.ok(r.finalizationMode==="retry-blocked-pending-reconciliation"||r.indeterminate===true);
// DELETE/SUBSTITUTION ATTACK: caller deletion/replacement cannot override durable gate truth.
writes=0;r=await certify({...baseArgs,retryRecoveryGate:undefined},{readRetryGate:durable(gate),commitFinalRecoveryRetry:async()=>{writes++;throw new Error("CAS MUST NOT RUN")}});assert.equal(r.certified,false);assert.equal(r.reconciliationRequired,true);assert.equal(writes,0);
writes=0;r=await certify({...baseArgs,retryRecoveryGate:{reconciliationRequired:false,certified:true,status:"globally_recovered"}},{readRetryGate:durable(gate),commitFinalRecoveryRetry:async()=>{writes++;throw new Error("CAS MUST NOT RUN")}});assert.equal(r.certified,false);assert.equal(r.reconciliationRequired,true);assert.equal(writes,0);
// Poisoned durable gates are rejected by durability recovery before reconciliation/CAS.
for(const[field,value]of[["recoveryEpoch",8],["incidentId","cat-evil"],["bindingFingerprintDomain","purple.universe.zorg.clipboard"],["bindingFingerprintSchema",1]]){const t=tripwires(),poisoned=structuredClone(gate);poisoned.identity[field]=value;r=await certify({...baseArgs,retryRecoveryGate:gate},{...t.deps,readRetryGate:durable(poisoned)});assert.equal(r.certified,false,field);assert.equal(r.status,"durable_retry_gate_invalid",field);assert.equal(r.reconciliationRequired,true,field);assert.ok(r.reasons.includes("durable_retry_gate_invalid"),`${field}:${r.reasons}`);assert.deepEqual(t.counts(),{reads:0,writes:0},field)}
// Incident/epoch-valid, protocol-valid storage is still not enough: immutable recovery identity must match exactly.
for(const[field,value,reason]of[["bindingFingerprint","b".repeat(64),"retry_gate_bindingFingerprint_mismatch"],["authorityDecisionReference","forged-decision","retry_gate_authorityDecisionReference_mismatch"],["authorityReference","forged-root","retry_gate_authorityReference_mismatch"],["previousStateReference","forged-state","retry_gate_previousStateReference_mismatch"],["transitionReference","global:forged","retry_gate_transitionReference_mismatch"]]){const t=tripwires(),poisoned=structuredClone(gate);poisoned.identity[field]=value;r=await certify(baseArgs,{...t.deps,readRetryGate:durable(poisoned)});assert.equal(r.certified,false,field);assert.equal(r.status,"finalization_retry_gate_invalid",field);assert.ok(r.reasons.includes(reason),`${field}:${r.reasons}`);assert.deepEqual(t.counts(),{reads:0,writes:0},field)}
// Legacy naked/non-hash clipboard dies at durable-gate recovery itself, before the Certification perimeter.
{const t=tripwires(),bad=structuredClone(gate);delete bad.identity.bindingFingerprintDomain;delete bad.identity.bindingFingerprintSchema;bad.identity.bindingFingerprint="trust-me-bro";r=await certify(baseArgs,{...t.deps,readRetryGate:durable(bad)});assert.equal(r.status,"durable_retry_gate_invalid");assert.ok(r.reasons.includes("durable_retry_gate_invalid"));assert.deepEqual(t.counts(),{reads:0,writes:0})}
// Cross-protocol Recovery Reality substitution cannot masquerade as Recovery Authority.
{const t=tripwires(),bad=structuredClone(gate);bad.identity.bindingFingerprintDomain="iband.movie-mentor.operations.reconciliation-recovery-reality";r=await certify(baseArgs,{...t.deps,readRetryGate:durable(bad)});assert.equal(r.status,"durable_retry_gate_invalid");assert.deepEqual(t.counts(),{reads:0,writes:0})}
// FORGED COMPLETION CLAIM: caller flags have zero authority while durable storage says unresolved.
writes=0;r=await certify({...baseArgs,retryRecoveryGate:{...gate,reconciliationCompleted:true,certified:true,status:"globally_recovered"}},{readRetryGate:durable(gate),commitFinalRecoveryRetry:async()=>{writes++;throw new Error("CAS MUST NOT RUN")}});assert.equal(r.certified,false);assert.equal(r.reconciliationRequired,true);assert.equal(writes,0);
{const t=tripwires();r=await certify(baseArgs,{...t.deps,readRetryGate:async()=>({found:true,durable:true,reconciliationRequired:true})});assert.equal(r.certified,false);assert.equal(r.status,"durable_retry_gate_invalid");assert.deepEqual(t.counts(),{reads:0,writes:0})}
console.log("CHICKEN ZORG DURABLE GATE TORTURE AUTHORITY-v2 PASSED: unknown recovery universe dies before gate storage; caller deletion/substitution/completion cannot override durable restart truth; legacy, Purple Universe, wrong-schema, Recovery Reality and poisoned gate identities die before reconciliation; retry CAS remains zero.");