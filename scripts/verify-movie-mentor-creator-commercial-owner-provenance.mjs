import assert from "node:assert/strict";
import fs from "node:fs/promises";
import {createMovieMentorProductionCreatorCommercialComposition} from "../ai/MovieMentorProductionCreatorCommercialComposition.js";
import {createMovieMentorProductionAuthenticationComposition} from "../ai/MovieMentorProductionAuthenticationComposition.js";
const AUTH_DOMAIN="iband.movie-mentor.production-authentication-composition",CREATOR_DOMAIN="iband.movie-mentor.production-creator-commercial-composition";
const verifyCredential=async()=>Object.freeze({verified:true,principalId:"creator-1"});
const verifier=Object.freeze({version:"1.0.0",domain:"iband.movie-mentor.journey-recovery-clerk-credential-verifier",provider:"clerk",algorithm:"RS256",networkMode:"pinned-public-key",authorizedParties:Object.freeze(["https://app.example.com"]),verifyCredential});
const env={MOVIE_MENTOR_CLERK_JWT_KEY:"PUBLIC",MOVIE_MENTOR_CLERK_AUTHORIZED_PARTIES_JSON:'["https://app.example.com"]',MOVIE_MENTOR_CLERK_ISSUER:"issuer",MOVIE_MENTOR_AUDIENCE:"audience"};
const authentication=createMovieMentorProductionAuthenticationComposition({env,createVerifier:()=>verifier}),authenticationProof=authentication.getStatus();
const purchaseProof=Object.freeze({domain:"iband.movie-mentor.production-commercial-purchase-intent-authority",production:true,durablePurchaseIntent:true,immutableCommercialTerms:true,serverOwnedPolicy:true,processLocalFallback:false});
const checkoutProof=Object.freeze({domain:"iband.movie-mentor.production-commercial-checkout-authority",production:true,durableCheckoutBinding:true,serverOwnedIdempotency:true,purchaseIntentProvenanceRequired:true,explicitProviderRequired:true,processLocalFallback:false});
const catalogueProof=Object.freeze({domain:"iband.movie-mentor.production-commercial-package-catalogue-authority",production:true,serverOwned:true,creatorMutable:false,immutableSnapshotRequired:true,configurationSource:"MOVIE_MENTOR_COMMERCIAL_POLICY_JSON"});
const purchaseIntentAuthority=Object.freeze({async createPurchaseIntent(){return Object.freeze({commercialIntentId:"intent-1"});},getStatus(){return purchaseProof;}}),checkoutAuthority=Object.freeze({async initiateCheckout(){return Object.freeze({authorized:true});},getStatus(){return checkoutProof;}}),packageCatalogueAuthority=Object.freeze({async listCommercialPackages(){return Object.freeze([]);},getStatus(){return catalogueProof;}});
const composition=createMovieMentorProductionCreatorCommercialComposition({authentication,purchaseIntentAuthority,checkoutAuthority,packageCatalogueAuthority}),status=composition.getStatus();
assert.equal(composition.ready,true);assert.equal(status,composition.getStatus());assert.equal(status.domain,CREATOR_DOMAIN);assert.equal(status.production,true);assert.equal(status.authenticationProvenanceRequired,true);assert.equal(status.authenticationDomain,AUTH_DOMAIN);assert.equal(status.authenticationStatus,authenticationProof,"Exact authentication owner proof must cross into creator commercial proof.");assert.equal(status.purchaseIntentStatus,purchaseProof);assert.equal(status.checkoutStatus,checkoutProof);assert.equal(status.catalogueStatus,catalogueProof);assert.equal(status.publicRouteCandidate,true);assert.equal(status.processLocalFallback,false);
const clonedAuthStatus=Object.freeze({...authenticationProof});const persuasiveLookalike=Object.freeze({...authentication,status:clonedAuthStatus,getStatus:()=>clonedAuthStatus});
assert.throws(()=>createMovieMentorProductionCreatorCommercialComposition({authentication:persuasiveLookalike,purchaseIntentAuthority,checkoutAuthority,packageCatalogueAuthority}),e=>e.code==="MOVIE_MENTOR_CREATOR_COMMERCIAL_AUTHENTICATION_REQUIRED","Deep-equal self-consistent authentication lookalike must receive zero creator-commercial authority.");
assert.throws(()=>createMovieMentorProductionCreatorCommercialComposition({authentication,purchaseIntentAuthority:{createPurchaseIntent:purchaseIntentAuthority.createPurchaseIntent},checkoutAuthority,packageCatalogueAuthority}),e=>e.code==="MOVIE_MENTOR_CREATOR_COMMERCIAL_PURCHASE_INTENT_REQUIRED");assert.throws(()=>createMovieMentorProductionCreatorCommercialComposition({authentication,purchaseIntentAuthority,checkoutAuthority:{initiateCheckout:checkoutAuthority.initiateCheckout},packageCatalogueAuthority}),e=>e.code==="MOVIE_MENTOR_CREATOR_COMMERCIAL_CHECKOUT_REQUIRED");assert.throws(()=>createMovieMentorProductionCreatorCommercialComposition({authentication,purchaseIntentAuthority,checkoutAuthority,packageCatalogueAuthority:{listCommercialPackages:packageCatalogueAuthority.listCommercialPackages}}),e=>e.code==="MOVIE_MENTOR_CREATOR_COMMERCIAL_PACKAGE_CATALOGUE_REQUIRED");
const creatorSource=await fs.readFile(new URL("../ai/MovieMentorProductionCreatorCommercialComposition.js",import.meta.url),"utf8");assert.match(creatorSource,/authenticationStatus=ownedStatus\(authentication\)/);assert.match(creatorSource,/isMovieMentorProductionAuthenticationOwnerProof\(authentication,status\)/);assert.match(creatorSource,/if\(!authenticationProven\(authentication,authenticationStatus\)\)[\s\S]*?const requestAuthority=createMovieMentorCreatorCommercialRequestAuthority/,"Authentication owner proof must be consumed before creator request authority is constructed.");
const http=await fs.readFile(new URL("../ai/MovieMentorProductionCommercialHttpIngress.js",import.meta.url),"utf8");assert.match(http,/const creatorStatus=ownedStatus\(creator\)/);assert.match(http,/creatorProven\(creatorStatus\)/);assert.ok(http.indexOf("creatorProven(creatorStatus)")<http.indexOf("creatorRouter:creator.router"));
console.log("PASS GATES OF PERSUASION: creator commercial composition consumes the exact authentication owner proof.");console.log("PASS: reconstructed authentication identity cannot persuade the composition into constructing commercial authority.");console.log("LAW: AUTHENTICATION OWNER PROOF → CREATOR COMMERCIAL COMPOSITION → CREATOR COMMERCIAL OWNER PROOF → HTTP.");
