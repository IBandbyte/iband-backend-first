import assert from "node:assert/strict";import fs from "node:fs";import crypto from "node:crypto";
import {createMovieMentorResultCandidateMongoStore} from "../ai/MovieMentorResultCandidateMongoStore.js";import {recoverStagedResultTurn} from "../ai/MovieMentorTurnRuntime.js";import {MOVIE_MENTOR_CREATOR_STATE_CONSUMPTION_PROOF_DOMAIN,MOVIE_MENTOR_CREATOR_STATE_CONSUMPTION_SCHEMA} from "../ai/MovieMentorCreatorStateConsumptionAuthority.js";
const stable=v=>{if(Array.isArray(v))return v.map(stable);if(v&&typeof v==="object"){const o={};for(const k of Object.keys(v).sort())o[k]=stable(v[k]);return o;}return v;};const hash=v=>crypto.createHash("sha256").update(JSON.stringify(stable(v))).digest("hex");let row=null;const fakeModel={findOne(){return{lean(){return{exec:async()=>row};}}},async create(v){row=structuredClone(v);return row;}};const physicalIndexes=[{key:{executionId:1},unique:true},{key:{candidateReference:1},unique:true}];let physicalIndexReads=0;const execution={authorized:true,executionAuthorized:true,executionId:"execution-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"request-1",ownerId:"worker-1",leaseGeneration:1,leaseReference:"lease-1",fencingToken:"fence-1"},creatorStateConsumptionProof={domain:MOVIE_MENTOR_CREATOR_STATE_CONSUMPTION_PROOF_DOMAIN,schema:MOVIE_MENTOR_CREATOR_STATE_CONSUMPTION_SCHEMA,authorized:true,currentOwnershipVerified:true,principalId:"creator-1",projectId:"project-1",ownershipRef:"ownership-1",ownershipRevision:1,stage:"result-candidate",revision:7,creatorStateGeneration:4,creatorStateFingerprint:"creator-state-fingerprint-4",executionId:"execution-1",providerCallId:null},store=createMovieMentorResultCandidateMongoStore({mongoModel:fakeModel,readIndexes:async collectionName=>{physicalIndexReads+=1;assert.equal(collectionName,"movie_mentor_result_candidate");return physicalIndexes;},executionCollection:false,randomId:()=>"candidate-1",now:()=>new Date("2032-01-01T00:00:00Z")}),payload={response:"creator-result",metadata:{source:"orchestration"}},staged=await store.stageCandidate({execution,resultPayload:payload,creatorStateConsumptionProof});assert.equal(staged.executionId,"execution-1");assert.equal(staged.resultDigest,hash(payload));assert.equal(staged.creatorStateRevision,7);assert.equal(staged.creatorStateGeneration,4);assert.equal(physicalIndexReads,1,"staged result recovery court must prove owned candidate physical uniqueness before durable staging");assert.equal((await store.stageCandidate({execution,resultPayload:payload,creatorStateConsumptionProof})).idempotent,true);assert.equal(physicalIndexReads,1,"successful candidate physical readiness must be cached across idempotent replay");await assert.rejects(()=>store.stageCandidate({execution,resultPayload:{response:"different"},creatorStateConsumptionProof}),e=>e.code==="MOVIE_MENTOR_RESULT_CANDIDATE_CONFLICT");
let canonical=null,settleCalls=0;const existing={found:true,phase:"closing",executionId:"execution-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"request-1"};const recovered=await recoverStagedResultTurn({existing,inferenceExecutionAuthority:{readResultCandidate:async()=>staged,reconcileExecutionClosure:async()=>({authorized:true,closed:true,currentRealityVerified:true,executionId:"execution-1",closureReference:"closure-1",closureCertificateDigest:"closure-digest"}),readCanonicalResult:async()=>canonical,commitCanonicalResult:async({result})=>(canonical={authorized:true,committed:true,currentRealityVerified:true,candidateLineageVerified:true,resultFinalizationVerified:true,executionPhase:"finalized",providerEffectRealityRevision:1,executionId:"execution-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"request-1",resultReference:"result-1",candidateReference:staged.candidateReference,resultDigest:hash(result),closureReference:"closure-1",closureCertificateDigest:"closure-digest",resultPayload:structuredClone(result)})},settlementAuthority:{reconcile:async()=>{settleCalls++;return{authorized:true,settled:true,outcome:"consumed",resultFinalizationVerified:true,executionPhase:"settled",providerEffectRealityRevision:1,executionId:"execution-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",resultReference:"result-1",candidateReference:staged.candidateReference,resultDigest:canonical.resultDigest,closureCertificateDigest:"closure-digest",idempotent:false};}}});assert.equal(recovered.response,"creator-result");assert.equal(recovered.metadata.canonicalResult.replayedFromDurableResult,true);assert.equal(recovered.metadata.canonicalResult.settlementExecutionPhase,"settled");assert.equal(recovered.metadata.canonicalResult.creatorResponseAuthorityVerified,true);assert.equal(settleCalls,1);await assert.rejects(()=>recoverStagedResultTurn({existing:{found:true,phase:"closing",executionId:"execution-2"},inferenceExecutionAuthority:{readResultCandidate:async()=>null},settlementAuthority:{}}),e=>e.code==="MOVIE_MENTOR_RESULT_CANDIDATE_RECOVERY_REQUIRED");
const runtime=fs.readFileSync(new URL("../ai/MovieMentorTurnRuntime.js",import.meta.url),"utf8"),candidate=fs.readFileSync(new URL("../ai/MovieMentorResultCandidateMongoStore.js",import.meta.url),"utf8"),composition=fs.readFileSync(new URL("../ai/MovieMentorProductionInferenceExecutionComposition.js",import.meta.url),"utf8"),gateway=fs.readFileSync(new URL("../movieMentorTurn.js",import.meta.url),"utf8"),canonicalAuthority=fs.readFileSync(new URL("../ai/MovieMentorCanonicalResultAuthority.js",import.meta.url),"utf8");const orchestrate=runtime.indexOf("result = await orchestrate("),stage=runtime.indexOf("stageResultCandidate(",orchestrate),closing=runtime.indexOf("beginExecutionClosing(",stage),canonicalCommit=runtime.indexOf("commitCanonicalResult(",closing),settle=runtime.indexOf("settlementAuthority.reconcile(",canonicalCommit);assert.ok(orchestrate>=0&&stage>orchestrate&&closing>stage&&canonicalCommit>closing&&settle>canonicalCommit);assert.match(runtime,/recoverStagedResultTurn/);assert.match(runtime,/executionPhase !== "settled"/);assert.match(runtime,/settlementBindingKeys[\s\S]*"candidateReference"/);assert.match(runtime,/readResultCandidate/);assert.match(candidate,/session\.withTransaction/);assert.match(candidate,/creatorStateLedger\(\)\.updateOne/);assert.match(candidate,/creatorStateRevision/);assert.match(candidate,/creatorStateGeneration/);assert.match(candidate,/creatorStateFingerprint/);assert.match(candidate,/MOVIE_MENTOR_RESULT_CANDIDATE_CREATOR_STATE_FENCED/);assert.match(candidate,/executionLedger\(\)\.updateOne/);assert.match(candidate,/resultCandidateBarrierRevision:1/);assert.match(candidate,/phase:"active"/);assert.match(candidate,/leaseExpiresAt:\{\$gt:record\.stagedAt\}/);assert.match(candidate,/MOVIE_MENTOR_RESULT_CANDIDATE_CONFLICT/);assert.match(candidate,/physicalUniqueIndexReadiness:true/);assert.match(composition,/createMovieMentorResultCandidateMongoStore\(\{readIndexes:readPhysicalIndexes\}\)/);assert.match(composition,/creatorStateConsumptionProof/);assert.match(composition,/stageResultCandidate/);assert.match(composition,/readResultCandidate:durableCandidateStore\.readByExecution/);assert.match(canonicalAuthority,/readResultCandidate/);assert.match(canonicalAuthority,/candidateLineageVerified:true/);assert.match(gateway,/durableResultCandidateRequired:true/);assert.match(gateway,/stageResultCandidate/);
console.log("5A.24 staged result crash recovery catastrophe gate: GREEN");console.log("✓ orchestration result is immutable and durable only after owned physical uniqueness is proven, before execution enters CLOSING");console.log("✓ staging atomically fences both current creator state and the execution ledger before candidate persistence");console.log("✓ candidate durability preserves exact creator-state provenance that earned admission");console.log("✓ ACK-loss replay is idempotent; conflicting candidate cannot replace the first result");console.log("✓ canonical finalization is wired to revalidate exact durable candidate lineage");console.log("✓ CLOSING retry can recover staged payload -> current closure -> canonical result -> atomic SETTLED debit without provider replay");console.log("LAW: NO OWNED PHYSICAL UNIQUENESS + NO CURRENT CREATOR-STATE ATOMIC FENCE + NO DURABLE PRE-CLOSURE RESULT CANDIDATE + NO SHARED CLOSURE SERIALIZATION + NO CANONICAL LINEAGE PROOF + NO SETTLED DEBIT PROOF -> NO CRASHED-SUCCESS RECOVERY AUTHORITY");