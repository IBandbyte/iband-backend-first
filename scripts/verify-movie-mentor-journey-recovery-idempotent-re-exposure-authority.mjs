import assert from "node:assert/strict";
import { createMovieMentorJourneyRecoveryExpressRouter } from "../ai/MovieMentorJourneyRecoveryExpressRouterFactory.js";
console.log("5A.29 — Journey recovery idempotent historical re-exposure authority torture");
function makeResponse(){return{statusCode:200,body:null,exposures:0,forbidden:0,status(code){this.statusCode=code;return this;},json(body){this.body=body;if(body?.success===true)this.exposures+=1;if(this.statusCode===403&&body?.success===false)this.forbidden+=1;return this;}};}
async function run({revokeAfterIdempotentResolution=false}={}){let ownershipCurrent=true,idempotentResolved=false,authorizeCalls=0;const requestAuthority={async authorize(){authorizeCalls+=1;return ownershipCurrent?{authorized:true,principalId:"creator-1",projectId:"project-1",ownershipRef:"ownership-A",ownershipRevision:7,authenticationSource:"torture",authorizationSource:"torture"}:{authorized:false,principalId:"creator-1",projectId:"project-1"};}};const publicationBoundary={async publish(){idempotentResolved=true;return{recoveryStatus:"idempotent",projectId:"project-1",principalId:"creator-1",ownershipRef:"ownership-A",ownershipRevision:7,recoveryRevision:7,recoveryGeneration:7,recoveryReference:"recovery-7",recoveryFingerprint:"recovery-fp-7",lineageId:"lineage-1",authorityGeneration:11,progressionRevision:19,envelopeFingerprint:"fingerprint-existing-checkpoint",capturedAt:"2026-09-05T23:00:00.000Z"};}};const router=createMovieMentorJourneyRecoveryExpressRouter({verifyCredential:async()=>({}),expectedIssuer:"issuer",expectedAudience:"audience",createRequestAuthority:()=>requestAuthority,createPublicationBoundary:()=>publicationBoundary,createHttpAdapter:({publicationBoundary})=>({async handle({request,projectId}){const publication=await publicationBoundary.publish({request,projectId,expectedRecoveryRevision:7,envelope:{}});assert.equal(publication.recoveryStatus,"idempotent");if(revokeAfterIdempotentResolution&&idempotentResolved)ownershipCurrent=false;return{statusCode:200,authorityBinding:{principalId:publication.principalId,projectId:publication.projectId,ownershipRef:publication.ownershipRef,ownershipRevision:publication.ownershipRevision},body:{success:true,status:publication.recoveryStatus,projectId:publication.projectId,recoveryRevision:publication.recoveryRevision,recoveryGeneration:publication.recoveryGeneration}};}})});const layer=router.stack.find(entry=>entry.route?.path==="/:projectId/recovery");assert.ok(layer);const res=makeResponse();await layer.route.stack[0].handle({params:{projectId:"project-1"},body:{expectedRecoveryRevision:7,envelope:{}},headers:{authorization:"Bearer token"}},res);return{res,authorizeCalls};}
{const{res,authorizeCalls}=await run({revokeAfterIdempotentResolution:false});assert.equal(res.exposures,1);assert.equal(res.statusCode,200);assert.equal(authorizeCalls,1);}
{const{res,authorizeCalls}=await run({revokeAfterIdempotentResolution:true});assert.equal(res.exposures,0);assert.equal(res.statusCode,403);assert.equal(res.forbidden,1);assert.equal(authorizeCalls,1);}
console.log("PASS Journey recovery idempotent historical re-exposure authority torture.");
console.log("LAW: idempotent history may preserve recovery reality; current ownership generation independently authorizes creator-facing re-exposure.");
