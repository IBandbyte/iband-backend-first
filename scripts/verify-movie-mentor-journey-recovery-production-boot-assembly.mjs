import assert from "node:assert/strict";
import fs from "node:fs";
import {assembleMovieMentorJourneyRecoveryProductionBoot} from "../ai/MovieMentorJourneyRecoveryProductionBootAssembly.js";
function app(){return{use(){}};} const verifyCredential=async()=>({verified:true});
const auth=Object.freeze({ready:true,bootWired:true,verifyCredential,expectedIssuer:"issuer",expectedAudience:"audience"});const activation=Object.freeze({ready:true,bootWired:true,activationAuthority:async()=>({}),renewActivation:async()=>({}),assertFence:async()=>({}),processInstanceId:"p",deploymentId:"d"});
const exposure=Object.freeze({authorized:true,reason:"production-recovery-exposure-authorized",bootAuthentication:auth,bootActivation:activation,ownerBoundProof:true});
let mountCalls=0;const forged=await assembleMovieMentorJourneyRecoveryProductionBoot({app:app(),createBootAuthentication:()=>auth,createBootActivation:()=>activation,authorizeExposure:()=>Object.freeze({...exposure}),isExposureOwnedProof:v=>v===exposure,configureBootMount:async()=>{mountCalls++;return{mounted:true};}});assert.equal(forged.mounted,false);assert.equal(forged.reason,"production-recovery-exposure-owner-proof-not-proven");assert.equal(mountCalls,0);
let mountInput=null;const mounted=await assembleMovieMentorJourneyRecoveryProductionBoot({app:app(),createBootAuthentication:()=>auth,createBootActivation:()=>activation,authorizeExposure:()=>exposure,isExposureOwnedProof:v=>v===exposure,configureBootMount:async input=>{mountInput=input;return Object.freeze({mountable:true,mounted:true,reason:"recovery-route-mounted",basePath:"/api/movie-mentor-recovery"});}});assert.equal(mounted.mounted,true);assert.equal(mounted.exposureProof,exposure);assert.equal(mountInput.verifyCredential,auth.verifyCredential);assert.equal(mountInput.activationAuthority,activation.activationAuthority);
const wrongBinding=Object.freeze({...exposure,bootAuthentication:Object.freeze({...auth})});const denied=await assembleMovieMentorJourneyRecoveryProductionBoot({app:app(),createBootAuthentication:()=>auth,createBootActivation:()=>activation,authorizeExposure:()=>wrongBinding,isExposureOwnedProof:()=>true,configureBootMount:async()=>{throw new Error("must not mount");}});assert.equal(denied.mounted,false);assert.equal(denied.reason,"production-recovery-exposure-owner-proof-not-proven");
const assembly=fs.readFileSync(new URL("../ai/MovieMentorJourneyRecoveryProductionBootAssembly.js",import.meta.url),"utf8");assert.match(assembly,/isMovieMentorJourneyRecoveryProductionExposureOwnedProof/);assert.match(assembly,/exposure\.bootAuthentication!==bootAuthentication/);assert.match(assembly,/exposure\.bootActivation!==bootActivation/);assert.match(assembly,/production-recovery-exposure-owner-proof-not-proven/);
console.log("PASS ROUND SEVEN: final recovery mount consumes exact exposure owner proof and exact boot lineage.");console.log("LAW: BOOT OWNER PROOFS -> EXPOSURE OWNER PROOF -> FINAL ASSEMBLY -> ROUTE.");console.log("🐔 Zorg: 'Structurally equivalent exposure paperwork?' 🏏💥 NO.");
