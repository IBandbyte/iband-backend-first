import assert from "node:assert/strict";
import mongoose from "mongoose";
import { createMovieMentorInferenceExecutionMongoStore } from "../ai/MovieMentorInferenceExecutionMongoStore.js";
function q(value){return{lean(){return this;},async exec(){return value?structuredClone(value):null;}};}
function execution(schema){return{domain:"iband.movie-mentor.inference-execution-store",schema,executionId:`execution-schema-${schema}`,creatorTurnId:`turn-schema-${schema}`,principalId:"creator-schema-court",projectId:"project-schema-court",reservationId:`reservation-schema-${schema}`,requestDigest:`request-schema-${schema}`,phase:"active",ownerId:"owner-schema-court",leaseGeneration:4,leaseReference:"lease-schema-court",fencingToken:"fence-schema-court",leaseAcquiredAt:new Date("2035-01-01T00:00:00.000Z"),leaseExpiresAt:new Date("2035-01-01T00:10:00.000Z"),maxProviderCalls:2,providerCallsClaimed:0,providerCalls:[],providerEffectRealityRevision:0};}
async function attempt(durableSchema){let row=execution(durableSchema),filterSeen=null;const fake={findOne(filter){return q(row&&row.executionId===filter.executionId?row:null);},findOneAndUpdate(filter,update){filterSeen=structuredClone(filter);const schemaMatches=filter.schema===undefined||filter.schema===row.schema;const matches=schemaMatches&&filter.executionId===row.executionId&&filter.phase===row.phase&&filter.ownerId===row.ownerId&&filter.leaseGeneration===row.leaseGeneration&&filter.leaseReference===row.leaseReference&&filter.fencingToken===row.fencingToken;if(matches){row={...row,...(update.$set||{}),providerCalls:[...row.providerCalls,...(update.$push?.providerCalls?[update.$push.providerCalls]:[])],providerCallsClaimed:row.providerCallsClaimed+(update.$inc?.providerCallsClaimed||0)};return q(row);}return q(null);}};const previous=mongoose.models.MovieMentorInferenceExecution;mongoose.models.MovieMentorInferenceExecution=fake;try{const store=createMovieMentorInferenceExecutionMongoStore({connect:async()=>{}});const result=await store.claimProviderCall({executionId:row.executionId,ownerId:row.ownerId,leaseGeneration:row.leaseGeneration,leaseReference:row.leaseReference,fencingToken:row.fencingToken,providerCallId:`call-schema-${durableSchema}`,slotId:"semantic",task:"movie-mentor-semantic",admittedAt:new Date("2035-01-01T00:01:00.000Z")});return{result,filterSeen,row};}finally{if(previous)mongoose.models.MovieMentorInferenceExecution=previous;else delete mongoose.models.MovieMentorInferenceExecution;}}
const current=await attempt(6);assert.equal(current.result.claimed,true,"current schema 6 must admit provider call");
const legacy=await attempt(5);assert.equal(legacy.result.claimed,false,"legacy schema 5 must not acquire a new durable provider-call claim");
assert.equal(current.filterSeen?.schema,6,"provider-call atomic filter must own current schema 6");assert.equal(legacy.filterSeen?.schema,6,"legacy row must not be silently upgraded while acquiring provider-call authority");
console.log("GREEN: provider-call store atomically owns current execution schema at durable claim.");
console.log("LAW: THE STORE THAT MINTS A PROVIDER-CALL CLAIM MAY NOT BORROW CURRENT-SCHEMA AUTHORITY FROM ITS CALLER OR UPGRADE LEGACY HISTORY AS A SIDE EFFECT.");
