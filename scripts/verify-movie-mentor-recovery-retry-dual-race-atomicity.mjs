import assert from"node:assert/strict";import{retryFinalRecovery}from"../ai/MovieMentorRecoveryFinalizationRetryControl.js";
const identity={incidentId:"cat-1",recoveryEpoch:9,authorityDecisionReference:"decision-cat1-e9",authorityReference:"recovery-root-A",bindingFingerprint:"a".repeat(64),previousStateReference:"distributed-state-cat1-e9",transitionReference:"global:decision-cat1-e9"},absenceProof={valid:true,safeToRetry:true,retryMustUseSameIdentity:true,reference:"transaction-absence:A|B"},gateAbsenceProof={valid:true,absent:true,authoritative:true,quorum:true,reference:"retry-gate-absence:cat-1:9:A|B"};
// Both historical proofs are valid. At mutation time BOTH forbidden facts appear concurrently.
let casAttempts=0,globalMutations=0,durableReads=0;let r=await retryFinalRecovery({originalIdentity:identity,retryIdentity:structuredClone(identity),absenceProof,gateAbsenceProof},{commitFinalRecoveryRetry:async({requirements})=>{casAttempts++;assert.equal(requirements.recheckGateAbsenceAtMutation,true);assert.equal(requirements.recheckTransactionAbsenceAtMutation,true);const gateExists=true,transactionExists=true;if(gateExists||transactionExists)return{committed:false,indeterminate:false,reason:"dual_absence_invalidated_before_retry_cas"};globalMutations++;return{committed:true}},readFinalRecoveryByIdentity:async()=>{durableReads++;throw new Error("denied CAS has no retry commit to read")}});assert.equal(r.retried,false);assert.equal(r.indeterminate,false);assert.ok(r.reasons.includes("dual_absence_invalidated_before_retry_cas"));assert.equal(casAttempts,1);assert.equal(globalMutations,0);assert.equal(durableReads,0);
// Prove the storage contract cannot claim success unless ALL comparison fences were executed in the same atomic transaction.
const incomplete=["gateAbsenceCompared","transactionAbsenceCompared","identityCompared","previousStateReferenceCompared","authorityHighWatermarkCompared","noExistingMatchingCommitCompared","sameAtomicTransaction"];
for(const missing of incomplete){const commit={committed:true,gateAbsenceCompared:true,transactionAbsenceCompared:true,identityCompared:true,previousStateReferenceCompared:true,authorityHighWatermarkCompared:true,noExistingMatchingCommitCompared:true,sameAtomicTransaction:true,reference:"retry-commit"};commit[missing]=false;let reads=0;r=await retryFinalRecovery({originalIdentity:identity,retryIdentity:identity,absenceProof,gateAbsenceProof},{commitFinalRecoveryRetry:async()=>commit,readFinalRecoveryByIdentity:async()=>{reads++;return{found:true}}});assert.equal(r.retried,false,missing);assert.equal(r.indeterminate,true,missing);assert.ok(r.reasons.includes("final_retry_cas_fence_incomplete"),missing);assert.equal(reads,0,missing)}
// Happy boundary: both are still absent and every fence is proven; only then may durable readback occur.
let reads=0;r=await retryFinalRecovery({originalIdentity:identity,retryIdentity:identity,absenceProof,gateAbsenceProof},{commitFinalRecoveryRetry:async()=>({committed:true,gateAbsenceCompared:true,transactionAbsenceCompared:true,identityCompared:true,previousStateReferenceCompared:true,authorityHighWatermarkCompared:true,noExistingMatchingCommitCompared:true,sameAtomicTransaction:true,reference:"retry-commit-ok"}),readFinalRecoveryByIdentity:async()=>{reads++;return{found:true,durable:true,sameAtomicTransaction:true,commitReference:"retry-commit-ok",authorityHighWatermark:{recoveryEpoch:9,decisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint},globalState:{state:"globally_recovered",recoveryEpoch:9,authorityDecisionReference:identity.authorityDecisionReference,bindingFingerprint:identity.bindingFingerprint,transitionReference:identity.transitionReference},reference:"retry-readback-ok"}});assert.equal(r.retried,true);assert.equal(r.status,"globally_recovered");assert.equal(reads,1);
console.log("DUAL-RACE ATOMICITY PASSED: simultaneous late gate + late original transaction cannot split finalization; every gate/transaction/identity/HWM fence must be proven inside one atomic retry CAS, otherwise outcome is indeterminate and no readback success is accepted.");