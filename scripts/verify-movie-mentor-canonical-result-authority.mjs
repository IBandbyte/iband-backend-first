import assert from "node:assert/strict";
import { createMovieMentorCanonicalResultAuthority } from "../ai/MovieMentorCanonicalResultAuthority.js";

const results=new Map();let closureCurrent=true,currentReason="";
const currentClosure=Object.freeze({authorized:true,closed:true,currentRealityVerified:true,executionId:"exec-1",creatorTurnId:"turn-1",principalId:"creator-1",projectId:"project-1",reservationId:"reservation-1",requestDigest:"request-digest",closureReference:"closure-1",closureCertificateDigest:"closure-digest-1"});
const store={async readByExecution(id){return results.get(id)||null;},async commit(record){if(results.has(record.executionId))return results.get(record.executionId);const frozen=Object.freeze({...record});results.set(record.executionId,frozen);return frozen;}};
const assertCurrentClosure=async input=>closureCurrent&&input.executionId==="exec-1"&&(!input.closureReference||input.closureReference==="closure-1")&&(!input.closureCertificateDigest||input.closureCertificateDigest==="closure-digest-1")?currentClosure:{authorized:false,closed:false,currentRealityVerified:false,reason:currentReason||"provider-effect-conflict"};
const authority=createMovieMentorCanonicalResultAuthority({store,assertCurrentClosure,now:()=>new Date("2026-08-30T12:00:00.000Z"),randomId:()=>"result-1"});
const closure={authorized:true,closed:true,currentRealityVerified:true,executionId:"exec-1",closureReference:"closure-1",closureCertificateDigest:"closure-digest-1"},payload={response:{message:"Creator result"},metadata:{agent:"mentor"}};
const first=await authority.commitResult({closure,result:payload});assert.equal(first.authorized,true);assert.equal(first.currentRealityVerified,true);assert.equal(first.idempotent,false);assert.equal(first.reservationId,"reservation-1");assert.equal(first.resultReference,"canonical-result-result-1");
const replay=await authority.commitResult({closure,result:{metadata:{agent:"mentor"},response:{message:"Creator result"}}});assert.equal(replay.idempotent,true);assert.equal(replay.resultDigest,first.resultDigest);await assert.rejects(()=>authority.commitResult({closure,result:{response:{message:"different"}}}),e=>e.code==="MOVIE_MENTOR_CANONICAL_RESULT_CONFLICT");
closureCurrent=false;currentReason="late-provider-effect-conflict";const stale=await authority.readResult({executionId:"exec-1"});assert.equal(stale.authorized,false);assert.equal(stale.committed,true);assert.equal(stale.reason,"canonical-result-current-closure-invalid");await assert.rejects(()=>authority.commitResult({closure,result:payload}),e=>e.code==="MOVIE_MENTOR_CANONICAL_RESULT_CLOSURE_STALE");
closureCurrent=true;const current=await authority.readResult({executionId:"exec-1"});assert.equal(current.authorized,true);assert.equal(current.currentRealityVerified,true);assert.equal(current.resultDigest,first.resultDigest);
console.log("GREEN: canonical result is immutable, replayable and revalidated against current CLOSED provider-effect reality before it can authorize downstream settlement.");
console.log("LAW: A HISTORICAL CLOSURE CERTIFICATE IS NOT AN ETERNAL BEARER TOKEN; CURRENT PROVIDER REALITY MUST STILL SUPPORT IT.");
