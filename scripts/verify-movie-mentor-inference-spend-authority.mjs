import assert from "node:assert/strict";import fs from "node:fs";import {createMovieMentorInferenceSpendAuthority} from "../ai/MovieMentorInferenceSpendAuthority.js";import {createMovieMentorProductionInferenceSpendComposition} from "../ai/MovieMentorProductionInferenceSpendComposition.js";
function reservation(request,revision=2){return{...request,entitlementRevision:revision,status:"reserved",reservedAt:new Date().toISOString()};}async function rejectsCode(fn,code){await assert.rejects(fn,e=>e?.code===code);}const binding=Object.freeze({authenticated:true,projectAuthorized:true,principalId:"creator-1",projectId:"project-1"});
{const authority=createMovieMentorInferenceSpendAuthority({store:{reserve:async request=>({granted:true,reservation:reservation(request)})},createReservationId:()=>"reservation-1"});const result=await authority.reserveTurn({serverAuthority:binding,projectId:"project-1"});assert.equal(result.authorized,true);assert.equal(result.reservationId,"reservation-1");assert.equal(result.operation,"movie-mentor-turn");}
{const authority=createMovieMentorInferenceSpendAuthority({store:{reserve:async()=>({granted:false,reason:"empty"})}});await rejectsCode(()=>authority.reserveTurn({serverAuthority:binding,projectId:"project-1"}),"MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_DENIED");}
{const authority=createMovieMentorInferenceSpendAuthority({store:{reserve:async()=>{throw Object.assign(new Error("down"),{code:"MOVIE_MENTOR_INFERENCE_SPEND_AUTHORITY_UNAVAILABLE"});}}});await rejectsCode(()=>authority.reserveTurn({serverAuthority:binding,projectId:"project-1"}),"MOVIE_MENTOR_INFERENCE_SPEND_AUTHORITY_UNAVAILABLE");}
{const authority=createMovieMentorInferenceSpendAuthority({store:{reserve:async request=>({granted:true,reservation:reservation({...request,projectId:"project-evil"})})}});await rejectsCode(()=>authority.reserveTurn({serverAuthority:binding,projectId:"project-1"}),"MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_INVALID");}
{const authority=createMovieMentorInferenceSpendAuthority({store:{reserve:async request=>({granted:true,reservation:reservation(request)})}});await rejectsCode(()=>authority.reserveTurn({serverAuthority:{...binding,principalId:""},projectId:"project-1"}),"MOVIE_MENTOR_INFERENCE_SPEND_PRINCIPAL_REQUIRED");await rejectsCode(()=>authority.reserveTurn({serverAuthority:binding,projectId:"project-2"}),"MOVIE_MENTOR_INFERENCE_SPEND_PROJECT_CONFLICT");await rejectsCode(()=>authority.reserveTurn({serverAuthority:{...binding,authenticated:false},projectId:"project-1"}),"MOVIE_MENTOR_INFERENCE_SPEND_AUTHORITY_REQUIRED");}
{let remaining=1;const atomicStore={reserve:async request=>{await Promise.resolve();if(remaining<request.units)return{granted:false,reason:"empty"};remaining-=request.units;return{granted:true,reservation:reservation(request)};}};let id=0;const authority=createMovieMentorInferenceSpendAuthority({store:atomicStore,createReservationId:()=>`race-${++id}`});const results=await Promise.allSettled([authority.reserveTurn({serverAuthority:binding,projectId:"project-1"}),authority.reserveTurn({serverAuthority:binding,projectId:"project-1"})]);assert.equal(results.filter(r=>r.status==="fulfilled").length,1);assert.equal(results.filter(r=>r.status==="rejected"&&r.reason?.code==="MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_DENIED").length,1);assert.equal(remaining,0);}
{const originalMongo=process.env.MONGO_URI,originalMongoDb=process.env.MONGODB_URI;delete process.env.MONGO_URI;delete process.env.MONGODB_URI;const closed=createMovieMentorProductionInferenceSpendComposition();assert.equal(closed.ready,false);assert.equal(closed.reason,"inference-spend-store-not-configured");if(originalMongo===undefined)delete process.env.MONGO_URI;else process.env.MONGO_URI=originalMongo;if(originalMongoDb===undefined)delete process.env.MONGODB_URI;else process.env.MONGODB_URI=originalMongoDb;}
{const methodOnly={reserve:async request=>({granted:true,reservation:reservation(request)})};const rejected=createMovieMentorProductionInferenceSpendComposition({store:methodOnly});assert.equal(rejected.ready,false);assert.equal(rejected.reason,"inference-spend-injected-capability-not-proven");assert.equal(rejected.authority,null);}
{const selfDescribed={reserve:async request=>({granted:true,reservation:reservation(request)}),getStatus:()=>({configured:true,readiness:"injected"})};const rejected=createMovieMentorProductionInferenceSpendComposition({store:selfDescribed});assert.equal(rejected.ready,false);assert.equal(rejected.reason,"inference-spend-injected-capability-not-proven");}
{const storeStatus=Object.freeze({configured:true,readiness:"injected-proven",atomicity:"mongo-transaction",settlement:"external-durable-current-reality-authority-only",durableReservationRead:true,genericSettlementCapability:false,processLocalFallback:false});const proven={reserve:async request=>({granted:true,reservation:reservation(request)}),readReservation:async()=>null,getStatus:()=>storeStatus};const composed=createMovieMentorProductionInferenceSpendComposition({store:proven});assert.equal(composed.ready,true);assert.equal(typeof composed.authority.reserveTurn,"function");assert.equal(typeof composed.getStatus,"function");const ownerProof=composed.getStatus();assert.equal(ownerProof,composed.status);assert.equal(composed.getStatus(),ownerProof,"repeated getStatus must return the exact owner proof object");assert.equal(Object.isFrozen(ownerProof),true);assert.equal(ownerProof.domain,"iband.movie-mentor.production-inference-spend-composition");assert.equal(ownerProof.storeStatus,storeStatus,"owner proof must bind the exact consumed store proof");const reconstructed=Object.freeze({...ownerProof});assert.deepEqual(reconstructed,ownerProof);assert.notEqual(reconstructed,ownerProof,"structural equality must not reincarnate owner proof identity");const lookalike=Object.freeze({...composed,status:reconstructed,getStatus:()=>reconstructed});assert.notEqual(lookalike.getStatus(),composed.status,"lookalike composition must not possess the instantiated owner's proof");const result=await composed.authority.reserveTurn({serverAuthority:binding,projectId:"project-1"});assert.equal(result.authorized,true);}
const runtimeSource=fs.readFileSync(new URL("../ai/MovieMentorTurnRuntime.js",import.meta.url),"utf8"),gatewaySource=fs.readFileSync(new URL("../movieMentorTurn.js",import.meta.url),"utf8"),serverSource=fs.readFileSync(new URL("../server.js",import.meta.url),"utf8"),storeSource=fs.readFileSync(new URL("../ai/MovieMentorInferenceSpendMongoStore.js",import.meta.url),"utf8"),compositionSource=fs.readFileSync(new URL("../ai/MovieMentorProductionInferenceSpendComposition.js",import.meta.url),"utf8"),envSource=fs.readFileSync(new URL("../.env.example",import.meta.url),"utf8");
const reserveIndex=runtimeSource.indexOf("spendAuthority.reserveTurn"),orchestrateIndex=runtimeSource.indexOf("orchestrate(");assert.ok(reserveIndex>=0&&orchestrateIndex>reserveIndex,"spend reservation must precede orchestration");assert.match(runtimeSource,/MOVIE_MENTOR_INFERENCE_SPEND_AUTHORITY_REQUIRED/);assert.match(runtimeSource,/assertRuntimeServerAuthority\(\{ serverAuthority: deps\.serverAuthority, requestedProjectId: identity\.projectId \}\)/);assert.match(runtimeSource,/reserveTurn\(\{ serverAuthority: deps\.serverAuthority, projectId: durableProjectId \}\)/);assert.match(gatewaySource,/serverAuthorityFrom\(authorized\.authority\)/);assert.doesNotMatch(gatewaySource,/serverAuthority:authorized\.body/);assert.match(gatewaySource,/MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_DENIED/);assert.match(gatewaySource,/return 402/);assert.match(gatewaySource,/router\.post\("\/state\/sync"/);assert.match(serverSource,/createMovieMentorProductionInferenceSpendComposition/);assert.match(serverSource,/spendCompositionProven\(spendComposition,spendStatus\)/);assert.match(serverSource,/status===composition\?\.status/);assert.match(serverSource,/status\?\.domain===SPEND_DOMAIN/);assert.match(serverSource,/status\?\.storeStatus===composition\?\.storeStatus/);assert.match(serverSource,/production-inference-spend-capability-not-proven/);assert.match(serverSource,/createMovieMentorProductionInferenceSettlementComposition/);assert.match(serverSource,/inferenceSpendAuthority:spendComposition\.authority/);assert.match(serverSource,/inferenceExecutionAuthority:executionAuthority/);assert.match(serverSource,/inferenceSettlementAuthority:settlementComposition\.authority/);assert.match(serverSource,/executionComposition\?\.ready\s*!==\s*true/);assert.doesNotMatch(serverSource,/createMovieMentorInferenceSettlementReconciliationAuthority/);assert.doesNotMatch(serverSource,/app\.use\("\/api\/movie-mentor-semantic"/);assert.doesNotMatch(serverSource,/app\.use\("\/api\/movie-mentor-specialists"/);assert.doesNotMatch(serverSource,/app\.use\("\/api\/movie-mentor-synthesis"/);assert.doesNotMatch(serverSource,/app\.use\("\/api\/ai-mentor"/);assert.match(serverSource,/browserOriginAuthority\.authorizeRequest/);assert.match(storeSource,/withTransaction/);assert.match(storeSource,/remainingUnits:\{\$gte:n\.units\}/);assert.match(storeSource,/remainingUnits:-n\.units,reservedUnits:n\.units/);assert.match(storeSource,/reservationSchema\.index\(\{reservationId:1\},\{unique:true\}\)/);assert.match(storeSource,/processLocalFallback:false/);assert.doesNotMatch(compositionSource,/readiness:\s*["']injected["']/);assert.match(compositionSource,/inference-spend-injected-capability-not-proven/);assert.match(compositionSource,/getStatus\(\) \{ return status; \}/);assert.match(envSource,/No process-local inference-credit fallback is permitted/);console.log("✓ production spend composition owns one immutable stable proof and the HTTP mount consumes that exact owner proof");console.log("LAW: NO COMPONENT GETS CREDIT FOR ITS NEIGHBOUR'S PROOF. PROOF DOES NOT REINCARNATE.");console.log("Movie Mentor 5A.5 inference spend authority torture: PASS");