import assert from "node:assert/strict";
import crypto from "node:crypto";
import {createMovieMentorStripeCommercialProviderAdapter} from "../ai/MovieMentorStripeCommercialProviderAdapter.js";
import {createMovieMentorCommercialPaymentEvidenceAuthority} from "../ai/MovieMentorCommercialPaymentEvidenceAuthority.js";
import {createMovieMentorCommercialProviderIngressAuthority} from "../ai/MovieMentorCommercialProviderIngressAuthority.js";
function digest(value){return crypto.createHash("sha256").update(JSON.stringify(value)).digest("hex");}
const stage=process.argv[2]||"full",commercialIntentId="intent-stripe-contract",checkoutReference="cs_contract_1",providerPaymentReference="pi_contract_1";
const snapshot={packageId:"creator-20",provider:"stripe",providerProductId:"price-20",amountMinor:1200,currency:"GBP",environment:"live",units:20,policyVersion:"v1"};const intent=Object.freeze({commercialIntentId,principalId:"creator-1",...snapshot,policyDigest:digest(snapshot),status:"created"});
const signedEvent=Object.freeze({id:"evt_contract_1",type:"checkout.session.completed",livemode:true,data:{object:{id:checkoutReference,client_reference_id:commercialIntentId,payment_intent:providerPaymentReference,metadata:{commercialIntentId,providerProductId:"price-20"},payment_status:"paid",amount_total:1200,currency:"gbp"}}});
const stripe={checkout:{sessions:{create:async()=>{throw new Error("checkout creation is not part of this court");}}},webhooks:{constructEvent(){return signedEvent;}}};const adapter=createMovieMentorStripeCommercialProviderAdapter({stripe,webhookSecret:"whsec_test",successUrl:"https://app.example/success",cancelUrl:"https://app.example/cancel"}),delivery={rawBody:Buffer.from("signed"),signature:"sig"};
const purchaseIntentAuthority={resolvePurchaseIntent:async({commercialIntentId:id})=>id===commercialIntentId?intent:null,getStatus:()=>Object.freeze({domain:"iband.movie-mentor.production-commercial-purchase-intent-authority",production:true,durablePurchaseIntent:true,immutableCommercialTerms:true,serverOwnedPolicy:true,processLocalFallback:false})};
const checkoutStatus=Object.freeze({domain:"iband.movie-mentor.production-commercial-checkout-authority",production:true,durableCheckoutBinding:true,checkoutBindingResolution:true,providerPaymentReferenceBinding:true,providerPaymentReferenceResolution:true,serverOwnedIdempotency:true,purchaseIntentProvenanceRequired:true,explicitProviderRequired:true,processLocalFallback:false});let boundPayment=null;
const checkoutBindingAuthority={resolveCheckoutBinding:async({commercialIntentId:id})=>id===commercialIntentId?Object.freeze({commercialIntentId,provider:"stripe",status:"completed",checkoutReference,providerPaymentReference:boundPayment}):null,bindProviderPaymentReference:async({commercialIntentId:id,provider,checkoutReference:ref,providerPaymentReference:payment})=>{assert.equal(id,commercialIntentId);assert.equal(provider,"stripe");assert.equal(ref,checkoutReference);boundPayment=payment;return Object.freeze({commercialIntentId,provider,status:"completed",checkoutReference:ref,providerPaymentReference:payment});},resolveCheckoutBindingByProviderPaymentReference:async({provider,providerPaymentReference:payment})=>provider==="stripe"&&payment===boundPayment?Object.freeze({commercialIntentId,provider,status:"completed",checkoutReference,providerPaymentReference:payment}):null,getStatus:()=>checkoutStatus};
const issuanceStatus=Object.freeze({domain:"iband.movie-mentor.production-entitlement-issuance-authority",production:true,durableAtomicIssuance:true,evidenceIdentityUnique:true,issuanceReceiptDurable:true,processLocalFallback:false}),reversalStatus=Object.freeze({domain:"iband.movie-mentor.production-commercial-reversal-authority",production:true,durableAtomicSuspension:true,reversalIdentityUnique:true,currentEntitlementSuspension:true,reversalReceiptDurable:true,pendingReversalHistoryDurable:true,pendingReversalIdentityUnique:true,pendingReversalReconciliation:true,processLocalFallback:false}),reversalAuthority={suspendVerifiedReversal:async()=>{throw new Error("reversal outside payment court");},preserveVerifiedReversalHistory:async()=>{throw new Error("pending reversal outside payment court");},reconcilePendingReversals:async()=>Object.freeze({reconciled:true,count:0}),getStatus:()=>reversalStatus};
async function proveAdapter(){const verified=await adapter.verifyDelivery({delivery}),normalized=await adapter.normalizeEvent({verifiedDelivery:verified});assert.equal(normalized.eventId,"evt_contract_1");assert.equal(normalized.eventKind,"checkout.session.completed");assert.equal(normalized.checkoutReference,checkoutReference);assert.equal(normalized.providerPaymentReference,providerPaymentReference);assert.equal(normalized.commerciallyFinal,true);return normalized;}
async function proveEvidence(){const evidenceAuthority=createMovieMentorCommercialPaymentEvidenceAuthority({verifyDelivery:({delivery:rawDelivery})=>adapter.verifyDelivery({delivery:rawDelivery}),normalizeEvent:({verifiedDelivery})=>adapter.normalizeEvent({verifiedDelivery}),resolvePurchaseIntent:purchaseIntentAuthority.resolvePurchaseIntent});const evidence=await evidenceAuthority.verifyCommercialDelivery({delivery});assert.equal(evidence.verified,true);assert.equal(evidence.evidenceId,"evt_contract_1");assert.equal(evidence.evidenceKind,"checkout.session.completed");assert.equal(evidence.commercialReference,commercialIntentId);assert.equal(evidence.checkoutReference,checkoutReference);return evidence;}
async function proveFullIngress(){let issuedEvidence=null;const issuanceAuthority={issueVerifiedEvidence:async({evidence})=>{issuedEvidence=evidence;return Object.freeze({issued:true,evidence});},getStatus:()=>issuanceStatus};const ingress=createMovieMentorCommercialProviderIngressAuthority({providers:{stripe:adapter},purchaseIntentAuthority,checkoutBindingAuthority,issuanceAuthority,reversalAuthority});const result=await ingress.processProviderDelivery({provider:"stripe",delivery});assert.equal(result?.issued,true);assert.equal(boundPayment,providerPaymentReference);assert.equal(issuedEvidence?.evidenceId,"evt_contract_1");assert.equal(issuedEvidence?.checkoutReference,checkoutReference);assert.equal(ingress.getStatus().commercialVerificationBeforePaymentLineage,true);}
async function proveCheckoutMismatchFailsClosed(){let issuanceCalls=0,bindingCalls=0;const mismatchedCheckout={resolveCheckoutBinding:async()=>Object.freeze({commercialIntentId,provider:"stripe",status:"completed",checkoutReference:"cs_different_authorized_session"}),bindProviderPaymentReference:async()=>{bindingCalls++;throw new Error("payment lineage must never be reached before commercial checkout verification");},resolveCheckoutBindingByProviderPaymentReference:async()=>null,getStatus:()=>checkoutStatus};const issuanceAuthority={issueVerifiedEvidence:async({evidence})=>{issuanceCalls++;return Object.freeze({issued:true,evidence});},getStatus:()=>issuanceStatus};const ingress=createMovieMentorCommercialProviderIngressAuthority({providers:{stripe:adapter},purchaseIntentAuthority,checkoutBindingAuthority:mismatchedCheckout,issuanceAuthority,reversalAuthority});await assert.rejects(()=>ingress.processProviderDelivery({provider:"stripe",delivery}),error=>error?.code==="MOVIE_MENTOR_COMMERCIAL_EVIDENCE_CHECKOUT_BINDING_MISMATCH");assert.equal(bindingCalls,0);assert.equal(issuanceCalls,0);}
if(stage==="adapter")await proveAdapter();else if(stage==="evidence")await proveEvidence();else if(stage==="full"){await proveAdapter();await proveEvidence();await proveFullIngress();await proveCheckoutMismatchFailsClosed();await import("./verify-movie-mentor-reversal-before-payment-lineage-authority.mjs");await import("./verify-movie-mentor-payment-lineage-after-commercial-verification-authority.mjs");}else throw new Error(`Unknown court stage: ${stage}`);
console.log(`GREEN: Stripe payment evidence contract stage ${stage} crossed its exact production-owned boundary.`);console.log("LAW: A PROVIDER ADAPTER DOES NOT OWN PAYMENT EVIDENCE UNTIL THE CONSUMING AUTHORITY CAN READ ITS EXACT EVIDENCE COORDINATES.");console.log("LAW: A SIGNED PAID PROVIDER EVENT MAY PROVE PAYMENT HISTORY; IT MAY NOT BORROW PAYMENT OR ENTITLEMENT AUTHORITY FROM A DIFFERENT CHECKOUT SESSION.");console.log("LAW: SIGNED PROVIDER TRANSPORT MAY PROVE DELIVERY; ONLY VERIFIED COMMERCIAL EVIDENCE MAY MINT DURABLE PAYMENT LINEAGE.");
