import assert from"node:assert/strict";
import{RECOVERY_AUTHORITY_BINDING_HASH_DOMAIN as DOMAIN,RECOVERY_AUTHORITY_BINDING_HASH_SCHEMA as SCHEMA}from"../ai/MovieMentorRecoveryAuthority.js";
import{reconcileFinalRecovery as reconcile}from"../ai/MovieMentorRecoveryFinalizationReconciliationControl.js";
const digest="a".repeat(64),authorityDecision={authorized:true,reference:"decision-cat1-e9",authorityReference:"recovery-root-A",bindingFingerprint:digest,bindingFingerprintDomain:DOMAIN,bindingFingerprintSchema:SCHEMA},stateTransition={applied:true,to:"globally_recovered",recoveryEpoch:9,transitionReference:"global:decision-cat1-e9",recoveryAuthorityReference:"decision-cat1-e9",recoveryAuthorityBindingFingerprint:digest,recoveryAuthorityBindingFingerprintDomain:DOMAIN,recoveryAuthorityBindingFingerprintSchema:SCHEMA},input={incidentId:"cat-1",recoveryEpoch:9,authorityDecision,stateTransition,expectedStateReference:"distributed-state-cat1-e9"},durable={found:true,durable:true,sameAtomicTransaction:true,commitReference:"final-commit-9002",authorityHighWatermark:{recoveryEpoch:9,decisionReference:"decision-cat1-e9",bindingFingerprint:digest,bindingFingerprintDomain:DOMAIN,bindingFingerprintSchema:SCHEMA},globalState:{state:"globally_recovered",recoveryEpoch:9,authorityDecisionReference:"decision-cat1-e9",bindingFingerprint:digest,bindingFingerprintDomain:DOMAIN,bindingFingerprintSchema:SCHEMA,transitionReference:"global:decision-cat1-e9"},reference:"restart-readback-9002"},absent=id=>({nodeId:id,found:false,absenceProven:true,reference:`absence-${id}`}),found=id=>({nodeId:id,found:true,reference:`found-${id}`}),authority=async({view})=>({authoritative:true,staleReplica:false,nodeId:view.nodeId});
let r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>structuredClone(durable)});assert.equal(r.reconciled,true);assert.equal(r.committed,true);assert.equal(r.bindingFingerprintDomain,DOMAIN);assert.equal(r.bindingFingerprintSchema,SCHEMA);
for(const poisoned of[{...authorityDecision,bindingFingerprintDomain:undefined,bindingFingerprintSchema:undefined},{...authorityDecision,bindingFingerprintDomain:"purple-universe"},{...authorityDecision,bindingFingerprintSchema:1},{...authorityDecision,bindingFingerprintDomain:"iband.movie-mentor.operations.reconciliation-recovery-reality"}]){r=await reconcile({...input,authorityDecision:poisoned},{readFinalRecoveryByIdentity:async()=>structuredClone(durable)});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("recovery_authority_binding_digest_protocol_mismatch"))}
for(const poisoned of[{...stateTransition,recoveryAuthorityBindingFingerprintDomain:undefined,recoveryAuthorityBindingFingerprintSchema:undefined},{...stateTransition,recoveryAuthorityBindingFingerprintDomain:"purple-universe"},{...stateTransition,recoveryAuthorityBindingFingerprintSchema:1},{...stateTransition,recoveryAuthorityBindingFingerprintDomain:"iband.movie-mentor.operations.reconciliation-recovery-reality"}]){r=await reconcile({...input,stateTransition:poisoned},{readFinalRecoveryByIdentity:async()=>structuredClone(durable)});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("recovery_state_authority_digest_protocol_mismatch"))}
for(const mutate of[h=>{delete h.bindingFingerprintDomain;delete h.bindingFingerprintSchema},h=>h.bindingFingerprintDomain="purple-universe",h=>h.bindingFingerprintSchema=1,h=>h.bindingFingerprintDomain="iband.movie-mentor.operations.reconciliation-recovery-reality"]){const x=structuredClone(durable);mutate(x.authorityHighWatermark);r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>x});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("durable_final_commit_authority_digest_protocol_mismatch"))}
for(const mutate of[g=>{delete g.bindingFingerprintDomain;delete g.bindingFingerprintSchema},g=>g.bindingFingerprintDomain="purple-universe",g=>g.bindingFingerprintSchema=1,g=>g.bindingFingerprintDomain="iband.movie-mentor.operations.reconciliation-recovery-reality"]){const x=structuredClone(durable);mutate(x.globalState);r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>x});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("durable_final_commit_authority_digest_protocol_mismatch"))}
r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>({found:false}),readAbsenceViews:async()=>[absent("B")],verifyAbsenceAuthority:authority});assert.equal(r.reconciled,false);assert.equal(r.safeToRetry,false);assert.ok(r.reasons.includes("distributed_absence_quorum_not_reached"));
r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>({found:false}),readAbsenceViews:async()=>[absent("B"),absent("C")],verifyAbsenceAuthority:authority});assert.equal(r.reconciled,true);assert.equal(r.committed,false);assert.equal(r.safeToRetry,true);assert.equal(r.identity.bindingFingerprintDomain,DOMAIN);assert.equal(r.identity.bindingFingerprintSchema,SCHEMA);
r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>({found:false}),readAbsenceViews:async()=>[found("A"),absent("B"),found("C")],verifyAbsenceAuthority:authority});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("matching_final_commit_exists_on_authoritative_view"));
let torn=structuredClone(durable);delete torn.globalState;r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>torn});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("partial_final_commit_view_denied"));torn=structuredClone(durable);delete torn.authorityHighWatermark;r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>torn});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("partial_final_commit_view_denied"));
torn=structuredClone(durable);torn.authorityHighWatermark.bindingFingerprint="b".repeat(64);r=await reconcile(input,{readFinalRecoveryByIdentity:async()=>torn});assert.equal(r.reconciled,false);assert.ok(r.reasons.includes("durable_final_commit_identity_mismatch"));
console.log("Recovery Finalization Reconciliation v1.2 passed: complete Authority-v2 domain/schema/fingerprint identity survives reconciliation; legacy, Purple Universe, cross-protocol, wrong-schema, torn and conflicting durable realities fail closed.");
