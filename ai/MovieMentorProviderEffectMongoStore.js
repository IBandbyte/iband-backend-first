import mongoose from "mongoose";

const VERSION="1.1.1",DOMAIN="iband.movie-mentor.provider-effect-reality",SCHEMA=2,COLLECTION="movie_mentor_provider_effect_reality";
const STATES=Object.freeze(["unknown","confirmed","conflict"]);let connectionPromise=null,model=null;
const text=v=>typeof v==="string"?v.trim():"";const date=v=>{const d=v instanceof Date?new Date(v):new Date(v);return Number.isNaN(d.getTime())?null:d;};const iso=v=>{const d=date(v);return d?d.toISOString():"";};const plain=v=>v&&typeof v.toObject==="function"?v.toObject():v;function fail(code,message,extras={}){const e=new Error(message);e.code=code;Object.assign(e,extras);throw e;}function mongoUri(){return text(process.env.MONGO_URI||process.env.MONGODB_URI||"");}
function getModel(){if(model)return model;const evidenceSchema=new mongoose.Schema({externalEffectId:{type:String,required:true,trim:true},provider:{type:String,required:true,trim:true},observedAt:{type:Date,required:true},source:{type:String,required:true,trim:true}},{_id:false,strict:true,minimize:false});const schema=new mongoose.Schema({domain:{type:String,required:true,immutable:true},schema:{type:Number,required:true,immutable:true},providerCallId:{type:String,required:true,immutable:true,trim:true},executionId:{type:String,required:true,immutable:true,trim:true},slotId:{type:String,required:true,immutable:true,trim:true},task:{type:String,required:true,immutable:true,trim:true},state:{type:String,enum:STATES,required:true},dispatchUnknownAt:{type:Date,required:true,immutable:true},revision:{type:Number,min:0,default:null},evidence:{type:[evidenceSchema],default:[]}},{collection:COLLECTION,timestamps:true,strict:true,minimize:false});schema.index({providerCallId:1},{unique:true});schema.index({executionId:1,slotId:1});model=mongoose.models.MovieMentorProviderEffectReality||mongoose.model("MovieMentorProviderEffectReality",schema);return model;}
async function ensureConnection(){const uri=mongoUri();if(!uri)fail("MOVIE_MENTOR_PROVIDER_EFFECT_MONGO_NOT_CONFIGURED","Provider effect reality requires MONGO_URI or MONGODB_URI.");if(mongoose.connection.readyState===1)return mongoose.connection;if(!connectionPromise)connectionPromise=mongoose.connect(uri,{serverSelectionTimeoutMS:5000,maxPoolSize:10}).catch(error=>{connectionPromise=null;fail("MOVIE_MENTOR_PROVIDER_EFFECT_MONGO_UNAVAILABLE",`Provider effect Mongo store unavailable: ${error instanceof Error?error.message:"Mongo connection failed."}`,{retryable:true});});await connectionPromise;return mongoose.connection;}
function normalize(record){if(!record)return null;const v=plain(record);if(v.domain!==DOMAIN||![1,SCHEMA].includes(v.schema)||![v.providerCallId,v.executionId,v.slotId,v.task].every(text)||!STATES.includes(text(v.state))||!iso(v.dispatchUnknownAt))fail("MOVIE_MENTOR_PROVIDER_EFFECT_RECORD_INVALID","Durable provider effect record is malformed.");const evidence=Array.isArray(v.evidence)?v.evidence.map(i=>Object.freeze({externalEffectId:text(i.externalEffectId),provider:text(i.provider),observedAt:iso(i.observedAt),source:text(i.source)})):[];if(evidence.some(i=>!i.externalEffectId||!i.provider||!i.observedAt||!i.source))fail("MOVIE_MENTOR_PROVIDER_EFFECT_RECORD_INVALID","Durable provider effect evidence is malformed.");const ids=new Set(evidence.map(i=>i.externalEffectId)),expected=ids.size===0?"unknown":ids.size===1?"confirmed":"conflict";if(text(v.state)!==expected)fail("MOVIE_MENTOR_PROVIDER_EFFECT_STATE_INVALID","Provider effect state does not match durable evidence reality.");return Object.freeze({schema:v.schema,providerCallId:text(v.providerCallId),executionId:text(v.executionId),slotId:text(v.slotId),task:text(v.task),state:expected,dispatchUnknownAt:iso(v.dispatchUnknownAt),revision:Number.isSafeInteger(v.revision)?v.revision:null,evidence:Object.freeze(evidence)});}
function createMovieMentorProviderEffectMongoStore({mongoModel=null,connect=ensureConnection}={}){const storeModel=()=>mongoModel||getModel();async function ready(){if(!mongoModel)await connect();}async function readEffect(providerCallId){await ready();const r=await storeModel().findOne({providerCallId:text(providerCallId)}).lean().exec();return r?normalize(r):null;}async function readEffectsByExecution(executionId){await ready();const rows=await storeModel().find({executionId:text(executionId)}).lean().exec();return Object.freeze(rows.map(normalize));}
 async function beginUnknown({providerCallId,executionId,slotId,task,dispatchUnknownAt}={}){await ready();const c={domain:DOMAIN,schema:SCHEMA,providerCallId:text(providerCallId),executionId:text(executionId),slotId:text(slotId),task:text(task),state:"unknown",dispatchUnknownAt:date(dispatchUnknownAt),revision:0,evidence:[]};if(![c.providerCallId,c.executionId,c.slotId,c.task].every(Boolean)||!c.dispatchUnknownAt)fail("MOVIE_MENTOR_PROVIDER_EFFECT_UNKNOWN_INVALID","UNKNOWN transition requires durable provider-call identity and time.");try{return normalize(await storeModel().create(c));}catch(error){if(error?.code!==11000)throw error;const existing=await readEffect(c.providerCallId);if(existing&&existing.executionId===c.executionId&&existing.slotId===c.slotId&&existing.task===c.task)return existing;fail("MOVIE_MENTOR_PROVIDER_EFFECT_IDENTITY_CONFLICT","Provider-call identity is already bound to a different effect universe.");}}
 async function appendEvidence({providerCallId,externalEffectId,provider,observedAt,source="provider-response"}={}){await ready();const callId=text(providerCallId),effectId=text(externalEffectId),providerName=text(provider),at=date(observedAt),evidenceSource=text(source);if(!callId||!effectId||!providerName||!at||!evidenceSource)fail("MOVIE_MENTOR_PROVIDER_EFFECT_EVIDENCE_INVALID","Provider effect evidence requires call identity, external effect identity, provider and observation time.");for(let attempt=0;attempt<12;attempt+=1){const current=await readEffect(callId);if(!current)return null;if(current.evidence.some(i=>i.externalEffectId===effectId))return current;const nextState=current.evidence.length===0?"confirmed":"conflict",revisionFilter=current.revision===null?{$or:[{revision:{$exists:false}},{revision:null}]}:{revision:current.revision};const written=await storeModel().findOneAndUpdate({providerCallId:callId,...revisionFilter,"evidence.externalEffectId":{$ne:effectId}},{$push:{evidence:{externalEffectId:effectId,provider:providerName,observedAt:at,source:evidenceSource}},$set:{state:nextState,schema:SCHEMA},$inc:{revision:1}},{new:true,runValidators:true}).lean().exec();if(written)return normalize(written);}fail("MOVIE_MENTOR_PROVIDER_EFFECT_EVIDENCE_CONTENTION","Provider effect evidence could not converge after repeated durable CAS contention.",{retryable:true});}return Object.freeze({readEffect,readEffectsByExecution,beginUnknown,appendEvidence});}
function getMovieMentorProviderEffectMongoStoreStatus(){const configured=Boolean(mongoUri());return Object.freeze({version:VERSION,domain:DOMAIN,schema:SCHEMA,collection:COLLECTION,configured,readiness:configured?"durable-mongo":"fail-closed-no-mongo",effectStates:STATES,cas:"revision"});}export{VERSION as MOVIE_MENTOR_PROVIDER_EFFECT_MONGO_STORE_VERSION,DOMAIN as MOVIE_MENTOR_PROVIDER_EFFECT_MONGO_STORE_DOMAIN,createMovieMentorProviderEffectMongoStore,getMovieMentorProviderEffectMongoStoreStatus};export default createMovieMentorProviderEffectMongoStore;
