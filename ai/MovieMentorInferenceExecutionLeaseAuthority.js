import crypto from "node:crypto";

const VERSION = "1.6.0";
const DOMAIN = "iband.movie-mentor.inference-execution-lease";
const DEFAULT_LEASE_MS = 60_000;
const DEFAULT_MAX_PROVIDER_CALLS = 5;
function text(value) { return typeof value === "string" ? value.trim() : ""; }
function freeze(value) { return Object.freeze(value); }
function fail(code, message, extras = {}) { const error = new Error(message); error.code = code; Object.assign(error, extras); throw error; }
function instant(value) { if (value === null || value === undefined || value === "") fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TIME_INVALID", "Inference execution lease time is invalid."); const parsed = value instanceof Date ? new Date(value) : new Date(value); if (Number.isNaN(parsed.getTime())) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TIME_INVALID", "Inference execution lease time is invalid."); return parsed; }
function live(record, at) { return record && text(record.phase) === "active" && instant(record.leaseExpiresAt).getTime() > at.getTime(); }
function sameTurnBinding(record, request) { return text(record?.creatorTurnId) === request.creatorTurnId && text(record?.principalId) === request.principalId && text(record?.projectId) === request.projectId && text(record?.reservationId) === request.reservationId && text(record?.requestDigest) === request.requestDigest && record?.maxProviderCalls === request.maxProviderCalls; }
function evidence(record, extras = {}) { return freeze({ authorized: true, domain: DOMAIN, executionId: text(record.executionId), creatorTurnId: text(record.creatorTurnId), principalId: text(record.principalId), projectId: text(record.projectId), reservationId: text(record.reservationId), requestDigest: text(record.requestDigest), phase: text(record.phase), ownerId: text(record.ownerId), leaseGeneration: record.leaseGeneration, leaseReference: text(record.leaseReference), fencingToken: text(record.fencingToken), leaseExpiresAt: instant(record.leaseExpiresAt).toISOString(), maxProviderCalls: record.maxProviderCalls, providerCallsClaimed: record.providerCallsClaimed, executionAuthorized: text(record.phase) === "active", ...extras }); }
function rejectRevokedForwardExecution(record) { const phase = text(record?.phase); if (phase === "quarantined") fail("MOVIE_MENTOR_INFERENCE_EXECUTION_QUARANTINED", "Creator turn belongs to a durably quarantined inference universe whose historical proof cannot authorize a new execution lease.", { retryable: false, executionId: text(record?.executionId) || null, quarantinedFromPhase: text(record?.quarantinedFromPhase) || null, reason: text(record?.quarantineReason) || "durable-inference-universe-quarantined" }); if (phase === "aborted") fail("MOVIE_MENTOR_INFERENCE_EXECUTION_ABORTED", "Creator turn belongs to a durably aborted inference universe and cannot be reopened as live execution authority.", { retryable: false, executionId: text(record?.executionId) || null, reason: text(record?.abortReason) || "durable-inference-universe-aborted" }); }

function createMovieMentorInferenceExecutionLeaseAuthority({ store = null, now = () => new Date(), leaseMs = DEFAULT_LEASE_MS, maxProviderCalls = DEFAULT_MAX_PROVIDER_CALLS, randomId = () => crypto.randomUUID() } = {}) {
  if (typeof store?.readExecution !== "function" || typeof store?.readExecutionByCreatorTurn !== "function" || typeof store?.createExecution !== "function" || typeof store?.replaceExecution !== "function" || typeof store?.claimProviderCall !== "function") fail("MOVIE_MENTOR_INFERENCE_EXECUTION_STORE_REQUIRED", "Inference execution lease authority requires a durable execution and provider-call claim store.");
  if (!Number.isSafeInteger(leaseMs) || leaseMs <= 0) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_DURATION_INVALID", "Inference execution lease duration must be a positive safe integer.");
  if (!Number.isSafeInteger(maxProviderCalls) || maxProviderCalls < 1) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_PROVIDER_BUDGET_INVALID", "Inference execution provider-call budget must be a positive safe integer.");
  const ownedExecutionEvidence = new WeakSet();
  function issueEvidence(record, extras = {}) { const proof = evidence(record, extras); ownedExecutionEvidence.add(proof); return proof; }
  async function rereadExact(executionId, ownerId, generation, reference, fencingToken) { const durable = await store.readExecution(executionId); return durable && text(durable.ownerId) === ownerId && durable.leaseGeneration === generation && text(durable.leaseReference) === reference && text(durable.fencingToken) === fencingToken ? durable : null; }
  async function findExecutionByCreatorTurn({creatorTurnId=null,principalId=null,projectId=null,requestDigest=null}={}){const turn=text(creatorTurnId),principal=text(principalId),project=text(projectId),digest=text(requestDigest);if(!turn||!principal||!project||!digest)fail("MOVIE_MENTOR_INFERENCE_EXECUTION_LOOKUP_BINDING_REQUIRED","Creator-turn convergence lookup requires turn, principal, project and request digest.");const current=await store.readExecutionByCreatorTurn({creatorTurnId:turn,principalId:principal,projectId:project});if(!current)return freeze({found:false,authorized:false,reason:"execution-not-found",creatorTurnId:turn,principalId:principal,projectId:project});if(text(current.requestDigest)!==digest)fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TURN_IDENTITY_CONFLICT","Creator turn identity is already bound to a different immutable execution request.");return issueEvidence(current,{found:true,convergenceEvidence:true});}
  async function openExecution(input = {}) { const request = freeze({ creatorTurnId: text(input.creatorTurnId), principalId: text(input.principalId), projectId: text(input.projectId), reservationId: text(input.reservationId), requestDigest: text(input.requestDigest), ownerId: text(input.ownerId), maxProviderCalls: input.maxProviderCalls ?? maxProviderCalls }); if ([request.creatorTurnId, request.principalId, request.projectId, request.reservationId, request.requestDigest, request.ownerId].some((value) => !value)) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_BINDING_REQUIRED", "Inference execution requires creator turn, principal, project, reservation, request digest and owner identity."); if (!Number.isSafeInteger(request.maxProviderCalls) || request.maxProviderCalls < 1) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_PROVIDER_BUDGET_INVALID", "Inference execution provider-call budget must be a positive safe integer."); const existing = await store.readExecutionByCreatorTurn(request); if (existing) { if (!sameTurnBinding(existing, request)) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TURN_IDENTITY_CONFLICT", "Creator turn identity is already bound to a different immutable execution request."); rejectRevokedForwardExecution(existing); return issueEvidence(existing, { idempotent: true, created: false }); } const at = instant(now()), executionId = `inference-execution-${randomId()}`, leaseReference = `inference-lease-1-${randomId()}`, fencingToken = `inference-fence-1-${randomId()}`; const next = freeze({ executionId, ...request, phase: "active", leaseGeneration: 1, leaseReference, fencingToken, leaseAcquiredAt: at.toISOString(), leaseExpiresAt: new Date(at.getTime() + leaseMs).toISOString(), providerCallsClaimed: 0, providerCalls: [] }); try { const created = await store.createExecution(next); if (created) return issueEvidence(created, { idempotent: false, created: true }); } catch (error) { const exact = await rereadExact(executionId, request.ownerId, 1, leaseReference, fencingToken); if (exact) { rejectRevokedForwardExecution(exact); return issueEvidence(exact, { idempotent: true, created: true, reconciledAckLoss: true }); } const replay = await store.readExecutionByCreatorTurn(request); if (replay) { if (!sameTurnBinding(replay, request)) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TURN_IDENTITY_CONFLICT", "Creator turn identity is already bound to a different immutable execution request."); rejectRevokedForwardExecution(replay); return issueEvidence(replay, { idempotent: true, created: false }); } throw error; } const replay = await store.readExecutionByCreatorTurn(request); if (!replay) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_CREATE_RACE_LOST", "Inference execution creation lost a durable race without a recoverable creator-turn binding."); if (!sameTurnBinding(replay, request)) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_TURN_IDENTITY_CONFLICT", "Creator turn identity is already bound to a different immutable execution request."); rejectRevokedForwardExecution(replay); return issueEvidence(replay, { idempotent: true, created: false }); }
  async function acquireExecution({ executionId, ownerId } = {}) { const id = text(executionId), requestedOwnerId = text(ownerId); if (!id || !requestedOwnerId) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_ACQUIRE_BINDING_REQUIRED", "Execution acquisition requires execution and owner identity."); const current = await store.readExecution(id); if (!current) return freeze({ authorized: false, reason: "execution-not-found" }); if (text(current.phase) !== "active") return freeze({ authorized: false, reason: "execution-not-active", phase: text(current.phase) }); const at = instant(now()); if (live(current, at)) { if (text(current.ownerId) !== requestedOwnerId) return freeze({ authorized: false, reason: "execution-lease-held-by-another-owner", holderOwnerId: text(current.ownerId), leaseGeneration: current.leaseGeneration }); return issueEvidence(current, { idempotent: true, acquired: false }); } const generation = current.leaseGeneration + 1, leaseReference = `inference-lease-${generation}-${randomId()}`, fencingToken = `inference-fence-${generation}-${randomId()}`, next = freeze({ ...current, ownerId: requestedOwnerId, leaseGeneration: generation, leaseReference, fencingToken, leaseAcquiredAt: at.toISOString(), leaseExpiresAt: new Date(at.getTime() + leaseMs).toISOString() }); try { const written = await store.replaceExecution(next, { expectedPhase: current.phase, expectedLeaseGeneration: current.leaseGeneration, expectedLeaseReference: current.leaseReference }); if (written) return issueEvidence(written, { idempotent: false, acquired: true }); } catch (error) { const exact = await rereadExact(id, requestedOwnerId, generation, leaseReference, fencingToken); if (exact) return issueEvidence(exact, { idempotent: true, acquired: true, reconciledAckLoss: true }); throw error; } const exact = await rereadExact(id, requestedOwnerId, generation, leaseReference, fencingToken); if (exact) return issueEvidence(exact, { idempotent: true, acquired: true }); return freeze({ authorized: false, reason: "execution-lease-race-lost" }); }
  async function renewExecution(execution = {}) { if(!execution||typeof execution!=="object"||!ownedExecutionEvidence.has(execution))return freeze({authorized:false,reason:"execution-owner-proof-required"}); const {executionId,ownerId,leaseGeneration,leaseReference,fencingToken}=execution,id=text(executionId),requestedOwnerId=text(ownerId),reference=text(leaseReference),fence=text(fencingToken); if(!id||!requestedOwnerId||!reference||!fence||!Number.isSafeInteger(Number(leaseGeneration))) fail("MOVIE_MENTOR_INFERENCE_EXECUTION_RENEW_BINDING_REQUIRED","Execution renewal requires complete lease fencing evidence."); const current=await store.readExecution(id),at=instant(now()); if(!current||text(current.phase)!=="active"||text(current.ownerId)!==requestedOwnerId||current.leaseGeneration!==Number(leaseGeneration)||text(current.leaseReference)!==reference||text(current.fencingToken)!==fence)return freeze({authorized:false,reason:"execution-lease-fenced"}); if(!live(current,at))return freeze({authorized:false,reason:"execution-lease-expired"}); const currentExpiry=instant(current.leaseExpiresAt),nextExpiry=new Date(Math.max(currentExpiry.getTime(),at.getTime())+leaseMs).toISOString(),next=freeze({...current,leaseExpiresAt:nextExpiry}); try{const written=await store.replaceExecution(next,{expectedPhase:current.phase,expectedLeaseGeneration:current.leaseGeneration,expectedLeaseReference:current.leaseReference,expectedLeaseExpiresAt:currentExpiry.toISOString()});if(written)return issueEvidence(written,{renewed:true});}catch(error){const exact=await store.readExecution(id);if(exact&&text(exact.ownerId)===requestedOwnerId&&exact.leaseGeneration===current.leaseGeneration&&text(exact.leaseReference)===reference&&text(exact.fencingToken)===fence&&instant(exact.leaseExpiresAt).toISOString()===nextExpiry)return issueEvidence(exact,{renewed:true,reconciledAckLoss:true});throw error;}return freeze({authorized:false,reason:"execution-lease-renewal-race-lost"}); }
  async function assertFence(execution = {}) { if(!execution||typeof execution!=="object"||!ownedExecutionEvidence.has(execution))return freeze({authorized:false,reason:"execution-owner-proof-required"}); const {executionId,ownerId,leaseGeneration,leaseReference,fencingToken}=execution,current=await store.readExecution(text(executionId)),at=instant(now()); if(!live(current,at)||text(current.ownerId)!==text(ownerId)||current.leaseGeneration!==Number(leaseGeneration)||text(current.leaseReference)!==text(leaseReference)||text(current.fencingToken)!==text(fencingToken))return freeze({authorized:false,reason:"execution-lease-fenced"}); return issueEvidence(current); }
  async function assertProviderDispatch({ providerCall = null } = {}) { const executionId=text(providerCall?.executionId),providerCallId=text(providerCall?.providerCallId),slotId=text(providerCall?.slotId),leaseGeneration=Number(providerCall?.leaseGeneration); if(providerCall?.authorized!==true||!executionId||!providerCallId||!slotId||!Number.isSafeInteger(leaseGeneration))return freeze({authorized:false,dispatchAuthorized:false,reason:"provider-call-authority-invalid"}); const current=await store.readExecution(executionId),at=instant(now()); if(!live(current,at))return freeze({authorized:false,dispatchAuthorized:false,reason:"execution-not-active",phase:text(current?.phase)}); if(current.leaseGeneration!==leaseGeneration)return freeze({authorized:false,dispatchAuthorized:false,reason:"execution-generation-fenced"}); const admitted=current.providerCalls?.find(call=>text(call.providerCallId)===providerCallId&&text(call.slotId)===slotId); if(!admitted)return freeze({authorized:false,dispatchAuthorized:false,reason:"provider-call-not-in-current-execution-universe"}); if(text(admitted.leaseReference)!==text(current.leaseReference)||text(admitted.fencingToken)!==text(current.fencingToken))return freeze({authorized:false,dispatchAuthorized:false,reason:"provider-call-fence-not-current"}); return freeze({authorized:true,dispatchAuthorized:true,executionId,providerCallId,slotId,leaseGeneration,currentLeaseReference:text(current.leaseReference),currentFencingToken:text(current.fencingToken)}); }
  async function claimProviderCall({ execution = null, slotId = null, task = null } = {}) { if(!execution||typeof execution!=="object"||!ownedExecutionEvidence.has(execution))fail("MOVIE_MENTOR_INFERENCE_EXECUTION_OWNER_PROOF_REQUIRED","Provider execution requires execution evidence issued by this exact inference execution lease authority instance."); const slot=text(slotId),callTask=text(task),executionId=text(execution?.executionId),ownerId=text(execution?.ownerId),leaseReference=text(execution?.leaseReference),fencingToken=text(execution?.fencingToken),leaseGeneration=Number(execution?.leaseGeneration); if(execution?.authorized!==true||execution?.executionAuthorized!==true||!executionId||!ownerId||!leaseReference||!fencingToken||!Number.isSafeInteger(leaseGeneration)||!slot||!callTask)fail("MOVIE_MENTOR_INFERENCE_PROVIDER_CALL_AUTHORITY_REQUIRED","Provider execution requires current durable ACTIVE execution fencing and a logical provider-call slot."); const at=instant(now()),providerCallId=`provider-call-${randomId()}`; try{ const decision=await store.claimProviderCall({executionId,ownerId,leaseGeneration,leaseReference,fencingToken,providerCallId,slotId:slot,task:callTask,admittedAt:at.toISOString()}); if(decision?.claimed===true&&text(decision?.providerCall?.providerCallId)===providerCallId)return freeze({authorized:true,dispatchAuthorized:true,providerCallId,slotId:slot,task:callTask,executionId,ownerId,leaseGeneration,leaseReference,fencingToken,admittedAt:text(decision.providerCall.admittedAt),providerCallsClaimed:decision.execution?.providerCallsClaimed??null}); const existing=decision?.existingProviderCall;if(existing)return freeze({authorized:false,dispatchAuthorized:false,reason:text(existing.providerCallId)===providerCallId?"provider-call-claim-replayed":"provider-call-slot-already-admitted",existingProviderCallId:text(existing.providerCallId),slotId:slot}); const current=decision?.execution;if(!current)return freeze({authorized:false,dispatchAuthorized:false,reason:"execution-not-found"});if(text(current.phase)!=="active")return freeze({authorized:false,dispatchAuthorized:false,reason:"execution-not-active",phase:text(current.phase)});if(current.providerCallsClaimed>=current.maxProviderCalls)return freeze({authorized:false,dispatchAuthorized:false,reason:"provider-call-budget-exhausted"});return freeze({authorized:false,dispatchAuthorized:false,reason:"execution-lease-fenced"}); }catch(error){ const durable=await store.readExecution(executionId),exact=durable?.providerCalls?.find(call=>text(call.providerCallId)===providerCallId&&text(call.slotId)===slot); if(exact){ const recovered=freeze({authorized:true,dispatchAuthorized:false,providerCallId,slotId:slot,task:callTask,executionId,ownerId,leaseGeneration,leaseReference,fencingToken,admittedAt:text(exact.admittedAt),providerCallsClaimed:durable.providerCallsClaimed,reconciledAckLoss:true}); const current=await assertProviderDispatch({providerCall:recovered}); return freeze({...recovered,dispatchAuthorized:current.dispatchAuthorized===true,reason:current.dispatchAuthorized===true?undefined:(current.reason||"recovered-admission-no-current-dispatch-authority")}); } throw error; } }
  return freeze({findExecutionByCreatorTurn,openExecution,acquireExecution,renewExecution,assertFence,assertProviderDispatch,claimProviderCall});
}
export { VERSION as MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_AUTHORITY_VERSION, DOMAIN as MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_AUTHORITY_DOMAIN, DEFAULT_LEASE_MS as MOVIE_MENTOR_INFERENCE_EXECUTION_DEFAULT_LEASE_MS, DEFAULT_MAX_PROVIDER_CALLS as MOVIE_MENTOR_INFERENCE_EXECUTION_DEFAULT_MAX_PROVIDER_CALLS, createMovieMentorInferenceExecutionLeaseAuthority };
export default createMovieMentorInferenceExecutionLeaseAuthority;
