import crypto from "node:crypto";
import {createTurnContextEnvelope} from "./MovieMentorTurnContextControl.js";import {orchestrateMovieMentorTurn} from "./MovieMentorTurnOrchestrator.js";import {interpretMovieMentorSemantics} from "./MovieMentorSemanticInterpreter.js";import {executeMovieMentorSpecialistWorkOrder,LIVE_AGENT_IDS,MOVIE_MENTOR_SPECIALIST_EXECUTOR_VERSION,SPECIALIST_CONTRACT_VERSION} from "./MovieMentorSpecialistExecutor.js";import {synthesizeMovieMentorResponse} from "./MovieMentorSynthesisEngine.js";import {buildCurrentCreatorTruthView} from "./MovieMentorCreatorTruthViewControl.js";import {readAuthoritativeTurnSource,readAuthoritativeRevision,readAuthoritativeCreatorState} from "./MovieMentorCreatorStateStore.js";
const MOVIE_MENTOR_TURN_RUNTIME_VERSION="2.0.0";const s=v=>typeof v==="string"?v.trim():"";function clone(v){if(v===undefined)return undefined;try{return JSON.parse(JSON.stringify(v));}catch{return v;}}function messageFrom(i){return s(i?.input?.message||i?.message||"");}function identityFrom(i){return{projectId:s(i?.projectId||i?.context?.projectId)||null,creatorSessionId:s(i?.creatorSessionId||i?.context?.creatorSessionId)||null};}function runtimeError(code,message,extras={}){const e=new Error(message);e.code=code;Object.assign(e,extras);return e;}function buildRequestDigest({creatorMessage,projectId,options}={}){return crypto.createHash("sha256").update(JSON.stringify({creatorMessage:s(creatorMessage),projectId:s(projectId),options:clone(options||{})})).digest("hex");}function findProviderEvidence(value,seen=new Set()){if(!value||typeof value!=="object"||seen.has(value))return null;seen.add(value);const externalEffectId=s(value.responseId||value.providerResponseId||value.externalEffectId);if(externalEffectId)return{externalEffectId,provider:s(value.provider)||"unknown-provider"};for(const child of Object.values(value)){const found=findProviderEvidence(child,seen);if(found)return found;}return null;}
function buildTurnEnvelopeFromDurableState({creatorMessage,state}={}){if(!s(creatorMessage))throw runtimeError("MOVIE_MENTOR_TURN_MESSAGE_REQUIRED","A creator message is required for a Movie Mentor turn.");if(!state||typeof state!=="object")throw runtimeError("MOVIE_MENTOR_CREATOR_STATE_INVALID","Durable creator state is required to build a Movie Mentor turn.");const currentCreatorTruth=buildCurrentCreatorTruthView(state.creatorConfirmedContext||[]);return createTurnContextEnvelope({projectId:state.projectId||null,creatorSessionId:state.creatorSessionId||null,creatorMessage:s(creatorMessage),revision:{capturedRevision:state.revision,authoritativeRevision:state.revision,authorityReference:state.revisionAuthorityReference},creatorState:{generation:state.creatorStateGeneration,fingerprint:state.creatorStateFingerprint,authorityReference:state.creatorAuthorityReference},snapshotReference:state.snapshotReference,capturedAt:state.capturedAt,creatorConfirmedContext:clone(currentCreatorTruth),projectJourney:clone(state.projectJourney??null),memoryContext:clone(state.memoryContext??null),responseBlueprint:clone(state.responseBlueprint??null),communicationPlan:clone(state.communicationPlan??null)});}
function resultResponse(canonical,settlement,{replayed=false}={}){return{...clone(canonical.resultPayload),metadata:{...(clone(canonical.resultPayload)?.metadata||{}),canonicalResult:{authorized:true,resultReference:canonical.resultReference,resultDigest:canonical.resultDigest,executionId:canonical.executionId,closureReference:canonical.closureReference,closureCertificateDigest:canonical.closureCertificateDigest,reservationId:canonical.reservationId,settlement:"consumed",settlementIdempotent:settlement?.idempotent===true,replayedFromDurableResult:replayed}}};}
function createFencedInferenceOrchestrationDeps({execution,inferenceExecutionAuthority,deps={},onClaim=null}={}){if(execution?.authorized!==true||typeof inferenceExecutionAuthority?.claimProviderCall!=="function")throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_AUTHORITY_REQUIRED","Fenced orchestration requires authoritative durable inference execution.");const invoke=async(slotId,task,providerFunction)=>{const decision=await inferenceExecutionAuthority.claimProviderCall({execution,slotId,task});if(decision?.dispatchAuthorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_PROVIDER_CALL_NOT_AUTHORIZED","Provider call was not admitted under the current durable execution lease.",{reason:decision?.reason||"provider-call-not-authorized",slotId});onClaim?.(decision);if(typeof inferenceExecutionAuthority.beginProviderDispatch==="function"){const dispatch=await inferenceExecutionAuthority.beginProviderDispatch({providerCall:decision});if(dispatch?.dispatchAuthorized!==true)throw runtimeError("MOVIE_MENTOR_PROVIDER_EFFECT_DISPATCH_NOT_AUTHORIZED","Provider dispatch requires durable UNKNOWN effect reality.",{reason:dispatch?.reason||"provider-effect-unknown-not-durable",providerCallId:decision.providerCallId});}if(typeof inferenceExecutionAuthority.assertProviderDispatch==="function"){const current=await inferenceExecutionAuthority.assertProviderDispatch({providerCall:decision});if(current?.dispatchAuthorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_PROVIDER_DISPATCH_FENCED","Provider dispatch authority was revoked before the irreversible provider boundary.",{reason:current?.reason||"provider-dispatch-fenced",providerCallId:decision.providerCallId});}try{const result=await providerFunction(),evidence=findProviderEvidence(result);if(evidence&&typeof inferenceExecutionAuthority.contributeProviderEffectEvidence==="function")await inferenceExecutionAuthority.contributeProviderEffectEvidence({providerCallId:decision.providerCallId,...evidence,source:"provider-response"});return result;}catch(error){const evidence=findProviderEvidence(error?.providerEffectEvidence||null);if(evidence&&typeof inferenceExecutionAuthority.contributeProviderEffectEvidence==="function")await inferenceExecutionAuthority.contributeProviderEffectEvidence({providerCallId:decision.providerCallId,...evidence,source:"provider-error-evidence"});throw error;}};const interpret=deps.interpretSemantics||interpretMovieMentorSemantics,synthesize=deps.synthesizeResponse||synthesizeMovieMentorResponse,executeWorkOrder=deps.executeSpecialistWorkOrder||executeMovieMentorSpecialistWorkOrder;return Object.freeze({async interpretSemantics(input){return invoke("semantic","movie-mentor-semantic",()=>interpret(input));},async executeSpecialistPlan(plan={}){const contributions=[],skipped=[],failures=[],metadata=[];for(const workOrder of Array.isArray(plan?.workOrders)?plan.workOrders:[]){const agentId=s(workOrder?.agentId);if(!LIVE_AGENT_IDS.has(agentId)){skipped.push({agentId:agentId||null,reason:"agent-not-live-yet"});continue;}try{const result=await invoke(agentId,`movie-mentor-specialist:${agentId}`,()=>executeWorkOrder(clone(workOrder),deps.specialistDeps||{}));contributions.push(result.contribution);metadata.push({agentId,metadata:clone(result.metadata||null)});}catch(error){failures.push({agentId:agentId||null,code:error?.code||"SPECIALIST_EXECUTION_FAILED",message:error instanceof Error?error.message:"Specialist execution failed.",validationIssues:Array.isArray(error?.validationIssues)?error.validationIssues:[]});}}return{version:MOVIE_MENTOR_SPECIALIST_EXECUTOR_VERSION,contractVersion:SPECIALIST_CONTRACT_VERSION,status:failures.length?"partial":"completed",contributions,skipped,failures,metadata,authority:{providerCallsRequireDurableExecutionClaim:true,providerDispatchRequiresDurableUnknown:true,providerDispatchRequiresCurrentExecutionFence:true,creatorTruthDominates:true,specialistsRemainProvisional:true},liveAgents:[...LIVE_AGENT_IDS]};},async synthesizeResponse(input){return invoke("synthesis","movie-mentor-synthesis",()=>synthesize(input));}});}
async function openLiveExecution({input,creatorMessage,durableProjectId,reservation,serverAuthority,inferenceExecutionAuthority,deps,requestDigest=null}={}){const creatorTurnId=s(input?.creatorTurnId);if(!creatorTurnId)throw runtimeError("MOVIE_MENTOR_CREATOR_TURN_ID_REQUIRED","Live paid inference requires a stable creatorTurnId supplied before the first transport attempt.");const ownerId=s(deps.createExecutionOwnerId?.())||`turn-owner-${crypto.randomUUID()}`,digest=s(requestDigest)||buildRequestDigest({creatorMessage,projectId:durableProjectId,options:input?.options||{}}),opened=await inferenceExecutionAuthority.openExecution({creatorTurnId,principalId:s(serverAuthority?.principalId),projectId:durableProjectId,reservationId:s(reservation?.reservationId),requestDigest:digest,ownerId});if(opened?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_OPEN_DENIED","Durable inference execution could not be opened.");if(s(opened.phase)!=="active")throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_RECOVERY_REQUIRED","Existing creator turn is no longer executable and must converge through durable recovery.",{phase:s(opened.phase),executionId:opened.executionId,retryable:true});if(s(opened.ownerId)===ownerId){const current=await inferenceExecutionAuthority.assertFence(opened);if(current?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_NOT_AUTHORIZED","New inference execution does not hold a current durable lease.");return current;}const acquired=await inferenceExecutionAuthority.acquireExecution({executionId:opened.executionId,ownerId});if(acquired?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_NOT_AUTHORIZED","Inference execution lease is not currently owned by this turn attempt.",{reason:acquired?.reason||"lease-not-authorized"});return acquired;}
async function acquireExistingExecution({existing,inferenceExecutionAuthority,deps}={}){const ownerId=s(deps.createExecutionOwnerId?.())||`turn-owner-${crypto.randomUUID()}`;if(s(existing?.phase)!=="active")throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_RECOVERY_REQUIRED","Existing creator turn is not executable.",{phase:s(existing?.phase),executionId:existing?.executionId,retryable:true});const acquired=await inferenceExecutionAuthority.acquireExecution({executionId:existing.executionId,ownerId});if(acquired?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_LEASE_NOT_AUTHORIZED","Existing inference execution lease cannot currently be acquired.",{reason:acquired?.reason||"lease-not-authorized",executionId:existing.executionId,retryable:true});return acquired;}
async function replayTerminalTurn({existing,inferenceExecutionAuthority,settlementAuthority}={}){if(s(existing?.phase)!=="closed")return null;const canonical=await inferenceExecutionAuthority.readCanonicalResult({executionId:existing.executionId});if(canonical?.authorized!==true||canonical?.committed!==true)return null;const settlement=await settlementAuthority.reconcile({executionId:existing.executionId});if(settlement?.authorized!==true||settlement?.settled!==true||settlement?.outcome!=="consumed")throw runtimeError("MOVIE_MENTOR_INFERENCE_SETTLEMENT_RECONCILIATION_PENDING","Durable canonical result exists but settlement has not converged.",{executionId:existing.executionId,retryable:true});return resultResponse(canonical,settlement,{replayed:true});}
async function recoverStagedResultTurn({existing,inferenceExecutionAuthority,settlementAuthority}={}){if(!["closing","closed"].includes(s(existing?.phase)))return null;const candidate=await inferenceExecutionAuthority.readResultCandidate(existing.executionId);if(!candidate)throw runtimeError("MOVIE_MENTOR_RESULT_CANDIDATE_RECOVERY_REQUIRED","Non-executable inference universe has no durable staged result candidate.",{executionId:existing.executionId,phase:existing.phase,retryable:true});const closure=await inferenceExecutionAuthority.reconcileExecutionClosure({executionId:existing.executionId});if(closure?.closed!==true||closure?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_CLOSURE_PENDING","Staged result exists but current provider reality cannot yet close.",{executionId:existing.executionId,reason:closure?.reason||"closure-pending",retryable:true});let canonical=await inferenceExecutionAuthority.readCanonicalResult({executionId:existing.executionId});if(canonical?.authorized!==true||canonical?.committed!==true)canonical=await inferenceExecutionAuthority.commitCanonicalResult({closure,result:candidate.resultPayload});if(canonical?.authorized!==true||canonical?.committed!==true)throw runtimeError("MOVIE_MENTOR_CANONICAL_RESULT_COMMIT_INVALID","Recovered staged result could not become canonical.",{executionId:existing.executionId,retryable:true});const settlement=await settlementAuthority.reconcile({executionId:existing.executionId});if(settlement?.authorized!==true||settlement?.settled!==true||settlement?.outcome!=="consumed")throw runtimeError("MOVIE_MENTOR_INFERENCE_SETTLEMENT_RECONCILIATION_PENDING","Recovered canonical result has not converged to settlement.",{executionId:existing.executionId,retryable:true});return resultResponse(canonical,settlement,{replayed:true});}
async function runMovieMentorTurn(input={},deps={}){const creatorMessage=messageFrom(input);if(!creatorMessage)throw runtimeError("MOVIE_MENTOR_TURN_MESSAGE_REQUIRED","A creator message is required for a Movie Mentor turn.");const creatorTurnId=s(input?.creatorTurnId);if(!creatorTurnId)throw runtimeError("MOVIE_MENTOR_CREATOR_TURN_ID_REQUIRED","Live paid inference requires a stable creatorTurnId supplied before the first transport attempt.");const identity=identityFrom(input);if(!identity.projectId&&!identity.creatorSessionId)throw runtimeError("MOVIE_MENTOR_CREATOR_STATE_IDENTITY_REQUIRED","projectId or creatorSessionId is required for a durable Movie Mentor turn.");const readSource=deps.readAuthoritativeTurnSource||readAuthoritativeTurnSource,revisionReader=deps.readAuthoritativeRevision||readAuthoritativeRevision,stateReader=deps.readAuthoritativeCreatorState||readAuthoritativeCreatorState,orchestrate=deps.orchestrateTurn||orchestrateMovieMentorTurn,spendAuthority=deps.inferenceSpendAuthority,inferenceExecutionAuthority=deps.inferenceExecutionAuthority,settlementAuthority=deps.inferenceSettlementAuthority;if(typeof spendAuthority?.reserveTurn!=="function"||typeof spendAuthority?.readReservation!=="function"||typeof spendAuthority?.settleTurn!=="function")throw runtimeError("MOVIE_MENTOR_INFERENCE_SPEND_AUTHORITY_REQUIRED","Movie Mentor turn runtime requires durable reservation, rehydration and settlement authority.");const executionEnabled=typeof inferenceExecutionAuthority?.findExecutionByCreatorTurn==="function"&&typeof inferenceExecutionAuthority?.openExecution==="function"&&typeof inferenceExecutionAuthority?.claimProviderCall==="function",closureEnabled=executionEnabled&&typeof inferenceExecutionAuthority?.beginExecutionClosing==="function"&&typeof inferenceExecutionAuthority?.reconcileExecutionClosure==="function",candidateEnabled=closureEnabled&&typeof inferenceExecutionAuthority?.stageResultCandidate==="function"&&typeof inferenceExecutionAuthority?.readResultCandidate==="function",resultEnabled=candidateEnabled&&typeof inferenceExecutionAuthority?.commitCanonicalResult==="function"&&typeof inferenceExecutionAuthority?.readCanonicalResult==="function",settlementEnabled=resultEnabled&&typeof settlementAuthority?.reconcile==="function";if(!executionEnabled)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_AUTHORITY_REQUIRED","Paid Movie Mentor inference cannot run without durable creator-turn convergence and execution authority.");if(!closureEnabled)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_CLOSURE_AUTHORITY_REQUIRED","Paid Movie Mentor inference cannot run without durable closure authority.");if(!candidateEnabled)throw runtimeError("MOVIE_MENTOR_RESULT_CANDIDATE_AUTHORITY_REQUIRED","Paid Movie Mentor inference cannot run without durable pre-closure result staging authority.");if(!resultEnabled)throw runtimeError("MOVIE_MENTOR_CANONICAL_RESULT_AUTHORITY_REQUIRED","Paid Movie Mentor inference cannot run without durable canonical result authority.");if(!settlementEnabled)throw runtimeError("MOVIE_MENTOR_INFERENCE_SETTLEMENT_RECONCILIATION_AUTHORITY_REQUIRED","Paid Movie Mentor inference cannot run without deterministic settlement reconciliation authority.");
 const state=await readSource(identity),envelope=buildTurnEnvelopeFromDurableState({creatorMessage,state}),durableProjectId=s(state?.projectId||envelope?.projectId),principalId=s(deps.serverAuthority?.principalId),requestDigest=buildRequestDigest({creatorMessage,projectId:durableProjectId,options:input?.options||{}});let existing=await inferenceExecutionAuthority.findExecutionByCreatorTurn({creatorTurnId,principalId,projectId:durableProjectId,requestDigest});if(existing?.found){const replay=await replayTerminalTurn({existing,inferenceExecutionAuthority,settlementAuthority});if(replay)return replay;const recovered=await recoverStagedResultTurn({existing,inferenceExecutionAuthority,settlementAuthority});if(recovered)return recovered;if(s(existing.phase)!=="active")throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_RECOVERY_REQUIRED","Creator turn already belongs to a non-executable durable universe.",{phase:s(existing.phase),executionId:existing.executionId,retryable:true});}
 let reservation,execution,claimedCount=0;if(existing?.found){reservation=await spendAuthority.readReservation({reservationId:existing.reservationId,principalId,projectId:durableProjectId});if(reservation?.authorized!==true||reservation.status!=="reserved")throw runtimeError("MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_REHYDRATION_INVALID","Existing active execution does not bind a live reserved spend authority.",{executionId:existing.executionId,reservationStatus:reservation?.status||null});execution=await acquireExistingExecution({existing,inferenceExecutionAuthority,deps});}else{reservation=await spendAuthority.reserveTurn({serverAuthority:deps.serverAuthority,projectId:durableProjectId});if(reservation?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_SPEND_RESERVATION_INVALID","Movie Mentor inference spend reservation was not authoritative.");try{execution=await openLiveExecution({input,creatorMessage,durableProjectId,reservation,serverAuthority:deps.serverAuthority,inferenceExecutionAuthority,deps,requestDigest});}catch(error){if(error?.code!=="MOVIE_MENTOR_INFERENCE_EXECUTION_TURN_IDENTITY_CONFLICT")throw error;await spendAuthority.settleTurn({reservation,outcome:"released",reason:"creator-turn-execution-race-lost-before-provider-claim"});existing=await inferenceExecutionAuthority.findExecutionByCreatorTurn({creatorTurnId,principalId,projectId:durableProjectId,requestDigest});if(existing?.found){const replay=await replayTerminalTurn({existing,inferenceExecutionAuthority,settlementAuthority});if(replay)return replay;const recovered=await recoverStagedResultTurn({existing,inferenceExecutionAuthority,settlementAuthority});if(recovered)return recovered;}if(existing?.found&&existing.phase==="active"){reservation=await spendAuthority.readReservation({reservationId:existing.reservationId,principalId,projectId:durableProjectId});execution=await acquireExistingExecution({existing,inferenceExecutionAuthority,deps});}else throw error;}}
 let orchestrationDeps={readAuthoritativeRevision:revisionReader,readAuthoritativeCreatorState:stateReader};const fenced=createFencedInferenceOrchestrationDeps({execution,inferenceExecutionAuthority,deps,onClaim:()=>{claimedCount+=1;}});orchestrationDeps={...orchestrationDeps,...fenced,verifyTurnContext:deps.verifyTurnContext,resolveContinuationReferences:deps.resolveContinuationReferences,commitCreatorDecision:deps.commitCreatorDecision,readAuthoritativeTurnSource:deps.readAuthoritativeTurnSource,applyMovieMentorCreatorStateTransition:deps.applyMovieMentorCreatorStateTransition,writeAuthoritativeCreatorState:deps.writeAuthoritativeCreatorState};let result;try{result=await orchestrate({message:creatorMessage,authoritativeTurnContext:envelope,options:clone(input?.options||{})},orchestrationDeps);}catch(error){if(claimedCount>0)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_UNRESOLVED","Inference execution failed after one or more durable provider-call claims; spend remains reserved for reconciliation.",{cause:error,retryable:true,executionId:execution?.executionId||null,providerCallsClaimed:claimedCount});try{await spendAuthority.settleTurn({reservation,outcome:"released",reason:"orchestration-failed-before-provider-claim"});}catch(settlementError){settlementError.cause=error;throw settlementError;}throw error;}
 const candidate=await inferenceExecutionAuthority.stageResultCandidate({execution,resultPayload:result});if(!candidate?.candidateReference||candidate.resultDigest==null)throw runtimeError("MOVIE_MENTOR_RESULT_CANDIDATE_STAGE_INVALID","Successful orchestration was not durably staged before closure.",{executionId:execution.executionId,retryable:true});const closing=await inferenceExecutionAuthority.beginExecutionClosing({execution});if(closing?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_CLOSURE_NOT_AUTHORIZED","Staged orchestration result could not surrender execution authority into durable closure.",{reason:closing?.reason||"closure-not-authorized",executionId:execution.executionId,retryable:true});const closure=await inferenceExecutionAuthority.reconcileExecutionClosure({executionId:execution.executionId});if(closure?.closed!==true||closure?.authorized!==true)throw runtimeError("MOVIE_MENTOR_INFERENCE_EXECUTION_CLOSURE_PENDING","Execution is frozen but provider-effect reality is not terminally closable; staged result and spend remain durable for reconciliation.",{reason:closure?.reason||"closure-pending",phase:closure?.phase||"closing",executionId:execution.executionId,retryable:true});const canonical=await inferenceExecutionAuthority.commitCanonicalResult({closure,result:candidate.resultPayload});if(canonical?.authorized!==true||canonical?.committed!==true)throw runtimeError("MOVIE_MENTOR_CANONICAL_RESULT_COMMIT_INVALID","Closed execution did not produce authoritative durable canonical result evidence.",{executionId:execution.executionId,retryable:true});const settlement=await settlementAuthority.reconcile({executionId:execution.executionId});if(settlement?.authorized!==true||settlement?.settled!==true||settlement?.outcome!=="consumed")throw runtimeError("MOVIE_MENTOR_INFERENCE_SETTLEMENT_RECONCILIATION_PENDING","Canonical result exists but deterministic creator debit has not converged; result remains durable and retryable.",{executionId:execution.executionId,reservationId:canonical.reservationId,retryable:true});return resultResponse(canonical,settlement);}
export{MOVIE_MENTOR_TURN_RUNTIME_VERSION,buildRequestDigest,buildTurnEnvelopeFromDurableState,findProviderEvidence,createFencedInferenceOrchestrationDeps,openLiveExecution,acquireExistingExecution,replayTerminalTurn,recoverStagedResultTurn,runMovieMentorTurn};export default runMovieMentorTurn;
