import crypto from "node:crypto";import mongoose from "mongoose";
const VERSION="1.3.0",DOMAIN="iband.movie-mentor.result-candidate-store",SCHEMA=1,COLLECTION="movie_mentor_result_candidate",EXECUTION_COLLECTION="movie_mentor_inference_execution";let connectionPromise=null,model=null;const text=v=>typeof v==="string"?v.trim():"";const plain=v=>v&&typeof v.toObject==="function"?v.toObject():v;const iso=v=>{if(v===null||v===undefined||v==="")return "";const d=v instanceof Date?new Date(v):new Date(v);return Number.isNaN(d.getTime())?"":d.toISOString();};const stable=v=>{if(Array.isArray(v))return v.map(stable);if(v&&typeof v==="object"){const o={};for(const k of Object.keys(v).sort())o[k]=stable(v[k]);return o;}return v;};const digest=v=>crypto.createHash("sha256").update(JSON.stringify(stable(v))).digest("hex");function fail(code,message,extras={}){const e=new Error(message);e.code=code;Object.assign(e,extras);throw e;}function instant(v){const normalized=iso(v);if(!normalized)fail("MOVIE_MENTOR_RESULT_CANDIDATE_TIME_INVALID","Result candidate staging time is invalid.",{retryable:false});return new Date(normalized);}function mongoUri(){return text(process.env.MONGO_URI||process.env.MONGODB_URI||"");}
function getModel(){if(model)return model;const schema=new mongoose.Schema({domain:{type:String,required:true,immutable:true},schema:{type:Number,required:true,immutable:true},candidateReference:{type:String,required:true,immutable:true,trim:true},executionId:{type:String,required:true,immutable:true,trim:true},creatorTurnId:{type:String,required:true,immutable:true,trim:true},principalId:{type:String,required:true,immutable:true,trim:true},projectId:{type:String,required:true,immutable:true,trim:true},reservationId:{type:String,required:true,immutable:true,trim:true},requestDigest:{type:String,required:true,immutable:true,trim:true},resultDigest:{type:String,required:true,immutable:true,trim:true},resultPayload:{type:mongoose.Schema.Types.Mixed,required:true,immutable:true},stagedFromLeaseGeneration:{type:Number,min:1,required:true,immutable:true},stagedFromLeaseReference:{type:String,required:true,immutable:true,trim:true},stagedFromFencingToken:{type:String,required:true,immutable:true,trim:true},stagedAt:{type:Date,required:true,immutable:true}},{collection:COLLECTION,timestamps:true,strict:true,minimize:false});schema.index({executionId:1},{unique:true});schema.index({candidateReference:1},{unique:true});model=mongoose.models.MovieMentorResultCandidate||mongoose.model("MovieMentorResultCandidate",schema);return model;}
async function ensureConnection(){const uri=mongoUri();if(!uri)fail("MOVIE_MENTOR_RESULT_CANDIDATE_MONGO_NOT_CONFIGURED","Result candidate staging requires MONGO_URI or MONGODB_URI.");if(mongoose.connection.readyState===1)return mongoose.connection;if(!connectionPromise)connectionPromise=mongoose.connect(uri,{serverSelectionTimeoutMS:5000,maxPoolSize:10}).catch(error=>{connectionPromise=null;fail("MOVIE_MENTOR_RESULT_CANDIDATE_MONGO_UNAVAILABLE",`Result candidate Mongo unavailable: ${error instanceof Error?error.message:"Mongo connection failed."}`,{retryable:true});});await connectionPromise;return mongoose.connection;}
function normalize(row){if(!row)return null;const v=plain(row);if(v.domain!==DOMAIN||v.schema!==SCHEMA||![v.candidateReference,v.executionId,v.creatorTurnId,v.principalId,v.projectId,v.reservationId,v.requestDigest,v.resultDigest,v.stagedFromLeaseReference,v.stagedFromFencingToken].every(text)||!Number.isSafeInteger(v.stagedFromLeaseGeneration)||v.stagedFromLeaseGeneration<1||!iso(v.stagedAt)||!v.resultPayload||digest(v.resultPayload)!==text(v.resultDigest))fail("MOVIE_MENTOR_RESULT_CANDIDATE_RECORD_INVALID","Durable result candidate is malformed or digest-invalid.");return Object.freeze({candidateReference:text(v.candidateReference),executionId:text(v.executionId),creatorTurnId:text(v.creatorTurnId),principalId:text(v.principalId),projectId:text(v.projectId),reservationId:text(v.reservationId),requestDigest:text(v.requestDigest),resultDigest:text(v.resultDigest),resultPayload:Object.freeze(stable(v.resultPayload)),stagedFromLeaseGeneration:v.stagedFromLeaseGeneration,stagedFromLeaseReference:text(v.stagedFromLeaseReference),stagedFromFencingToken:text(v.stagedFromFencingToken),stagedAt:iso(v.stagedAt)});}
function createMovieMentorResultCandidateMongoStore({mongoModel=null,connect=ensureConnection,startSession=()=>mongoose.startSession(),executionCollection=null,now=()=>new Date(),randomId=()=>crypto.randomUUID()}={}){const storeModel=()=>mongoModel||getModel(),executionLedger=()=>executionCollection||mongoose.connection.collection(EXECUTION_COLLECTION);async function ready(){if(!mongoModel)await connect();}async function readByExecution(executionId){await ready();const row=await storeModel().findOne({executionId:text(executionId)}).lean().exec();return row?normalize(row):null;}
 async function stageCandidate({execution=null,resultPayload=null}={}){await ready();if(execution?.authorized!==true||!text(execution.executionId)||!text(execution.creatorTurnId)||!text(execution.principalId)||!text(execution.projectId)||!text(execution.reservationId)||!text(execution.requestDigest)||!text(execution.ownerId)||!Number.isSafeInteger(execution.leaseGeneration)||!text(execution.leaseReference)||!text(execution.fencingToken))fail("MOVIE_MENTOR_RESULT_CANDIDATE_EXECUTION_AUTHORITY_REQUIRED","Result candidate staging requires exact live execution authority.");if(!resultPayload||typeof resultPayload!=="object")fail("MOVIE_MENTOR_RESULT_CANDIDATE_PAYLOAD_REQUIRED","Result candidate staging requires a result payload.");const payload=stable(resultPayload),record={domain:DOMAIN,schema:SCHEMA,candidateReference:`result-candidate-${randomId()}`,executionId:text(execution.executionId),creatorTurnId:text(execution.creatorTurnId),principalId:text(execution.principalId),projectId:text(execution.projectId),reservationId:text(execution.reservationId),requestDigest:text(execution.requestDigest),resultDigest:digest(payload),resultPayload:payload,stagedFromLeaseGeneration:execution.leaseGeneration,stagedFromLeaseReference:text(execution.leaseReference),stagedFromFencingToken:text(execution.fencingToken),stagedAt:instant(now())};if(mongoModel||executionCollection===false){const existing=await readByExecution(record.executionId);if(existing){if(existing.resultDigest===record.resultDigest&&digest(existing.resultPayload)===record.resultDigest)return Object.freeze({...existing,idempotent:true});fail("MOVIE_MENTOR_RESULT_CANDIDATE_CONFLICT","Execution already has a different immutable result candidate.");}return normalize(await storeModel().create(record));}
 const session=await startSession();let outcome=null;try{await session.withTransaction(async()=>{const existing=await storeModel().findOne({executionId:record.executionId}).session(session).lean().exec();if(existing){const durable=normalize(existing);if(durable.resultDigest===record.resultDigest&&digest(durable.resultPayload)===record.resultDigest){outcome=Object.freeze({...durable,idempotent:true});return;}fail("MOVIE_MENTOR_RESULT_CANDIDATE_CONFLICT","Execution already has a different immutable result candidate.");}const filter={executionId:record.executionId,creatorTurnId:record.creatorTurnId,principalId:record.principalId,projectId:record.projectId,reservationId:record.reservationId,requestDigest:record.requestDigest,phase:"active",ownerId:text(execution.ownerId),leaseGeneration:execution.leaseGeneration,leaseReference:text(execution.leaseReference),fencingToken:text(execution.fencingToken),leaseExpiresAt:{$gt:record.stagedAt}};const barrier=await executionLedger().updateOne(filter,{$inc:{resultCandidateBarrierRevision:1}},{session});if(barrier?.matchedCount!==1)fail("MOVIE_MENTOR_RESULT_CANDIDATE_EXECUTION_FENCED","Result candidate cannot be staged outside the exact live execution universe.",{retryable:false});const created=await storeModel().create([record],{session});outcome=normalize(created[0]);},{readConcern:{level:"snapshot"},writeConcern:{w:"majority"}});return outcome;}catch(error){if(error?.code===11000){const durable=await readByExecution(record.executionId);if(durable&&durable.resultDigest===record.resultDigest&&digest(durable.resultPayload)===record.resultDigest)return Object.freeze({...durable,idempotent:true});fail("MOVIE_MENTOR_RESULT_CANDIDATE_CONFLICT","Concurrent staging bound a different immutable result candidate.");}throw error;}finally{await session.endSession();}}
 return Object.freeze({stageCandidate,readByExecution});}
function getMovieMentorResultCandidateMongoStoreStatus(){const configured=Boolean(mongoUri());return Object.freeze({version:VERSION,domain:DOMAIN,schema:SCHEMA,collection:COLLECTION,configured,readiness:configured?"configured":"configuration-required",authority:"zero-until-current-closure-and-canonical-finalization",atomicFence:"shared-execution-write-barrier-before-closure"});}
export{VERSION as MOVIE_MENTOR_RESULT_CANDIDATE_MONGO_STORE_VERSION,DOMAIN as MOVIE_MENTOR_RESULT_CANDIDATE_MONGO_STORE_DOMAIN,createMovieMentorResultCandidateMongoStore,getMovieMentorResultCandidateMongoStoreStatus};export default createMovieMentorResultCandidateMongoStore;
