/** Movie Mentor Creator Branch Convergence Control v1.0.0 — dormant multi-device edit convergence boundary. */
const VERSION="1.0.0",CONTROL_ID="creator-branch-convergence-control",AUTHORITY="creator-branch-convergence-contract-only";function s(v){return typeof v==="string"?v.trim():""}function i(v){return Number.isInteger(v)&&v>=0?v:null}
async function verifyCreatorBranchConvergence({incidentId,projectId,baseRevision,branches=[],expectedHeadRevision}={}, {verifyBranch=null,verifyMerge=null}={}){const reasons=[];if(!s(incidentId))reasons.push("incident_id_required");if(!s(projectId))reasons.push("project_id_required");if(i(baseRevision)===null)reasons.push("base_revision_invalid");if(!Array.isArray(branches)||branches.length<1)reasons.push("branches_required");if(i(expectedHeadRevision)===null)reasons.push("expected_head_revision_invalid");if(reasons.length)return{verified:false,reasons};if(typeof verifyBranch!=="function")return{verified:false,reasons:["trusted_branch_verifier_required"]};const seen=new Set(),proofs=[];for(const b of branches){const id=s(b?.branchId),device=s(b?.deviceId),from=i(b?.fromRevisionExclusive),to=i(b?.toRevisionInclusive);if(!id||!device||from===null||to===null||to<=from){reasons.push("branch_shape_invalid");continue}if(seen.has(id)){reasons.push("duplicate_branch_id");continue}seen.add(id);let p;try{p=await verifyBranch({incidentId,projectId,branchId:id,deviceId:device,fromRevisionExclusive:from,toRevisionInclusive:to,reference:s(b?.reference)})}catch{reasons.push(`${id}:branch_verification_failed`);continue}if(p?.valid!==true||p?.complete!==true||!s(p?.reference))reasons.push(`${id}:branch_not_fully_verified`);else proofs.push({branchId:id,deviceId:device,fromRevisionExclusive:from,toRevisionInclusive:to,reference:s(p.reference)})}if(reasons.length)return{verified:false,reasons};if(typeof verifyMerge!=="function")return{verified:false,reasons:["trusted_branch_merge_verifier_required"]};let merge;try{merge=await verifyMerge({incidentId,projectId,baseRevision,branches:proofs,expectedHeadRevision})}catch{return{verified:false,reasons:["branch_merge_verification_failed"]}};if(merge?.valid!==true||merge?.allBranchesIncluded!==true||merge?.conflictsResolved!==true||merge?.creatorIntentPreserved!==true||i(merge?.headRevision)!==expectedHeadRevision||!s(merge?.reference))return{verified:false,reasons:["creator_branch_convergence_not_proven"]};return{verified:true,reasons:[],headRevision:merge.headRevision,mergeReference:s(merge.reference),branchReferences:proofs.map(p=>p.reference)}}
function getCreatorBranchConvergenceControlManifest(){return{id:CONTROL_ID,name:"Movie Mentor Creator Branch Convergence Control",version:VERSION,status:"standalone-dormant-not-wired",authority:AUTHORITY,deterministicControl:true,aiAgent:false,failClosed:true,purpose:"Prevent multi-device or offline concurrent creator edits from being silently overwritten during disaster preservation and restoration.",requirements:["verify-each-device-branch","include-all-verified-branches","resolve-conflicts-explicitly","preserve-creator-intent","prove-authoritative-merged-head"],restrictions:["revision-number-alone-does-not-prove-convergence","cannot-drop-a-device-branch","cannot-auto-declare-conflict-resolution-without-trusted-proof","no-live-crdt-ot-editor-or-merge-adapter"]}}
export{VERSION as CREATOR_BRANCH_CONVERGENCE_VERSION,CONTROL_ID as CREATOR_BRANCH_CONVERGENCE_CONTROL_ID,AUTHORITY as CREATOR_BRANCH_CONVERGENCE_AUTHORITY,verifyCreatorBranchConvergence,getCreatorBranchConvergenceControlManifest};export default verifyCreatorBranchConvergence;
