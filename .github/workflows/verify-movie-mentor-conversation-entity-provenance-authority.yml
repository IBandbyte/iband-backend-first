name: Verify Movie Mentor Conversation Entity Provenance Authority
on:
  pull_request:
    branches: [main]
    paths:
      - "ai/MovieMentorCreatorStateTransition.js"
      - "ai/MovieMentorContinuationReferenceControl.js"
      - "scripts/verify-movie-mentor-conversation-entity-provenance-authority.mjs"
      - ".github/workflows/verify-movie-mentor-conversation-entity-provenance-authority.yml"
  push:
    branches: ["verify-conversation-entity-provenance-authority"]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-conversation-entity-provenance-authority.mjs
      - run: node scripts/verify-movie-mentor-conversation-entity-provenance-authority.mjs
