name: Verify Movie Mentor Provider Null Model Authority

on:
  push:
    paths:
      - "ai/MovieMentorProviderOperationAuthority.js"
      - "ai/StructuredAIProviderClient.js"
      - "scripts/verify-movie-mentor-provider-null-model-authority.mjs"
      - ".github/workflows/verify-movie-mentor-provider-null-model-authority.yml"
  pull_request:
    paths:
      - "ai/MovieMentorProviderOperationAuthority.js"
      - "ai/StructuredAIProviderClient.js"
      - "scripts/verify-movie-mentor-provider-null-model-authority.mjs"
      - ".github/workflows/verify-movie-mentor-provider-null-model-authority.yml"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-provider-null-model-authority.mjs
      - run: node scripts/verify-movie-mentor-provider-null-model-authority.mjs
