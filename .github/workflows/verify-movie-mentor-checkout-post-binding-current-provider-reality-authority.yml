name: Verify Movie Mentor Checkout Post-Binding Current Provider Reality Authority

on:
  pull_request:
    branches: [main]
    paths:
      - 'ai/**'
      - 'scripts/verify-movie-mentor-checkout-post-binding-current-provider-reality-authority.mjs'
      - '.github/workflows/verify-movie-mentor-checkout-post-binding-current-provider-reality-authority.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-checkout-post-binding-current-provider-reality-authority.mjs
      - run: node scripts/verify-movie-mentor-checkout-post-binding-current-provider-reality-authority.mjs
