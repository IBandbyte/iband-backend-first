name: Verify Movie Mentor Creator Compensation Suspended Entitlement Authority

on:
  pull_request:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/verify-movie-mentor-creator-compensation-suspended-entitlement-authority.mjs
