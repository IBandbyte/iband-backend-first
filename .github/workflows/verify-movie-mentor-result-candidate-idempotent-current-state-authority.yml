name: Verify Movie Mentor Result Candidate Idempotent Current State Authority
on:
  pull_request:
    branches: [main]
    paths:
      - "ai/MovieMentorResultCandidateMongoStore.js"
      - "scripts/verify-movie-mentor-result-candidate-idempotent-current-state-authority.mjs"
      - ".github/workflows/verify-movie-mentor-result-candidate-idempotent-current-state-authority.yml"
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-result-candidate-idempotent-current-state-authority.mjs
      - run: node scripts/verify-movie-mentor-result-candidate-idempotent-current-state-authority.mjs
