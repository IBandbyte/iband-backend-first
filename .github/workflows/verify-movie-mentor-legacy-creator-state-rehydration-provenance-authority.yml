name: Verify Movie Mentor Legacy Creator-State Rehydration Provenance Authority
on:
  pull_request:
    branches: [main]
    paths:
      - "ai/MovieMentorCreatorStateStore.js"
      - "ai/MovieMentorTurnRuntime.js"
      - "ai/MovieMentorContinuationReferenceControl.js"
      - "ai/MovieMentorCreatorDecisionAuthority.js"
      - "scripts/verify-movie-mentor-legacy-creator-state-rehydration-provenance-authority.mjs"
      - ".github/workflows/verify-movie-mentor-legacy-creator-state-rehydration-provenance-authority.yml"
  push:
    branches: ["verify-legacy-creator-state-rehydration-provenance"]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-legacy-creator-state-rehydration-provenance-authority.mjs
      - run: node scripts/verify-movie-mentor-legacy-creator-state-rehydration-provenance-authority.mjs
