name: Verify Movie Mentor Checkout Revocation Recovery Capability Provenance

on:
  pull_request:
    branches: [main]
    paths:
      - 'ai/**'
      - 'scripts/verify-movie-mentor-checkout-revocation-recovery-capability-provenance.mjs'
      - '.github/workflows/verify-movie-mentor-checkout-revocation-recovery-capability-provenance.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-checkout-revocation-recovery-capability-provenance.mjs
      - run: node scripts/verify-movie-mentor-checkout-revocation-recovery-capability-provenance.mjs
