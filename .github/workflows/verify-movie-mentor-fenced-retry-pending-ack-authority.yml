name: Verify Movie Mentor Fenced Retry Pending Acknowledgement Authority
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - "ai/MovieMentorCommercialProviderIngressAuthority.js"
      - "ai/MovieMentorCommercialReversalAuthority.js"
      - "scripts/verify-movie-mentor-fenced-retry-pending-ack-authority.mjs"
      - ".github/workflows/verify-movie-mentor-fenced-retry-pending-ack-authority.yml"
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-fenced-retry-pending-ack-authority.mjs
      - run: node scripts/verify-movie-mentor-fenced-retry-pending-ack-authority.mjs
