name: Verify Movie Mentor Purchase Intent Historical Schema Authority

on:
  pull_request:
    paths:
      - 'ai/MovieMentorCommercialPurchaseIntentMongoStore.js'
      - 'scripts/verify-movie-mentor-purchase-intent-historical-schema-authority.mjs'
      - '.github/workflows/verify-movie-mentor-purchase-intent-historical-schema-authority.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-purchase-intent-historical-schema-authority.mjs
      - run: node scripts/verify-movie-mentor-purchase-intent-historical-schema-authority.mjs
