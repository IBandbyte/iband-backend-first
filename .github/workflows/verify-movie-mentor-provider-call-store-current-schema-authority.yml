name: Verify Movie Mentor Provider Call Store Current Schema Authority
on:
  pull_request:
    branches: [main]
    paths:
      - "ai/MovieMentorInferenceExecutionMongoStore.js"
      - "scripts/verify-movie-mentor-provider-call-store-current-schema-authority.mjs"
      - ".github/workflows/verify-movie-mentor-provider-call-store-current-schema-authority.yml"
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-provider-call-store-current-schema-authority.mjs
      - run: node scripts/verify-movie-mentor-provider-call-store-current-schema-authority.mjs
