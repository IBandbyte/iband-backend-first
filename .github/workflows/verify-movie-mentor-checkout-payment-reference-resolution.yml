name: Verify Movie Mentor Checkout Payment Reference Resolution

on:
  pull_request:
    paths:
      - "ai/MovieMentorCommercialCheckoutBindingMongoStore.js"
      - "scripts/verify-movie-mentor-checkout-payment-reference-resolution.mjs"
      - ".github/workflows/verify-movie-mentor-checkout-payment-reference-resolution.yml"
  workflow_dispatch:

jobs:
  verify-checkout-payment-reference-resolution:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install --no-audit --no-fund --ignore-scripts
      - name: Verify checkout payment reference resolution
        run: node scripts/verify-movie-mentor-checkout-payment-reference-resolution.mjs
