name: Verify Movie Mentor Suspended Entitlement Settlement Authority

on:
  pull_request:
    paths:
      - "ai/MovieMentorInferenceSettlementMongoStore.js"
      - "scripts/verify-movie-mentor-suspended-entitlement-settlement-authority.mjs"
      - ".github/workflows/verify-movie-mentor-suspended-entitlement-settlement-authority.yml"
  workflow_dispatch:

jobs:
  verify-suspended-entitlement-settlement-authority:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install --no-audit --no-fund --ignore-scripts
      - name: Verify suspended entitlement settlement authority
        run: node scripts/verify-movie-mentor-suspended-entitlement-settlement-authority.mjs
