name: Verify Movie Mentor Recovered Provider Model Authority

on:
  pull_request:
    paths:
      - 'ai/MovieMentorRecoveredProviderResultAuthority.js'
      - 'scripts/verify-movie-mentor-recovered-provider-model-authority.mjs'
      - '.github/workflows/verify-movie-mentor-recovered-provider-model-authority.yml'
  push:
    branches:
      - movie-mentor-recovered-model-authority
    paths:
      - 'ai/MovieMentorRecoveredProviderResultAuthority.js'
      - 'scripts/verify-movie-mentor-recovered-provider-model-authority.mjs'
      - '.github/workflows/verify-movie-mentor-recovered-provider-model-authority.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax check recovered provider result authority
        run: node --check ai/MovieMentorRecoveredProviderResultAuthority.js
      - name: Syntax check verifier
        run: node --check scripts/verify-movie-mentor-recovered-provider-model-authority.mjs
      - name: Verify recovered provider model authority
        run: node scripts/verify-movie-mentor-recovered-provider-model-authority.mjs
