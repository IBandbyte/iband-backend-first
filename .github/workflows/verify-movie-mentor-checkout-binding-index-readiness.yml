name: Verify Movie Mentor Checkout Binding Index Readiness

on:
  pull_request:
    paths:
      - 'ai/MovieMentorCommercialCheckoutBindingMongoStore.js'
      - 'ai/MovieMentorProductionCommercialCheckoutComposition.js'
      - 'scripts/verify-movie-mentor-checkout-binding-index-readiness.mjs'
      - '.github/workflows/verify-movie-mentor-checkout-binding-index-readiness.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check scripts/verify-movie-mentor-checkout-binding-index-readiness.mjs
      - run: node scripts/verify-movie-mentor-checkout-binding-index-readiness.mjs
