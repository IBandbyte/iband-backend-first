name: Verify Movie Mentor Turn Conflict Bound Loser Release Authority
on:
  push:
    branches: [verify-turn-conflict-bound-loser-release-authority]
  pull_request:
    paths:
      - "ai/MovieMentorTurnRuntime.js"
      - "scripts/verify-movie-mentor-turn-conflict-bound-loser-release-authority.mjs"
      - ".github/workflows/verify-movie-mentor-turn-conflict-bound-loser-release-authority.yml"
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: node --check scripts/verify-movie-mentor-turn-conflict-bound-loser-release-authority.mjs
      - run: node scripts/verify-movie-mentor-turn-conflict-bound-loser-release-authority.mjs
