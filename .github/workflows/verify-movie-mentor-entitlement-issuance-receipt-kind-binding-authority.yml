name: Verify Movie Mentor Entitlement Issuance Receipt Kind Binding Authority
on:
  push:
    branches: [verify-entitlement-issuance-receipt-kind-binding-authority]
  pull_request:
    branches: [main]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - run: npm install
      - run: node --check scripts/verify-movie-mentor-entitlement-issuance-receipt-kind-binding-authority.mjs
      - run: node scripts/verify-movie-mentor-entitlement-issuance-receipt-kind-binding-authority.mjs
