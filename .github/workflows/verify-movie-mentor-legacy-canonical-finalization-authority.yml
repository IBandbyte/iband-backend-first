name: Verify Movie Mentor Legacy Canonical Finalization Authority
on:
  pull_request:
    paths:
      - "ai/MovieMentorCanonicalResultMongoStore.js"
      - "scripts/verify-movie-mentor-legacy-canonical-finalization-authority.mjs"
      - ".github/workflows/verify-movie-mentor-legacy-canonical-finalization-authority.yml"
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - name: Syntax check legacy canonical finalization authority
        run: |
          node --check ai/MovieMentorCanonicalResultMongoStore.js
          node --check scripts/verify-movie-mentor-legacy-canonical-finalization-authority.mjs
      - name: Verify legacy canonical finalization authority
        run: node scripts/verify-movie-mentor-legacy-canonical-finalization-authority.mjs
