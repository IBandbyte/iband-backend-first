name: Verify Movie Mentor Settlement Current Entitlement Authority

on:
  pull_request:
    branches: [main]
    paths:
      - "ai/MovieMentorInferenceSettlementMongoStore.js"
      - "scripts/verify-movie-mentor-settlement-current-entitlement-authority.mjs"
      - ".github/workflows/verify-movie-mentor-settlement-current-entitlement-authority.yml"
  workflow_dispatch:

jobs:
  verify-settlement-current-entitlement-authority:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - name: Verify settlement current entitlement authority
        run: node scripts/verify-movie-mentor-settlement-current-entitlement-authority.mjs
