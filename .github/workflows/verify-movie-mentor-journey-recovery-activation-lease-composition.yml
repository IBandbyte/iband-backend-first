name: Verify Movie Mentor Journey Recovery Activation Lease Composition

on:
  push:
    paths:
      - "ai/MovieMentorJourneyRecoveryActivationLeaseComposition.js"
      - "scripts/verify-movie-mentor-journey-recovery-activation-lease-composition.mjs"
      - ".github/workflows/verify-movie-mentor-journey-recovery-activation-lease-composition.yml"
  pull_request:
    paths:
      - "ai/MovieMentorJourneyRecoveryActivationLeaseComposition.js"
      - "scripts/verify-movie-mentor-journey-recovery-activation-lease-composition.mjs"
      - ".github/workflows/verify-movie-mentor-journey-recovery-activation-lease-composition.yml"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund --ignore-scripts
      - run: node --check ai/MovieMentorJourneyRecoveryActivationLeaseComposition.js
      - run: node --check scripts/verify-movie-mentor-journey-recovery-activation-lease-composition.mjs
      - run: node scripts/verify-movie-mentor-journey-recovery-activation-lease-composition.mjs
